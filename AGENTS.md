# 项目级 Agent 规则

## Agent 正式版发布和验收（2026-09-10 用户明确授权）

- 本项目 Agent 相关功能和修复，完成本地检查、自动化测试和发布差异核对后，直接选择性部署到正式版 main / Render ltm-web / https://ltm-web-gt13.onrender.com 进行真实管理员业务测试；不再以免费 Staging 的资源不足阻塞验收，不重复询问相同范围的正式发布授权。本规则覆盖当前灵活回答、表格、图表、进度和公开研究更新，并优先于本项目旧的 Agent 仅限 Staging 规则。
- 不整分支合并无关 Staging 功能。验收必须核对本次实际正式版部署、新问题、新答案及业务证据；旧版本的成功记录、入口可见和自动化通过不能替代新版本业务验收。
- 复用现有管理员会话；已有自动填充凭据时自行点击登录并等待结果，不读取密码，不用访客代替业务验收。
- 既有费用上限仍有效；不由本授权推导充值、升配、无限预算、删除/覆盖交易事实或真实交易权限。非 Agent 功能沿用原环境和授权规则。

## 时间统计显示

- 所有面向用户的时间统计、状态时间和统计接口时间字段，统一只保留到秒；不得显示毫秒、微秒或秒后小数。
- 新增或修改时间统计时，必须在展示/API边界统一截断到秒；底层审计时间如保留更高精度，不得把该精度直接暴露给业务统计。
