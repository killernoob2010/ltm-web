# 版本更新记录

## 2026-07-22 Staging 旧台账管理第一阶段退役
- 环境与版本：仅 Staging Render `ltm-web-staging`；功能提交 `b5fd8ef` 已推送 `staging` 并完成真实页面验收。未操作 `main`、Production Render、正式数据库、环境变量或业务数据。
- 退役范围：侧边栏隐藏整个旧“台账管理”分组及上海钧能、钢材出口、子公司套保、期权交易四个旧入口；新“交易管理”的总览、持仓与交易、上海钧能台账、期权台账、汇总与导出五个入口保持不变。旧上海钧能历史查询继续保留，新增、编辑、删除、平仓、价格刷新、手动价格和导出统一返回 `410` 退役提示，避免产生双台账写入。
- 数据边界：不删除旧代码、旧路由或旧数据库表，不做迁移或历史数据清理。部署后只读核对 Staging 旧表行数保持 `sh_junneng_trades=1`、`sh_junneng_positions=1`、`sh_junneng_close_trades=2`，与部署前一致。
- 验证结果：本地 Python 419 项、Node 106 项全部通过，两个前端脚本语法检查和 `git diff --check` 通过。真实 Staging 管理员页面确认旧分组不存在、新交易管理五个入口均存在；点击期权台账后持仓页与数据正常加载，页面标题正确，浏览器控制台无 error/warn。
- 回滚与发布边界：代码可回滚到 `061615b`；回滚不涉及数据库。Production 发布仍需单独 Gate B 明确确认。

## 2026-07-22 Staging 期权台账顶部提示移除
- 环境与版本：仅 Staging Render `ltm-web-staging`；提交 `11a6e77` 已推送 `staging`，页面加载 `/static/trading_management.js?v=20260722b`。未操作 `main`、Production Render、数据库、环境变量或业务数据。
- 页面调整：删除期权台账页签右侧“仅展示已完成业务归属的数据；页面每15秒刷新……”整段辅助提示，避免挤压“当前持仓 / 平仓记录 / 全部交易”按钮布局；保留“上次更新时间”和“更新状态”。不修改行情、估值、IV、Greeks、刷新周期或业务归属逻辑。
- 验证结果：交易管理目标前端 22 项、Node 全量 106 项通过，JavaScript 语法与 `git diff --check` 通过。真实 Staging 页面确认提示文字不存在、三个页签按钮在同一行、更新时间与状态正常显示，页签可切换，浏览器控制台无 error/warn。
- 回滚与发布边界：代码可回滚到 `bd93e8f`；Production 发布仍需单独确认。

## 2026-07-22 Staging 订单融资集港状态与两日风险升级
- 环境与版本：仅 Staging Render `ltm-web-staging`；功能提交 `ab30c61` 已推送 `staging`，页面加载带 `order-finance-port-status-20260722` 标识的订单融资 CSS/JavaScript。未操作 `main`、Production Render、正式数据库、正式环境变量或正式业务数据。
- 状态与交互：订单融资状态链调整为“待放款 → 已放款待集港 → 已集港待装船 → 已装船待交单 → 已交单待回款 → 已回款待结案 → 已完成”。新增集港确认、撤销、实际集港日与操作审计字段；上端筛选和汇总新增“已集港待装船”。已集港记录仍可确认装船，装船后沿用原有交单、回款逻辑。
- 风险规则：已集港且距离最迟装船期大于 2 天为中风险；距离小于等于 2 天、当天、逾期或最迟装船期缺失为高风险，并提示“确认装船状态或交单状态”。确认装船或已有交单日后退出集港规则，按既有后续阶段规则计算。
- 验证结果：本地 Python 417 项、Node 106 项全部通过，两个前端脚本语法检查和 `git diff --check` 通过。真实 Staging 管理员页面完成 8 项 T2 验收：`Y-2026-18` 从已放款待集港流转为已集港待装船时保持中风险，并可继续确认装船进入已装船待交单；`Y-2026-12` 在最迟装船期已逾期时确认集港后显示高风险及目标提醒。两笔临时状态变更均已逐步撤销，最终汇总恢复为已放款待集港 8、已集港待装船 0、已装船待交单 0；控制台无 error/warn。
- 数据库与回滚：部署仅幂等新增 3 个可空集港确认字段，不改写 WPS 事实字段或同步架构。代码可回滚到功能前的 `ecb445f`；新增可空字段可安全保留。Production 发布仍需单独 Gate B 明确确认。

## 2026-07-22 Staging 期权行情刷新状态与持仓表精简
- 环境与版本：仅 Staging Render `ltm-web-staging`；功能提交 `ecb445f` 已推送 `staging`，页面加载 `/static/trading_management.js?v=20260722a` 与同版本样式。未操作 `main`、Production Render、正式 Supabase、正式环境变量或正式业务数据。
- 问题原因：页面原有 15 秒轮询和后端行情请求实际正常，但非交易时段或合约无新成交时返回值不变，页面又没有显示最近检查时间与“行情无变化”状态，用户无法判断自动刷新是否执行。
- 页面调整：期权当前持仓增加“上次更新时间”和“更新状态”，可区分数据已更新、已检查但行情无变化、部分行情已更新、参考结算行情、行情不可用和更新失败；删除标的、标的价格、到期日、估值日四列，仅保留期权合约估值和风险字段。明细 Delta / Gamma / Theta / Vega 明确为带买卖方向的单手口径，不乘持仓手数或合约乘数；组合汇总按单手值乘剩余手数，浮动盈亏仍按手数和合约乘数计算。
- 验证结果：Python 全量 414 项、Node 全量 105 项通过，`node --check frontend/trading_management.js` 和 `git diff --check` 通过。真实 Staging 页面更新时间从 `08:20:23` 推进到 `08:20:41`；后续行情变化时推进到 `08:21:04` 并显示“数据已更新”，`i2609-c-800` 的估值价、IV 和四项 Greeks 同步变化。表头为 13 个目标字段，四个已删除字段均未出现。
- 回滚与发布边界：代码可回滚到 `ddb8e50`；本次无数据库迁移、无事实数据写入、无交易操作。Production 发布仍需单独确认。

## 2026-07-21 Staging 期权 Black-76 估值口径修正
- 环境与版本：仅 Staging Render `ltm-web-staging`；功能提交 `0bcc7ce` 已推送 `staging` 并完成真实页面验收。未操作 `main`、Production Render、正式 Supabase、正式环境变量或正式业务数据。
- 修正内容：实时行情来源、估值价优先级、15 秒页面刷新、10 秒后端缓存、合约到期日、乘数和浮动盈亏口径保持不变；商品期货期权的 IV、Delta、Gamma、Theta、Vega 改为使用同一行情快照在本地按 Black-76 计算，不再直接采用天勤 Black-Scholes Greeks。结算单参考行情继续只作降级展示，不伪装实时 Greeks。
- 验证结果：Python 全量 414 项、Node 全量 104 项通过，`py_compile` 与 `git diff --check` 通过。真实 Staging 管理员页面 `i2609-c-800` 显示估值价 `2.3`、标的价 `749`、IV `19.5%`、Delta `0.1179`、Gamma `0.0049`、Theta `-0.1437`、Vega `0.4123`；页面标题、目标交互和控制台均正常。间隔 16 秒复读时行情未变化，因此数值保持一致。
- 回滚与发布边界：代码可回滚到 `19cb00c`；本次无数据库迁移、无数据写入、无交易操作。Production 发布仍需单独确认。

## 2026-07-21 Staging 期权实时口径与英文 Greeks 修正
- 环境与版本：仅 Staging Render `ltm-web-staging`；功能提交 `1b69049` 已推送 `staging`，测试页面加载 `/static/trading_management.js?v=20260721a`。未操作 `main`、Production Render、正式 Supabase、正式环境变量或正式业务数据。
- 问题原因：测试环境此前未配置 `TQSDK_USERNAME` / `TQSDK_PASSWORD`，`i2609-c-800` 的估值价实际为 2026-07-20 结算单参考价 `3.2`，标的价为结算参考值 `757.5`，不是最新成交价。页面虽每 15 秒请求一次，但后端始终返回相同结算快照；旧实现又使用该快照计算 IV 和 Greeks，导致历史估值被误认为实时值。前端测试还错误地把中文 Greek 表头当作验收条件。
- 修正口径：表格、组合汇总和总览统一使用 `Delta / Gamma / Theta / Vega` 英文名称并保持四位小数。最新成交价优先、买一卖一中间价次之、结算价仅作参考的估值顺序不变；未取得实时行情时保留结算参考价、标的参考价和估值日，但 IV 与 Greeks 返回空值，不再用历史结算快照伪装实时风险值。页面明确提示每 15 秒刷新及参考行情边界。
- 验证结果：Node 交易管理前端 21 项、Python 交易管理与估值 102 项通过，`git diff --check` 通过。Staging 已通过环境变量配置只读天勤认证，环境更新部署 `dep-d9fg835aeets73bvu23g` 于 13:32 live；未读取或记录凭据值。服务端真实业务查询确认 `i2609-c-800` 为实时状态，最新成交价 `2.2`、标的价 `746.5`、IV `19.6874%`、Delta `0.1130`、Gamma `0.0047`、Theta `-0.1436`、Vega `0.3994`。同一行情会话间隔 16 秒复测时，期权无新成交所以最新价与成交时间不变，标的价由 `746.0` 更新为 `746.5`，IV 与四项 Greeks 同步变化，确认 15 秒页面刷新与 10 秒后端缓存能够取得新行情。
- 回滚与发布边界：代码可回滚到 `347b55e`；本次不修改数据库、事实数据、上海钧能计算或任何交易接口。Production 发布及正式环境变量仍需单独确认。

## 2026-07-20 Staging 过期期权快照与四位小数修正
- 环境与版本：仅 Staging Render `ltm-web-staging` / Supabase `LTM WEB STAGING`；功能提交 `0874614` 已推送 `staging`，测试页面加载 `/static/trading_management.js?v=20260720i`。未操作 `main`、Production Render、正式 Supabase、正式环境变量或正式业务数据。
- 问题原因：当前最新 TXT 持仓快照仍停留在 2026-06-30，`i2608` 期权已于 2026-07-16 到期，但旧接口仅按快照日计算历史估值，没有再按当前北京时间判断合约是否已过期，因而把历史快照误显示为当前风险。
- 修正口径：已超过到期日的期权仍保留持仓行用于核对来源，并在到期日标记“已到期”；不再展示或汇总估值价、标的价格、IV、浮动盈亏及 Greeks。系统不自行推断行权或放弃，真实了结状态以后续 TXT 结算单为准。未到期持仓及组合汇总的德尔塔、伽马、西塔和维伽统一固定显示四位小数。
- 验证结果：本地 Python 全量 405 项、Node 全量 104 项通过，`compileall`、`node --check frontend/trading_management.js` 和 `git diff --check` 通过。Staging 真实页面显示 14 条已归属持仓，其中 11 条 `i2608` 均标记已到期且六项当前估值/风险数值为空；3 条 `i2609` 继续显示估值和四位小数 Greeks，例如 `i2609-p-700` 为 0.1657、-0.0048、0.1261、-0.6846。组合汇总仅含未到期持仓：浮盈 -63,986.31、德尔塔 69.7975、伽马 -2.0888、西塔 59.5939、维伽 -309.2884；控制台无 error/warn。
- 回滚与发布边界：代码可回滚到 `993a4d0`；本次不修改数据库、事实数据或上海钧能计算。Production 发布仍需单独确认。

## 2026-07-20 Staging 期权希腊字母单位修正
- 环境与版本：仅 Staging Render `ltm-web-staging` / Supabase `LTM WEB STAGING`；功能提交 `40ce8cb` 已推送 `staging`，测试页面加载 `/static/trading_management.js?v=20260720h`。未操作 `main`、Production Render、正式 Supabase、正式环境变量或正式业务数据。
- 问题原因：旧页面直接展示持仓风险敞口，按单位 Greek 乘买卖方向、剩余手数和合约乘数；同时西塔仍为年化原值、维伽仍按波动率变化 100% 计量，因此出现几千至几百万且无单位说明的数值。
- 修正口径：持仓明细改为带买卖方向的单手价格敏感度，不乘持仓手数或合约乘数；德尔塔、伽马直接使用单位值，西塔按年化原值除以 360 表示每日变化，维伽除以 100 表示 IV 变化 1 个百分点。组合汇总按修正后的单手值乘剩余手数聚合，不乘合约乘数。
- 页面结果：表头统一为“德尔塔 / 伽马 / 西塔 / 维伽”，删除“敞口”和英文名称。Staging 14 条已归属期权持仓均有完整数值，示例 `i2608-c-780` 卖出持仓为德尔塔 -0.172574、伽马 -0.007999、西塔 0.260284、维伽 -0.402898；组合汇总为德尔塔 69.369638、伽马 -6.645119、西塔 202.482797、维伽 -535.868832。
- 验证结果：本地 Python 全量 405 项、Node 全量 103 项通过，`compileall`、`node --check frontend/trading_management.js` 和 `git diff --check` 通过；Staging 加载 14 条持仓且表格无空值、无英文 Greek 表头、无“敞口”表头，控制台无 error/warn。
- 回滚与发布边界：代码可回滚到 `b6aa3cd`；本次不涉及数据库迁移、事实数据修改或上海钧能计算调整。Production 发布仍需单独确认。

## 2026-07-20 Staging 业务台账估值展示修正
- 环境与版本：仅 Staging Render `ltm-web-staging` / Supabase `LTM WEB STAGING`；功能提交 `17ef391` 已推送 `staging`，测试页面加载 `/static/trading_management.js?v=20260720g`。未操作 `main`、Production Render、正式 Supabase、正式环境变量或正式业务数据。
- 上海钧能：结算规则版本继续由后端内部返回和记录，页面删除规则版本列、规则代号及规则说明；本月平仓页仍展示平仓盈亏、资金利息、80%结算金额、20%结算金额和手续费。
- 期权估值：未配置实时行情时，按同一 TXT 结算快照关联期权结算价和标的期货结算价，在接口响应层计算估值日、到期日、IV 及持仓 Greek 敞口；不写入交易事实表。页面删除估值来源和估值状态，保留估值价、标的价格、IV、浮动盈亏、Delta/Gamma/Theta/Vega 敞口和估值日。
- 验证结果：本地 Python 全量 402 项、Node 全量 103 项通过，`compileall`、`node --check frontend/trading_management.js` 和 `git diff --check` 通过。Staging 真实页面显示 14 条已归属期权持仓且上述估值字段无空白，示例 `i2608-c-780` 为估值价 2.9、标的价 748、IV 20.26%、估值日 2026-06-30；上海钧能本月平仓汇总为平仓盈亏 61,210.28、资金利息 258.74、80% 48,761.19、20% 12,190.35、手续费 2,989.72，页面无规则版本字段或代号，控制台无 error/warn。
- 回滚与发布边界：代码可回滚到 `33a03bc`；本次不涉及数据库迁移或事实数据修改。Production 发布仍需单独确认。

## 2026-07-20 Staging 结算单导入与期权统一了结记录
- 环境与版本：仅 Staging Render `ltm-web-staging` / Supabase `LTM WEB STAGING`；已将验收候选 `074b840` 快进推送到 `staging`，测试页面已加载 `/static/trading_management.js?v=20260720b`。未操作 `main`、Production Render、正式 Supabase、正式环境变量或正式业务数据。
- 导入与去重：交易管理改为单个 TXT 结算单导入，自动识别日结单和月结单，按账单期间、事实身份和当前版本防止日结/月结重复；月结优先作为完整当期事实，日结用于补充期初连续性。
- 期权了结：普通平仓、行权、履约和到期放弃统一进入原有“平仓记录”，仅新增“了结类型”列；期权事件的计算平仓价为 0，买方到期损失全部权利金，卖方到期获得全部权利金。行权/履约只关联同日、行权价一致的实际导入标的期货开仓，绝不虚构成交；无法可靠匹配时保留原始事件并标记待核验。
- 数据库与恢复点：部署前在 Staging 私有 `codex_backups` schema 建立 `trading_close_facts_before_option_lifecycle_20260720`（4,702 行）和 `trading_statement_exercises_before_option_lifecycle_20260720`（0 行）。启动迁移只幂等新增可空字段、了结类型默认值及关联表；部署后原平仓事实仍为 4,702 行，原始行权事件和标的关联均为 0，无旧数据改写。相关三表均启用 RLS，仅 `postgres` / `service_role` 有表权限。
- 验证结果：本地 Python 全量 372 项、Node 全量 99 项通过，`compileall`、`node --check frontend/trading_management.js` 和 `git diff --check` 通过。真实样本回归确认 6 月普通平仓 2,351 条 + 到期放弃 1 条 = 统一了结 2,352 条；`i2607-p-750` 买方剩余 100 手以 0 了结，计算期权腿损益 -18,000，账单原始事件损益 0 单独保留，未生成虚构平仓交易。Staging 实际页面保留原有页签，“平仓记录”出现“了结类型”、“了结手数”和“成交平仓手数”，控制台无 error/warn。
- 回滚与发布边界：代码阻断时将 Staging 回滚到 `4675c6b`；新增字段和空关联表向后兼容，代码回滚时可保留，如确需数据级恢复再使用上述精确备份，禁止直接全表覆盖。Production 发布仍需单独 Gate B 确认、正式库备份和正式环境验收。

## 2026-07-17 Production 铁矿石期现改为 Staging 快照跟随
- 环境与版本：已将 Staging 验收候选 `2b54890` 快进发布到 `main`；Production Render `ltm-web` 代码部署 `dep-d9ctqtjeo5us73b5fkkg` 和环境部署 `dep-d9cts1vavr4c73ah3vo0` 均已 live。
- 同步架构：Staging 保持唯一 EBC / 新浪数据源，Production 改为 `snapshot_follower`，每 5 分钟读取受保护的 Staging 期现快照；快照校验版本、数量、哈希、重复键和审批元数据，既有业务键内容不一致时整包拒绝，只在单事务内追加缺失的结果与明细。未建立跨库连接，Production 未配置 EBC 凭据，复用既有共享密钥且发布过程未读取或展示密钥。
- 生产备份：发布前完整备份位于 `/Users/wangjingze/Documents/轻量化交易管理系统生产备份/20260717-140207`；`full.dump` 为 PostgreSQL custom dump，SHA-256 为 `a5cb7f51f132860491ef9f17170f725018cf13534615681ad8a93a0321ad87d3`，同目录期现结果和明细 CSV 各 60,424 行数据，分别保留发布前正式库状态。
- 数据结果：正式库由 2026-07-10 自动更新到 2026-07-16，首次快照批次状态为 `success`，覆盖 2026-07-13 至 2026-07-16，共写入 384 组、跳过 0 组、无错误码。结果和明细均由 60,424 增至 60,808 行，最新日 96 组，重复业务键和孤儿明细均为 0。
- 验证结果：本地 Python 全量 345 项、Node 全量 98 项通过，`compileall`、JavaScript 语法和 `git diff --check` 通过；自然轮询后结果/明细行数保持 60,808，快照版本仍为同一条成功记录，未重复写入。Production 访客“期现数据展示”显示最新数据日期 2026-07-16，与数据库一致，控制台无 error/warn。
- 回滚点：代码可回滚到发布前 `49df56d`；同步异常时先关闭 `IRON_ORE_BASIS_AUTO_SYNC_ENABLED`，本次无数据库结构迁移。新增数据为隔离的追加记录，只有确认需要数据级恢复时才依据上述备份制定精确回滚，禁止直接全表删除或覆盖正式业务数据。

## 2026-07-17 Production 预警触发时间统一为北京时间
- 环境与版本：Staging 与 Production 均已发布最终候选 `d408f99`；Production Render `ltm-web` 加载 `/static/app.js?v=risk-alert-beijing-time-v2-20260717`，`/api/health` 返回 `{"status":"ok"}`。本次为纯前端修复，不修改环境变量、数据库结构或正式业务数据。
- 修复内容：预警时间统一按数据库 UTC 语义转换为 `Asia/Shanghai`，兼容带偏移的新 PostgreSQL 时间和无偏移的旧 SQLite 迁移历史，并显示到秒。正式库样本 `2026-07-17 06:09:33.023732+00` 经线上资源转换为 `2026-07-17 14:09:33`；历史数据不做写回。
- 验证结果：本地 Python 全量 337 项、Node 全量 98 项通过，JavaScript 语法和 `git diff --check` 通过；Staging 与 Production 均加载最终资源。Production 服务健康，访客期现展示可正常进入且控制台无 error/warn。
- 期现状态：Staging 数据库与展示页均为 2026-07-16；Production 数据库与展示页仍为 2026-07-10、该日 96 组，且同步批次记录为 0。独立正式同步任务尚未落库，本次不并发配置 EBC、不手工同步，也不把正式期现问题标记为已修复。
- 回滚点：如前端时间显示异常，回滚到本轮发布前 `792dd2b`；本次无数据库回滚项。

## 2026-07-17 Staging 预警触发时间统一为北京时间
- 环境与版本：仅 Staging Render `ltm-web-staging`，首版功能提交 `26c1a25` 已部署为 live；最终候选补齐旧历史 UTC 兼容并加载 `/static/app.js?v=risk-alert-beijing-time-v2-20260717`。本阶段未修改 Production、正式环境变量或正式业务数据。
- 修复内容：新 PostgreSQL 预警时间带 UTC 偏移，旧 SQLite 迁移历史为无偏移 UTC 字符串；旧前端仅截断毫秒和偏移符号，导致页面少显示 8 小时。预警格式器现在将两类数据库时间都转换到 `Asia/Shanghai` 并显示到秒，共享格式器对其它无时区业务时间保持原值；不改写数据库历史时间。
- 验证结果：本地 Python 全量 337 项、Node 全量 98 项通过，JavaScript 语法和 `git diff --check` 通过；覆盖带偏移 UTC、无偏移历史 UTC、跨日和已有 `+08:00`。真实 Staging 期现展示最新日期为 2026-07-16，与 Staging 数据库一致，页面加载新资源且控制台无 error/warn；访客无期现管理和风险预警权限，对应功能由接口、数据库与自动化测试覆盖。
- 发布边界：Production 期现数据在本次检查时仍停留 2026-07-10，等待独立正式同步任务完成，不并发手工补写。Production 发布继续使用用户本轮明确授权，但必须在同步任务结束后再部署和验收。

## 2026-07-17 Production 预警历史按规则分组与汇总栏布局正式发布
- 环境与版本：已将 Staging 最终候选 `6b7d02d` 快进发布到 `main`，Production Render `ltm-web` / `https://ltm-web-gt13.onrender.com` 已加载 `/static/app.js?v=risk-alert-history-grouping-20260717` 与 `/static/styles.css?v=risk-alert-summary-layout-20260717`，`/api/health` 返回 `{"status":"ok"}`；未修改正式环境变量。
- 发布内容：普通用户只能查看和操作本人设置的规则与历史，管理员可查看、筛选和管理全部设置人的规则及历史，但不会收到他人规则通知；管理员代操作继续写入操作日志。预警历史只展示至少触发过一次的规则，以规则为第一层汇总当前值、预警值、次数、未读数、最近方向和最近时间，规则层每页 10 条，展开后详情每次加载 20 条；已触发规则删除后保留归档历史，汇总层可删除整组历史。
- 页面调整：风险预警保持规则在上、历史在下的单列布局；规则复选框为 `12 × 12`，历史汇总行压缩左侧间距，展开/收起与删除历史记录按钮上下排列且文字横向单行；所有预警时间显示到秒。
- 生产备份：发布前完整备份位于 `/Users/wangjingze/Documents/轻量化交易管理系统生产备份/20260717-140207`，`full.dump` 为 9,824,089 字节、`schema.sql` 为 322,524 字节，`pg_restore --list` 1,029 项，完整 dump SHA-256 为 `a5cb7f51f132860491ef9f17170f725018cf13534615681ad8a93a0321ad87d3`；同目录保留 16 张核心表 CSV。其他 0 字节 dump 目录均为失败尝试，不作为备份使用。
- 数据库迁移：应用启动已幂等增加可空 `alert_settings.archived_at`，并创建 `alert_history(alert_id, alert_time DESC, id DESC)` 查询索引；发布后确认字段可空、索引存在、孤儿历史为 0。发布前为 1 条规则、175 条历史；发布后为 1 条规则、176 条历史，新增 1 条来自正式后台扫描的正常触发，验收过程未手工扫描、模拟、确认、删除或改写任何正式预警业务数据。
- 验证结果：最终候选 Python 全量 337 项、Node 全量 98 项全部通过，`compileall`、全部前端 JavaScript 语法及 `git diff --check` 通过。正式页面管理员只读验收确认设置人筛选、规则汇总、详情展开、20/176 加载分页、秒级时间、12px 复选框和 114px 纵向操作区均生效，控制台无 error/warn。
- 回滚点：代码阻断时回滚到本轮发布前 `4c28cbd`；新增可空列和查询索引可安全保留，不直接删除现有业务历史。仅在确需数据库级恢复时使用上述备份。

## 2026-07-17 Staging 风险预警汇总栏紧凑布局
- 环境与版本：仅 Staging 测试版 Render `ltm-web-staging`，功能提交 `f9d4106` 已部署；页面加载 `/static/styles.css?v=risk-alert-summary-layout-20260717`。未操作 `main`、Production Render、正式 Supabase、正式环境变量或正式业务数据。
- 页面调整：预警规则全选和行选择复选框统一为数据展示同款 `12 × 12`；预警历史汇总行压缩规则列、指标列间距和左侧内边距，为操作区保留稳定宽度；“展开 / 收起”和“删除历史记录”改为上下排列，按钮文字保持横向单行。
- 验证结果：风险预警与数据展示相关 Node 前端回归 36 项全部通过，`git diff --check` 通过。真实 Staging 在 1280 × 720 视口下确认复选框计算尺寸为 12 × 12，操作列宽 114px、方向为纵向，两个按钮均为横向单行，页面客户区与滚动宽度同为 1030px、无横向溢出；展开后状态切换为“收起”，再次点击恢复，控制台无 error/warn。
- 影响与回滚：本次只修改前端 CSS、样式资源缓存版本和前端契约测试，不改 JavaScript 业务逻辑、API、数据库、权限、通知或历史分页。代码问题可回滚到 `f9d4106` 的前一提交 `ea0efef`。Production 发布仍需单独 Gate B 明确确认。

## 2026-07-17 Staging 预警历史按规则分组、个人工作区与管理员管理
- 环境与版本：仅 Staging 测试版 Render `ltm-web-staging` / Supabase `LTM WEB STAGING`；`staging` 已部署提交 `d5c275d`，页面加载 `/static/app.js?v=risk-alert-history-grouping-20260717` 与同版本样式资源，`/api/health` 返回 200。未操作 `main`、Production Render、正式 Supabase、正式环境变量或正式业务数据。
- 权限与通知：普通用户进入风险预警后可执行全部日常操作，但规则、历史、手动扫描和单条确认均只作用于本人数据；管理员可以查看全部规则和历史、按设置人筛选并代为编辑、启停、模拟、删除和确认，代操作写入包含原设置人用户 ID 的操作日志。通知仍严格只发送给规则设置人，管理员不会收到他人预警。
- 历史与生命周期：预警历史只展示至少触发过一次的规则，汇总层显示最近当前值、预警值、预警次数、未读数、最近方向和最近时间；规则层默认每页 10 条，展开后详情默认加载 20 条并可继续加载。删除已触发规则改为停用并归档，历史继续显示“规则已删除”；汇总层可删除整组历史，活动规则保留，归档规则清空最后历史后同步删除墓碑。所有预警时间显示到秒。
- 数据库影响：`alert_settings` 幂等增加可空 `archived_at`，`alert_history` 增加 `(alert_id, alert_time DESC, id DESC)` 查询索引；迁移后真实 Staging 已确认字段和索引存在。验收前后均为 1 条业务规则、1 条业务历史、0 条孤儿历史；两个临时账号、14 条临时规则及 39 条临时触发记录已按唯一前缀精确清理，临时账号和规则残留均为 0，管理员代操作审计保留。
- 验证结果：本地 Python 全量 337 项、Node 全量 97 项通过，`compileall`、全部前端 JavaScript 语法和 `git diff --check` 通过。真实 Staging 三账号验收确认：普通用户 A 的 12 条规则按 10 + 2 分页，单规则 25 条详情按 20 + 5 加载；普通用户 B 仅看到自己的 2 条规则；管理员按 B 筛选为 2 条、管理员通知为 0，代改日志存在；已触发规则归档保史、删除归档历史移除墓碑、删除活动规则历史保留规则均通过。浏览器确认管理员筛选、汇总字段、分页、详情展开、秒级时间、静态资源和控制台无 error。
- 回滚与发布边界：代码阻断时回滚到本功能前的 `4c28cbd`；新增可空列和索引可安全保留，不直接删除现有业务历史。Production 发布仍需单独 Gate B 明确确认、正式库备份和正式环境验收。

## 2026-07-17 Production 风险预警按设置人定向提醒正式发布
- 环境与版本：已将验收通过的 `staging` 候选合并到 `main`，Production Render `ltm-web` 已切换到合并提交 `953ced9`；正式页面加载 `/static/app.js?v=risk-alert-owner-notification-20260717` 与同版本样式资源，`/api/health` 返回 200。
- 发布内容：风险预警新规则绑定当前设置人的用户 ID 和姓名；未读通知、单条确认和全部确认仅对设置人本人有效，其他人员不会收到，也不能代确认。预警规则和预警历史改为单列上下布局，规则列表显示“设置人”，移除“提醒人员”输入，预警时间显示到秒。
- 生产备份：发布前完整备份位于 `/Users/wangjingze/Documents/轻量化交易管理系统生产备份/20260717-085603`，其中 `full.dump` 约 9.4 MB、`schema.sql` 约 315 KB，`pg_restore --list` 1,029 项，完整 dump SHA-256 为 `152cfe9459e6d2e90b467db69c0274bf8da11403f88c1011a386e555346c2e08`；同目录保留 16 张核心表 CSV。失败产生的 0 字节文件不作为备份使用。
- 数据库迁移：应用启动已幂等增加可空 `alert_settings.creator_user_id`；正式库发布前后风险预警规则均为 1 条，现有规则已唯一绑定设置人。存量规则只有在创建人姓名唯一匹配用户时回填，无法确认归属的规则保持空值，不广播给所有人。
- 验证结果：合并结果 Python 全量 323 项、Node 全量 96 项通过，`compileall`、全部前端 JavaScript 语法及 `git diff --check` 通过。Production 双账号临时规则验收确认设置人收到提醒、另一账号未收到且代确认返回 404、设置人确认后通知消失；临时规则和历史已精确删除。正式库复核为规则 1 条、已绑定 1 条、临时规则残留 0、孤立历史 0；线上 HTML 结构、正式 CSS 响应和静态资源版本均包含上下布局与“设置人”改动。
- 缓存与回滚：部署切换瞬间验收浏览器曾缓存旧样式内容，正式服务器随后已返回包含 `.risk-alert-stack` 单列 grid 的新 CSS；如个别已打开页面仍显示左右布局，使用 `Cmd+Shift+R` 强制刷新。代码阻断时回滚到发布前 `f43face`；新增可空列可安全保留。只有确需数据库恢复时才使用上述备份，禁止直接删除正式业务数据。

## 2026-07-17 Staging 风险预警按设置人定向提醒与页面精简
- 环境：仅 Staging 测试版 Render `ltm-web-staging` / Supabase `LTM WEB STAGING`，代码来源为 `staging`；未操作 `main`、Production Render、正式 Supabase、正式环境变量或正式业务数据。
- 发布内容：风险预警规则创建时保存当前登录用户 ID 和姓名，未读通知、单条已读及全部已读均按创建者用户 ID 隔离；其他人员不会收到该规则的提醒，也不能代替设置人确认该条提醒。规则列表保留共享查看，并把“创建人”明确显示为“设置人”；历史列表仅对设置人显示确认入口。
- 布局与时间：预警规则和预警历史由左右两列改为单列上下排列；移除容易造成广播误解的“提醒人员”输入；预警时间统一截取并显示到秒，不显示毫秒或更长的小数部分。
- 数据库兼容：`alert_settings` 增加可空 `creator_user_id`。新规则直接绑定当前登录账号；存量规则仅在创建人姓名能够唯一匹配一个用户时自动回填，无法唯一识别的旧规则保持空值，不错误广播给所有人。代码回滚时该兼容性可空列可以保留。
- commit / deploy：设计 `c8dbdfa`、实施计划 `2e7d4a8`、创建者绑定 `7c9ef22`、通知隔离 `fc4edd2`、页面精简 `6aca1d6`、资源断言 `756abe8`、历史确认入口保护 `47e83a0`；均已推送 `staging`。测试版已加载 `/static/app.js?v=risk-alert-owner-notification-20260717` 与同版本样式资源。
- 验证结果：本地 Python 全量 323 项、Node 全量 96 项通过，`compileall`、JavaScript 语法及 `git diff --check` 通过。真实 Staging 双账号临时规则验收确认：创建者 ID/姓名正确落库，设置人收到通知，另一账号未收到且代确认返回 404，设置人确认后通知消失；临时规则与历史已删除。线上 DOM/CSS 确认规则与历史为单列上下结构、“设置人”列存在、旧提醒人员输入不存在；页面标题与静态资源正确，控制台无 error/warn。
- 验收限制与发布边界：浏览器控制通道登录后无法触发侧栏页面切换，因此不声称完成风险预警页的人工点击可视化验收；本轮由真实 Staging 双账号 API、线上 DOM/CSS 和本地前后端回归共同覆盖。Production 发布仍需 Gate B 单独明确确认。

## 2026-07-17 Production 铁矿石期现自动同步与页面刷新代码发布
- 环境：Production Render `ltm-web` / `https://ltm-web-gt13.onrender.com`，发布分支为 `main`；Staging 验收版本与 Production 既有订单融资修复合并后发布，生产提交为 `fbd043d`。
- 发布内容：正式版加入铁矿石期现 EBC / 新浪增量取数、版本化计算规则、同步批次审计与上海时间 09:30、10:30、21:30 自动调度代码；期现管理和展示页面每次进入时重新读取最新数据日期，筛选状态保持不变，并移除最优仓单区域重复的“数据截至”日期。
- 差异处理：保留 Production 已上线的订单融资单一 WPS 源、还款风险和凭据恢复修复，同时合入 Staging 的期现模块；冲突仅涉及两个调度器启动顺序、静态资源版本、订单融资令牌恢复测试和发布记录，均按保留两边有效逻辑处理。
- 是否涉及数据库：应用启动会幂等创建期现同步批次和来源点表及兼容性字段；本次未手工修改正式数据库、未导入或改写正式业务数据，也未手工触发期现同步。未读取或修改 Production 的 `IRON_ORE_BASIS_AUTO_SYNC_ENABLED`、EBC 凭据等环境变量，自动同步仅在正式环境已具备配置且开关为 `true` 时运行。
- 验证结果：合并后本地 Python 317 项、Node 92 项全部通过，Python 编译、全部 JavaScript 语法和 `git diff --check` 通过。2026-07-17 08:14 CST 正式版 `/api/health` 返回 `{"status":"ok"}`，页面加载 `iron-ore-basis-auto-sync-20260714` CSS 和 `iron-ore-basis-date-refresh-20260716` JavaScript；访客进入期现数据展示后显示最新数据日期 2026-07-10，最优仓单数据正常，重复日期元素不存在，控制台无 warning/error。
- 已知边界与回滚：本次确认代码和页面发布成功，但未核验或调整 Production 自动同步环境配置，正式数据日期仍为发布前的 2026-07-10。若代码出现阻断问题，可回滚到发布前 `fb125da`；数据库新增对象为兼容性增量，不执行直接删除或正式数据回退。

## 2026-07-16 Staging 交单后回款风险与双库 WPS 同步保护
- 环境：仅 Staging 测试版 Render `ltm-web-staging` / Supabase `LTM WEB STAGING`，代码来源为 `staging`；未操作 `main`、Production Render、正式 Supabase、正式环境变量或正式业务数据。
- 风险口径：订单已交单后只按还款状态和融资到期日判断风险。已全部还款但未关闭为低风险；未还款且到期日在未来为中风险；到期日为当天或已逾期为高风险；到期日缺失为中风险并标记“融资到期日缺失”。交单后的发运、交单和提醒旧风险不再抬高综合风险或进入本周重点。
- 同步保护：SQLite、PostgreSQL 的同步状态增加待确认源版本、业务键摘要和记录数三个可空字段；WPS 下载后必须同时通过订单、额度、预警三个工作表、非空记录和非空业务键校验。检测到记录集合缩小时第一次只登记候选，同一源版本和同一业务键摘要连续第二次出现才允许事务性应用；候选变化则重新计次，正常成功、未变化或人工同步后清空候选。
- commit / deploy：风险规则 `f83de77`，待确认状态 `69fd667`，缩量两次确认 `f3243fd`，前端资源断言对齐 `668c2fc`，重新授权回退 `9f8bc3a`；均已推送 `staging`。页面加载 `/static/app.js?v=order-finance-repayment-risk-20260716`。当数据库保存的旧 refresh token 返回 401 时，只改用环境变量中的新 token 重试一次，成功后更新加密保存值；非 401 错误不回退。
- 验证结果：本地 Python 全量 297 项、Node 全量 92 项通过，`compileall`、JavaScript 语法和 `git diff --check` 通过。真实 Staging 页面标题、静态资源和控制台正常；真实样本 `H-2026-3` 已交单待回款、到期日 2026-07-24，综合风险为中、还款风险为中、其它指标为低，未进入本周重点。Staging 与 Production 当前活动记录均为 70 条且业务键摘要一致；两个环境均已配置同一套六个 WPS 环境变量名称和 09:00/17:00 更新机制。
- 当前阻塞：Staging 2026-07-16 09:00 自动同步在 `token_refresh` 阶段返回 HTTP 401；Production 同日 09:04 同步成功。代码和设计要求两个环境分别持有独立授权产生的可轮换 refresh token，不能复制同一个 token 长期共用。管理员需为 Staging 重新完成一次独立 WPS 用户授权并更新测试版 `WPS_USER_REFRESH_TOKEN` 秘密配置，之后再以一次成功自动同步完成测试版闭环验收；无需手工清理数据库中的旧加密 token。当前 Staging 仍保留上次成功数据，没有执行手工覆盖或业务数据写入。
- 回滚与发布边界：代码可按逆序回滚 `668c2fc`、`f3243fd`、`69fd667`、`f83de77`；新增列均为兼容性可空字段，代码回滚时可保留。Production 发布需要数据库备份、幂等迁移、独立 WPS 授权核对及用户 Gate B 明确确认。

## 2026-07-16 Staging 期现最新日期刷新与重复日期精简
- 环境：仅 Staging 测试版 Render `ltm-web-staging`，代码来源为 `staging`；未操作 `main`、Production Render、正式数据库或任何业务数据。
- 发布内容：期现数据管理和期现数据展示每次激活时重新读取 filters 接口的 `latest_data_date`，左上角日期不再停留在首次进入页面时的旧值；年份、品种和港口筛选器仍只初始化一次，页面切换后保留用户选择。删除最优仓单标题旁重复的“数据截至”日期，只保留页面左上角“最新数据日期”。
- commit / deploy：设计 `8bbe577`、实施计划 `7e0a54a`、主体修复 `3e34fa1` 已推送 `staging`；Render Staging 已加载 `/static/iron_ore_basis.js?v=iron-ore-basis-date-refresh-20260716`。
- 是否涉及数据库：否；未修改数据库、接口结构、自动同步调度、EBC/新浪取数或基差计算。验收时测试库最大业务日期为 2026-07-15。
- 验证结果：日期刷新专项测试先失败后通过，JavaScript 语法、`git diff --check` 和 Python 全量 286 项通过。Node 全量 91 项中 88 项通过，3 项为修改前 `09cc562` 已存在的 `app.js` 缓存版本断言漂移。真实 Staging 访客页显示“最新数据日期：2026-07-15”，无“数据截至”；取消 2024 年后切换页面再返回仍保持未选，静态资源版本正确，控制台无 warning/error。
- 验收限制与回滚：管理页刷新路径已由专项自动化测试覆盖；本轮浏览器管理账号重新登录未进入工作台，因此不声称完成管理页可视化点击验收。回滚主体提交 `3e34fa1` 即可恢复原前端行为，无数据库回滚项。Production 发布仍需单独确认。

## 2026-07-15 Staging 铁矿石期现自动同步容错修复
- 环境：仅 Staging 测试版 Render `ltm-web-staging` / Supabase `LTM WEB STAGING`，代码来源为 `staging`；未操作 `main`、Production Render、正式 Supabase 或正式数据。
- 发布内容：EBC 登录、EBC 查询和新浪 I0 请求增加有限重试，连接失败、超时、HTTP 429 和 5xx 最多执行 3 次，间隔 3 秒、8 秒；HTTP 401、403 和确定性业务错误不重试。同一 `slot_key` 的失败批次允许由后续调度自动补偿，批次最多执行 3 次；成功、部分成功、执行中或达到上限的批次不重复执行。试运行存在跳过组合时统一标记为 `partial`。
- 诊断与迁移：`iron_ore_basis_sync_runs` 幂等增加 `attempt_count`、`error_stage`、`http_status`、`last_attempt_at`，成功后清空旧错误字段；运行记录不保存密码、令牌、Cookie、数据库连接串或外部响应正文。SQLite 新建/旧表迁移和 PostgreSQL 增量迁移均纳入回归测试。
- commit / deploy：主体修复 `c1fed47`；首次 Staging 部署发现 PostgreSQL 文本时间字段与 `CURRENT_TIMESTAMP` 的 `COALESCE` 类型不一致，修复提交 `b4a5b4c` 增加显式文本转换并重新部署。`b4a5b4c` 已由 Render 自动部署为 live，应用启动和数据库初始化无错误，运行时自动同步开关为 `true`。
- 自动恢复结果：未手工触发同步。服务启动任务自动重试后以 `attempt_count=2` 完成，读取 210 个源点并写入 2026-07-14 的 96 个组合；随后 2026-07-15 09:30、10:30 两个原失败批次均由调度线程自动补偿为 `partial`，各读取 210 个源点、`attempt_count=2`、错误字段清空。2026-07-14 21:30 原失败批次保留失败审计，但最新日期已由后续批次恢复到 T-1。
- 验证结果：铁矿石期现专项 Python 39 项通过，Python 全量 286 项通过，`compileall` 和 `git diff --check` 通过。Node 全量现有 91 项中 88 项通过，3 项为本次修改前已存在的订单融资静态资源版本断言不一致，本次未修改前端文件。访客实测“期现数据展示”显示最新数据日期和最优仓单截至日期均为 2026-07-14，数据库该日 96 个组合，页面 15 个品种、8 个港口，控制台无 warning/error。
- 结论与回滚：本次判定为“偶发失败但已自动恢复”，仅完成 Staging 修复与验收，停在 Gate B 前。代码可依次回滚 `b4a5b4c`、`c1fed47`；新增列为兼容性可空/默认字段，代码回滚时可保留，不删除已有正确期现数据。

## 2026-07-14 Staging 铁矿石期现 API 增量同步候选
- 环境：仅 Staging 测试版 Render `ltm-web-staging` / Supabase `LTM WEB STAGING`；服务为 Standard，代码来源为 `staging`。未操作 `main`、Production Render、正式 Supabase 或正式数据。
- 发布内容：保留 2024-01-02 至 2026-07-10 的历史 Excel 结果作为存量底库，新增 EBC 现货指标和新浪 I0 收盘价 API 抓取、版本化业务规则、增量基差计算、来源点与结果双重去重、手工 dry-run / apply 命令，以及上海时间 09:30、10:30、21:30 三个自动检查时点。管理页和展示页顶部只显示结果表的最新数据日期。
- commit / deploy：初始候选 `a69456a`、验证记录 `769c79b` 及自动同步加固 `8978794` 已推送 `staging`。`8978794` 已在 Render Staging 重新部署为 live，测试服务运行时 `IRON_ORE_BASIS_AUTO_SYNC_ENABLED=true`。EBC 账号环境变量仅记录名称，密钥值不进入仓库、文档或日志。
- 数据库影响：应用启动时幂等创建隔离的同步批次和来源点表。2026-07-14 09:30 批次已自动把 2026-07-13 的 96 个完整基差组合写入结果及明细；后续重复校验读取 105 个来源点，新增来源点 0、来源差异 0、重复结果写入 0。2026-07-14 日内结果仍为 0，页面最新日期为 2026-07-13，符合 21:30 前不固化 T 日数据的规则。
- 修复与验证：启动补数边界改为 21:30 前仅到 T-1，21:30 后才允许 T 日；同时修复 PostgreSQL `CASE WHEN` 条件参数的整数/布尔兼容问题。本地 Python 245 项、Node 90 项、`compileall`、`git diff --check` 全部通过；真实 PostgreSQL 重复校验成功且去重生效。真实测试页加载 `iron-ore-basis-auto-sync-20260714` 资源，期现展示页显示最新日期 2026-07-13、15 个品种、3 个年份和 8 个港口入口。
- 评估与边界：本次按 D1/T1/R2/C1 执行，只开启 Staging 连续观察，未操作正式版。EBC 仍有间歇性 `http_error`，同一环境的下一次有限重试已成功；将继续观察 09:30、10:30、21:30 三个时点与页面最新数据日期。生产发布、生产环境变量或正式数据变更仍需单独 Gate B 确认。

## 2026-07-16 Production 订单融资单一 WPS 源双库同步正式发布
- 环境与提交：选择性发布订单融资同步改动到 `main`，Production 提交为 `94b82e8`，Staging 保持已验收提交 `d2c03a8`；未合并 staging 的铁矿石等无关功能。Production Render `ltm-web` 为唯一 `wps_source`，Staging Render `ltm-web-staging` 为 `snapshot_follower`，两个环境继续使用各自独立的 Supabase 数据库。
- 同步机制：Production 继续使用原企业 WPS 应用和现有用户授权只读读取同一份 WPS 文档；Staging 不再调用或刷新 WPS token，只通过 HTTPS、服务端 Bearer Secret 和受保护的 `/api/internal/order-finance/snapshot` 跟随 Production。两个环境保持每日北京时间 09:00、17:00 同一业务时点；源端未就绪时跟随端每 5 分钟重试，并按源版本和事实哈希幂等写入。
- 数据边界：跨环境快照只包含订单融资当前有效 WPS 事实字段，不包含数据库 ID、用户权限、操作日志、下一步、备注、人工装船确认等本地管理字段；Production 不配置 Staging 数据库连接，Staging 也不配置 Production 数据库连接。
- 生产备份与迁移：部署前在 Production 私有 schema `codex_backups` 建立 `order_finance_progress_before_snapshot_sync_20260716`（70 条）和 `order_finance_sync_status_before_snapshot_sync_20260716`（1 条）。部署仅对 `order_finance_sync_status` 幂等新增 `pending_source_version`、`pending_business_keys_hash` 两个可空文本列和 `pending_record_count` 非空整数列（默认 0）；上线后通过 `information_schema.columns` 确认三列全部存在。
- 验证结果：发布分支 Python 全量 `259` 项通过，5 条为既有依赖或 FastAPI 生命周期弃用警告；Python `compileall`、全部前端 JavaScript 语法检查和 `git diff --check` 通过。Production 页面标题正常、控制台无 error，`/api/health` 返回 200；快照正确密钥返回 200、错误密钥返回 404。即时跟随后 Staging 更新 2 条、无新增、无归档；两边最终均为 schema version 1、WPS source version 788、70 条记录，事实哈希一致为 `d65a823367effa4559ddc03c2828dadb19890330833e08a02f1c5fd9099db7b6`。
- 回滚点：代码回滚到 `c0e54f6`；新增三列为向后兼容增量列，代码回滚时可安全保留。若需恢复本次同步前订单融资数据，使用上述 Production 私有备份表，禁止直接删除正式业务数据。

## 2026-07-15 Production 订单融资 WPS 自动同步与业务节点正式发布
- 环境：Production Render `ltm-web` / `https://ltm-web-gt13.onrender.com`、Supabase `LTM WEB`；选择性发布订单融资改动到 `main`，功能提交为 `8ded47f`。本次未合并 staging 全分支，铁矿石基差自动同步、EBC 配置和铁矿石前后端文件均未进入正式版。
- 发布内容：订单融资接入 WPS 企业共享台账，每日 09:00、17:00 自动同步；页面仅显示上次同步时间和更新条数，线上台账优先于手工导入。业务节点统一为集港、装船、交单、回款；交单日来自 WPS，有交单日即默认已装船，交单需在融资到期日前 15 天完成，逾期未交单为中风险；回款以回款日对融资到期日或新到期日计算提前/逾期天数。页面默认进入“本周重点”，精简交单、装船、融资摘要、融资到期和回款状态展示，并统一将两家 918ING 银行简称为 `ING（香港）`、`ING（新加坡）`。
- 生产配置与数据：正式 Render 仅新增 `ORDER_FINANCE_WPS_*` 和 `WPS_*` 六个订单融资同步变量，未新增铁矿石同步变量。部署后 2026-07-15 15:10 首次自动同步成功，更新 70 条，WPS source version 为 787；正式库仍为 70 条订单融资记录，轮换后的 refresh token 已加密保存。
- 数据库备份与迁移：发布前完整 dump 位于 `/Users/wangjingze/Documents/轻量化交易管理系统生产备份/20260715-145711/full.dump`，大小 9,705,875 字节，`pg_restore --list` 995 项，SHA-256 `4c98715ab558b84f378a13113f21b226897222fc82763c19611dd84f3b306e0b`；同目录保留 `schema.sql`。迁移仅幂等新增 `order_finance_sync_status` 和可空的 `wps_refresh_token_ciphertext` 字段，不修改已有 70 条订单融资事实数据。
- 验证结果：订单融资 Python 58 项、Node 前端 10 项全部通过，JavaScript 语法、Python 编译、`git diff --check` 和铁矿石零差异检查通过。Render 部署 `8ded47f` 已 live，Production `/api/health` 返回 `{"status":"ok"}`；页面加载订单融资最终静态资源，铁矿石仍加载正式版原资源。当前浏览器正式登录会话已过期，因此未代替用户执行登录后的人工页面复核。
- 回滚点：代码回滚到发布前 `1b4b409`；新增同步状态表和可空字段可保留，不影响旧代码。如需数据库级恢复，使用上述完整 dump，禁止直接删除正式业务数据。

## 2026-07-14 Production 铁矿石期现与交易管理正式发布
- 环境：Production Render `ltm-web` / `https://ltm-web-gt13.onrender.com`、Supabase `LTM WEB`；正式代码由已验收的 staging 发布候选 `ff13902` 上线，交易表安全热修为 `f872dc7`，并同步保持 `staging`、`main` 一致。
- 发布内容：数据可视化管理正式增加现货/期现三级菜单、期现数据管理和期现数据展示；期现展示保留最优仓单、年份/品种共享筛选、日照港默认及 8 港口页签、按品种分图和按年份区分的日度基差。正式增加独立一级菜单“交易管理”，包含总览、持仓与交易、上海钧能台账、期权台账、汇总与导出五个页面。
- 生产备份与回滚：发布前完整逻辑备份位于 `/Users/wangjingze/Documents/轻量化交易管理系统生产备份/20260713-230729/full.dump`，大小约 3.5 MB，`pg_restore --list` 585 项，SHA-256 `8945f56b17e61ec76f7a636a293430d6fc6188447e9d0021d7373534d03c51f3`。代码回滚点为原 Production `5928c16`；新表均为隔离表，代码回滚时默认保留数据，只有确认需要数据库回滚时才依据备份和依赖顺序处理，禁止直接执行仓库中的全库 DDL 重建脚本。
- 期现数据结果：从已验收工作簿导入结果表 60,424 行、完整计算明细 60,424 行；工作簿 SHA-256 `b813c2897c5631ee5fe09db63bfc005e6433ae6929f7b2aab3a7d57d817f917c`。正式库重复业务键 0、孤儿明细 0、缺失明细 0，日期 2024-01-02 至 2026-07-10，共 15 个品种、8 个港口。最优仓单为 2026-07-10 昆巴粉/京唐港，湿吨价 746、标准化现货价 739.49、I0 收盘价 751.50、基差 -12.01。
- 交易数据结果：仅导入确认后的 2026 年 6 月有效批次，不复制 staging 的预览批次和已替代批次。正式批次 1 为 active：成交 2,753、平仓 2,351、持仓来源 13,017、最新持仓 669、持仓 4,082 手、保证金 26,177,056.5、成交手续费 35,380.88、平仓匹配手续费 16,885.34、事实平仓盈亏 3,497,480；开平链接 2,351、事实分配 1,306。同步业务归属 2,690 条，其中期货组/战略套保-期权结构化套利 1,979、期货组/一般套保-锁固定价 614、上海钧能/代内部公司套保 97；默认业务平仓分配 1,306 条，人工重配审计 0。
- 数据安全：期现结果与明细表、15 张交易管理表均启用 RLS；`anon`、`authenticated` 对这些表和序列均无直接权限，网页只经 FastAPI 和应用权限访问。正式权限核对为交易管理 10 名非访客可查看、其中 6 名可编辑，访客无交易管理权限；Supabase 安全顾问对本次 15 张交易表已无 ERROR，仅保留“RLS 已启用但无 Data API policy”的预期 INFO。
- 验证结果：发布候选 Python 200 项、Node 89 项通过；安全热修后 Python 201 项、Node 89 项、`compileall`、4 个 JavaScript 语法检查和 `git diff --check` 全部通过。Production `/api/health` 返回 `{"status":"ok"}`；浏览器实测新版静态资源、期现三级菜单、15 品种、8 港口、最优仓单和日度图表均正常，控制台无 error/warn。生产数据库精确行数、业务归属分组、持仓汇总、盈亏、手续费、RLS 和角色权限均已通过 SQL 复核。
- 已知边界：本版不实现期现趋势图以外的新分析、网页导出或完整计算明细独立查询；交易管理浮动盈亏和期权风险指标仍按已确认范围显示“待计算”，汇总与导出仅保留入口。项目旧表仍有历史 Supabase 安全顾问提示，未在本次发布中扩大范围处理。

## 2026-07-13 Staging 铁矿石期现共享组件一致性修复
- 环境：仅 Staging 测试版 `ltm-web-staging`；未操作 `main`、Production Render、正式数据库或任何业务数据。
- 发布内容：新增数据可视化共享组件，现货与期现的年份/品种筛选统一由同一个复选框组件生成并共用全选、全不选交互；现货图谱与期现基差图统一由同一个年度分面折线图组件渲染。期现仍保留日度日期映射、零轴、HTML 明细提示和港口页签等自身业务参数，不另造第二套筛选或图表实现。
- commit / deploy：`498b007` 已推送 `staging`，Render Staging 已加载 `/static/data_visualization_components.js?v=iron-ore-basis-ui-20260713c`、对应 `app.js` 和 `iron_ore_basis.js`。
- 是否涉及数据库：否；未修改接口、基差计算、港口数据、表结构或测试数据。用户复核后撤回的“港口数据相同”疑点未按缺陷修改，仅执行港口切换回归。
- 验证结果：Node 前端 86 项、相关 Python 只读接口 49 项、JavaScript 语法和 `git diff --check` 全部通过。真实 Staging 页面确认现货与期现筛选均使用 `dv-checkbox-label / checkbox-option`，计算样式同为 26px 最小高度、`0 8px` 内边距和相同边框；两类图表均使用 `year-small-multiples` 共享渲染器。期现页面显示 1 至 12 月和右上角单一年份图例，折线点击可高亮并再次点击取消，悬停显示真实日期/品种/港口/基差；切换京唐港后提示数据口径同步为京唐港。全不选/全选为 0/15、15/15；390×844 下无横向溢出；控制台无 error/warn。
- 评估与边界：按 D2/T2/R2/C1 聚焦返工验收，Staging 13/13 项通过。回滚点为 `498b007` 的前一提交；无数据库回滚项。Production 发布仍需单独确认。

## 2026-07-13 Staging 铁矿石期现页面结构与图表返工
- 环境：仅 Staging 测试版 `ltm-web-staging`；未操作 `main`、Production Render、正式 Supabase 或任何数据库数据。
- 发布内容：将数据管理、数据展示下的现货/期现入口从页面内部页签改为左侧真实三级菜单并继承原父级权限；期现筛选复用现货复选面板及全选/全不选操作；基差图表改为完整 1 至 12 月、右上角单一年份图例、真实日度点 HTML 提示框，并支持点击年度折线后跨品种高亮和再次点击取消。最优仓单、日照港默认、港口页签和当前港口单独加载逻辑保持不变。
- commit / deploy：`421aa02` 已推送 `staging`，Render Staging 已加载 `/static/app.js?v=iron-ore-basis-ui-20260713b`、`/static/styles.css?v=iron-ore-basis-ui-20260713b` 和对应基差 JS/CSS。
- 是否涉及数据库：否；未执行 Excel 导入、基差重算、表结构变更、全量行数复核或测试数据写入。
- 验证结果：新增语义测试先失败后通过；Node 聚焦回归 38 项、相关 Python 只读接口回归 49 项、JS 语法和 `git diff --check` 通过。内置浏览器实测四个三级菜单、三级面包屑、无页面内现货/期现页签、管理页 50 / 60,424 分页、筛选全选/全不选、期现展示默认日照港、2026-07-10 昆巴粉/京唐港最优仓单、曹妃甸港切换后最优仓单不变、1 至 12 月、单一年份图例、2024 折线高亮/取消、2024-01-24 BRBF 日度提示以及现货管理/展示回归；控制台无 error/warn。
- 评估与边界：本次按 D2/T2/R2/C1 聚焦验收，没有执行 T4 全业务链或全系统回归。回滚点为 `bc1d6f9`；无数据库回滚项。Production 发布仍需单独确认。

## 2026-07-13 Staging 铁矿石港口基差与期现模块
- 环境：仅 Staging 测试版 `ltm-web-staging` / Supabase `LTM WEB STAGING`；`main`、Production Render、正式 Supabase 和生产数据均未改动。
- 发布内容：数据管理新增“现货数据管理 / 期现数据管理”，期现管理按年份、品种、港口只读筛选并以 50 条分页加载；数据展示新增“现货数据展示 / 期现数据展示”，期现展示包含独立最优仓单、年份/品种筛选、日照港默认及 8 港口页签、按品种分图和按年份区分的日度基差曲线。后台新增精简结果表、完整计算明细表、Excel 校验导入器和只读查询 API；不提供网页导入、导出、编辑或删除。
- commit / deploy：需求与工作簿计划 `8da9752`、`8b43637`，模块实现 `fa407f7`，前端控制器缓存修复 `8a6ee69`；均已推送 `staging`，Render Staging 已加载 `/static/app.js?v=iron-ore-basis-20260713`、基差专用 JS/CSS 和移动端布局修复。
- 数据库备份与迁移：迁移前确认两张目标表不存在，并在私有 schema `codex_backups.iron_ore_basis_predeploy_20260713` 记录回滚锚点；随后只创建隔离的 `iron_ore_basis_results`、`iron_ore_basis_details` 及索引。两表启用 RLS，撤销 `anon`、`authenticated` 的表和序列权限，网页仅经 FastAPI 权限访问。回滚时先删除明细表，再删除结果表；不会影响已有业务表。
- 数据结果：工作簿 SHA-256 为 `b813c2897c5631ee5fe09db63bfc005e6433ae6929f7b2aab3a7d57d817f917c`。Staging 结果表 60,424 行、明细表 60,424 行，重复业务键 0、孤儿明细 0、缺失明细 0；日期 2024-01-02 至 2026-07-10，3 个年份、15 个品种、8 个港口。最优仓单为 2026-07-10 昆巴粉/京唐港，湿吨价 746、质量升贴水 28、品牌升贴水 0、标准化现货价 739.489711934156、I0 收盘价 751.5、基差 -12.0102880658436。
- 验证结果：本地 Python 全量 200 项、Node 前端全量 84 项通过，`compileall` 与 `git diff --check` 通过；真实工作簿本地重复导入后两表仍各 60,424 行。Staging 管理页实测显示 50 / 60,424 条并可继续加载；展示页默认日照港，切换曹妃甸港后最优仓单不变；390×844 下无横向溢出且工作区可滚动到达；页面标题、静态资源、控制台和基差图表均正常。
- 发布边界：当前停在 Gate B 前，Production 发布需用户另行明确确认。

## 2026-07-12 Staging 业务台账重复汇总区精简
- 环境：仅 Staging 测试版；Production 未改动。
- 发布内容：删除交易管理中“上海钧能台账”和“期权台账”顶部重复的独立指标汇总区，仅保留页签、筛选项和表格上方的下方汇总行；期权当前持仓原有的标的/看涨看跌/行权价/风险指标说明行保留。
- commit / deploy：`b5b733f`；已推送 `staging`，Render Staging 已加载 `/static/trading_management.js?v=20260712h`。
- 是否涉及数据库：否。
- 验证结果：Python 全量 190 项、Node 前端全量 77 项通过，`git diff --check` 通过。Staging 内置浏览器实测：上海钧能台账顶部重复区域为 0、下方汇总为 1 组；期权台账顶部重复区域为 0，业务汇总与原有风险说明各保留 1 组；两页布局正常，控制台无 error/warn。
- 回滚点：回滚 `b5b733f`；无数据库回滚。Production 发布仍需用户单独确认。

## 2026-07-12 Staging 交易管理筛选状态与批量归属性能优化
- 环境：仅 Staging 测试版；Production 未改动。
- 发布内容：修复资产类型等筛选条件生效后下拉框仍显示“全部”的状态回显问题，并补齐当前持仓资产类型过滤。筛选时不再同时预取另外两个页签；平仓记录和全部交易改为数据库筛选、汇总和分页，列表只返回当前页。批量业务归属由逐条查询/写入/回读/审计改为批量 upsert、批量平仓继承和批量审计，仍保持一个事务和逐笔审计记录。
- commit / deploy：`56c57c6` 筛选回显与批量归属，`ef70bb2` 事实列表数据库分页；均已推送 `staging`，Render Staging 已加载 `/static/trading_management.js?v=20260712g`。
- 是否涉及数据库：否；不改表结构，未对 Staging 业务归属执行批量写入。
- 验证结果：Python 全量 190 项、Node 前端全量 77 项通过，`git diff --check` 通过。本地临时库 700 条成交批量归属耗时 0.013 秒，落库 700 条归属与 700 条逐笔审计记录。Staging API 确认当前持仓期货 7 条、期权 16 条，平仓 2,351 条和成交 2,753 条的列表均只返回当前 20 条。测试版单请求实测约 2.1–4.2 秒，其中基础配置请求约 1.9 秒，因此本次主要消除筛选触发的三路并发与大结果传输，Render/Supabase 基础延迟仍是剩余耗时。
- 验收限制：内置浏览器已确认 URL、标题和 `20260712g` 资源；部署切换期的旧资源曾出现一次配置请求失败，刷新后新资源生效。Chrome 控制通道未能再次触发登录表单事件，因此本轮不声称已完成新版页面交互全流程验收。
- 回滚点：依次回滚 `ef70bb2`、`56c57c6`；无数据库回滚。Production 发布仍需用户单独确认。

## 2026-07-12 Staging 交易管理原型一致性、候选口径与分页修复
- 环境：仅 Staging 测试版；Production 未改动。
- 发布内容：事实层统一显示“平仓盈亏”，上海钧能和期权业务视图统一显示“业务归属盈亏”；默认业务开平关系展示文华事实盈亏，手动调整后展示调整后的业务盈亏且不修改事实值。持仓与交易恢复业务类型/策略、橙色“待确认”、归类筛选和聚合记录；当前持仓按合约、方向、资产类型聚合。上海钧能默认展示未归属 RB/HC 候选，明确归属其他业务后移出；上海钧能与期权三个页签接入方向、日期、归类筛选及 20/50/100 服务端分页。总览下方数据质量、业务归属分布、活跃合约改为同一横行三个紧凑卡片，并启用日/月/季/自定义时间范围请求。
- commit / deploy：设计 `cb6458c`，计划 `9733131`，事实归类与候选 `981929c`，业务口径、分页和总览布局 `a9eb0b4`，事实持仓聚合 `abde8b0`，业务持仓聚合 `dc4e695`，总览事实行数修正 `697e2b4`；均已推送 `staging`，最终前端资源版本为 `20260712e`。
- 是否涉及数据库：否；未修改表结构，未写入或删除测试业务数据。
- 验证结果：Python 全量 185 项、Node 前端全量 76 项通过，真实样本事实平仓盈亏保持 3,497,480，总览事实持仓仍为 669 条。Staging 持仓与交易页面由 669 条原始快照聚合为 23 组且手数 4,082、保证金 26,177,056.5 不变；上海钧能 19 条 RB 候选聚合为 1 组 260 手并显示“待确认”；期权全部交易共 1,979 条，50 条/页时共 40 页，第二页首行内容发生变化；业务平仓表同时显示事实平仓盈亏与业务归属盈亏；总览三个辅助卡片在 1280px 下为同一行三列，宽度约 319px/列，控制台无相关错误。
- 回滚点：按逆序回滚 `697e2b4`、`dc4e695`、`abde8b0`、`0b69a03`、`a9eb0b4`、`981929c`；无数据库回滚。Production 发布仍需用户单独确认。

## 2026-07-12 Staging 交易管理筛选、切换、摘要与趋势图调整
- 环境：仅 Staging 测试版；Production 未改动。
- 发布内容：持仓与交易、上海钧能及期权页面的合约搜索、资产、方向、归类状态和日期筛选压缩为桌面单行控件；持仓、平仓、全部交易增加同筛选条件缓存和相邻页签预取；上海钧能和期权台账移除“口径说明”侧卡，改为全宽记录数、手数、业务归属盈亏、浮动盈亏摘要，期权另起一行展示 Delta、Gamma、Theta、Vega；总览逐日平仓盈亏趋势接入 active 平仓事实按日汇总；数据质量说明字号调整为 13px 并压缩面板间距。未新增总览浮动盈亏指标，未启用任何待确认计算。
- commit / deploy：逐日事实序列 `dd6ddda`，页签缓存与预取 `1f0e7f9`，页面布局、图表及静态资源版本 `b8125e2`；已推送 `staging`，Render Staging 已加载 `20260712c` 静态资源。
- 是否涉及数据库：否；不修改表结构和测试业务数据。
- 验证结果：Python 全量 183 项、Node 前端全量 74 项通过，JavaScript 语法与 `git diff --check` 通过。真实三表样本回归确认逐日序列合计 3,497,480 且只读取 active 批次。本地 1280px 浏览器验证筛选栏 7 个控件同为 32px 高并保持单行；持仓→平仓→交易→持仓切换分别约 277/276/274ms；期权摘要为两行四列，数据质量说明为 13px，控制台无 error。Staging 页面身份、登录、控制台和 `20260712c` 资源版本已确认；Chrome 扩展未能触发动态侧栏切换，目标工作区渲染改在内置浏览器本地同版本完成。
- 回滚点：回滚 `b8125e2`、`1f0e7f9`、`dd6ddda`；本次无数据库回滚。Production 发布仍需用户单独确认。

## 2026-07-12 Staging 交易三表确认导入性能与覆盖口径修复
- 环境：仅 Staging 测试版；Production 未改动。
- 问题：真实三表预检后点击“确认覆盖导入”，页面长期停留在“正在覆盖导入并建立事实匹配”。此前验收只覆盖入口、布局、状态文案和本地 SQLite 业务函数，没有在 Render + PostgreSQL 上完成真实“上传、预检、确认、匹配、落库、页面回显”闭环。
- 根因：确认导入对来源行、事实身份、事实记录、平仓匹配和业务默认分配执行上万次逐行数据库操作；覆盖导入还逐笔查询业务继承。远端 PostgreSQL 网络往返使请求超过 300 秒。覆盖后稳定身份关联平仓事实时未限定 active 批次，还会造成成交侧事实盈亏重复统计。
- 修复：来源行、身份、成交/平仓/持仓事实全部批量准备和写入；PostgreSQL 批量执行使用 `execute_batch`；事实匹配、手续费更新和业务默认分配改为批量删除/写入，合约配置和继承关系一次预取；成交侧平仓盈亏只关联 active 平仓事实版本。
- commit / deploy：数据库批处理与匹配 `0533469`，覆盖继承预取 `82911be`，事实批量写入 `80f7aae`，active 版本口径 `b1d7ea2`；Render Staging 已部署并验证。
- 数据结果：使用确认的 2026 年 6 月真实三表重新覆盖导入，预检批次 5 确认耗时 19.06 秒并返回成功；成交 2,753、平仓 2,351、来源持仓 13,017、最新持仓 669、持仓 4,082 手、保证金 26,177,056.5。总览成交侧和平仓侧事实平仓盈亏均为 3,497,480，成交手续费 35,380.88、平仓匹配手续费 16,885.34。批次 5 为 active，批次 2 为 superseded。
- 验证结果：Python 全量 181 项、Node 前端全量 70 项通过；新增数据库往返、PostgreSQL 批处理、覆盖继承和 active 事实版本回归测试。Staging API 与实际页面均确认 669 条持仓和真实表格数据可见。
- 回滚点：代码可回滚 `b1d7ea2`、`80f7aae`、`82911be`、`0533469`；不建议回滚到逐行远端写入实现。测试版批次 5 已替代批次 2，Production 发布仍需单独确认。

## 2026-07-12 Staging 交易管理原型高保真还原与导入交互修复
- 环境：仅 Staging 测试版；左侧系统导航保持现有设计，Production 未改动。
- 发布内容：右侧交易管理工作区按已确认原型重新实现总览、持仓与交易、上海钧能台账、期权台账、汇总与导出；恢复原型的顶部筛选、指标带、双栏面板、数据质量、业务分布、台账页签、筛选摘要、表格、抽屉和输出占位组件。期权持仓恢复标的、看涨/看跌、行权价及 Delta/Gamma/Theta/Vega 列，未确认计算项继续显示“待计算”。
- 导入调整：全系统只在“持仓与交易”保留一个“三表导入”入口；改为右侧完整抽屉，成交、平仓、期末持仓三表必须齐全。预检和确认覆盖期间禁用控件并显示明确进度，失败信息留在抽屉内，避免用户无法判断是否正在处理。
- commit / deploy：原型约束与实施计划 `12900db`、`528eecf`，原型契约测试 `14d0534`，页面还原 `045633b`；Render Staging 已加载 `/static/trading_management.js?v=20260712b`、`/static/trading_management.css?v=20260712b` 和 `/static/app.js?v=trading-management-20260712b`。
- 是否涉及数据库：否；未上传真实三表，未修改 Staging 业务数据。
- 验证结果：Python 全量 175 项、Node 前端全量 70 项通过，JavaScript 语法与 `git diff --check` 通过。本地和 Staging 均以 1440×1000 浏览器逐页核验：五个页面可打开，左侧导航未变，导入入口仅 1 个，抽屉宽 460px 且完整位于视口内，包含 3 个文件输入；控制台无 error。
- 回滚点：回滚 `045633b` 可恢复前一版交易管理页面；本次无数据库回滚。Production 发布仍需用户单独确认。

## 2026-07-12 Staging 交易管理模块 P0
- 环境：仅 Staging 测试版；旧台账继续保留，`main`、Production 代码和 Production 数据均未改动。
- 发布内容：新增独立一级菜单“交易管理”，包含总览、持仓与交易、上海钧能台账、期权台账、汇总与导出五个页面。支持文华成交/平仓/期末持仓三表齐全后预检和确认覆盖；事实层只读，业务归属按整笔成交配置业务主体、基础/战略套保和可输入策略；业务开平支持候选校验、盈亏影响预览、原子确认、版本冲突保护、平仓自动继承、持仓同步、恢复事实默认关系和闭环差异标记。汇总与导出仅保留入口，浮动盈亏与期权风险指标按需求显示“待计算”。页面无任何真实下单、撤单或资金操作。
- 初始配置：从新版台账模板初始化业务主体和 14 个策略；上海钧能候选品种仅为 RB、HC；按 2026 年 6 月样本核验上期所 RB/HC、大商所 I/J 及 I 期权合约乘数。测试版配置了“宏源期货账户”导入选项，但本次未把本机真实三表上传到 Staging。
- commit / deploy：设计 `8df8bc8`、实施计划 `03cc810`、数据底座至业务视图 `22b5dd0`—`0a3b66e`、业务重配 `4ff71a6`、完整接口与页面 `b7a928b`、页面验收 `a53cc08`、迁移保护 `aae4c54`；Render Staging 已加载 `/static/trading_management.js?v=20260712`、`/static/trading_management.css?v=20260712` 和 `/static/app.js?v=trading-management-20260712`。
- 数据库备份与迁移：Staging 应用启动时在任何新菜单权限写入前，幂等创建私有备份表 `codex_backups.module_permissions_before_trading_management_20260712`；随后只新增 15 张独立 `trading_*` 表、索引、参考配置及缺失的新菜单权限，不迁移、不修改旧上海钧能和其它旧台账业务表。现有个人权限选择不会被覆盖。
- 验证结果：Python 全量 174 项通过，Node 前端全量 66 项通过，JavaScript 语法和 `git diff --check` 通过。真实 2026 年 6 月三表在临时本地库只读回归：成交 2,753、平仓 2,351、最新持仓 669、逐笔平仓盈亏 3,497,480、全部成交手续费 35,380.88、平仓匹配手续费 16,885.34、匹配覆盖 2,351/2,351、最新持仓保证金 26,177,056.50，均与需求基线一致。Staging 浏览器确认新旧一级菜单并存、五个新页面可访问、账户/主体/策略配置已加载、期权页默认全量口径说明可见、控制台无 error。
- 回滚点：代码回退 `aae4c54` 及本条列出的交易管理提交即可移除新入口；新增 `trading_*` 空表可暂留，若需删除须先确认无测试导入数据。新菜单权限可从上述 `codex_backups` 备份表恢复。Production 发布仍需用户单独确认。
## 2026-07-10 Production 用户权限、正式名单、订单融资数据与操作日志优化发布
- 环境：Production 正式版；用户已明确确认将当前 Staging 功能、正式用户名单及订单融资当前业务数据同步到正式版。
- 发布内容：将用户/领导/管理员三类身份、公司领导部门、部门默认权限与个人例外、四级操作权限、自助改密、管理员重置、账号启停、最后管理员保护，以及操作日志游标分页、降噪和冷归档能力由 `staging` 合并到 `main`。正式版静态资源已加载 `/static/app.js?v=operation-log-retention-20260710` 和对应样式版本。
- commit / deploy：业务代码合并提交 `e1c685c` 已推送 `main`，Render Production 自动部署并完成真实浏览器验收；发布前正式版代码回滚点为 `6be9076`。
- 发布前备份：在 Production Supabase 私有 schema `codex_backups` 创建 `users_before_auth_order_finance_20260710_9490b9e`（4 条）、`module_permissions_before_auth_order_finance_20260710_9490b9e`（39 条）、`user_sessions_before_auth_order_finance_20260710_9490b9e`（158 条）、`operation_logs_before_auth_order_finance_20260710_9490b9e`（90,384 条）和 `order_finance_progress_before_auth_order_finance_20260710_9490b9e`（0 条）。未读取或记录密码哈希及明文密码。
- 用户与权限同步：`/Users/wangjingze/建龙/人员名单.xlsx` 26 人整批预检通过后复用正式用户创建接口写入。正式版现有 30 个账号（含系统访客）、29 个后台可见用户、408 条最终权限；王景泽为管理部门管理员、14 个模块均为敏感权限且不提示改密；龙云飞在贸易处默认权限上额外获得两个订单融资模块日常操作权限；公司领导和曹骧特例均按已确认规则写入。Staging 与 Production 的用户及权限业务指纹完全一致。
- 订单融资数据同步：通过正式导入接口读取 `/Users/wangjingze/建龙/贸易处/YOLANDA和香港建龙出口钢材信用证台账.xlsx`，写入 70 条有效记录、70 个唯一业务键，无导入警告；当前融资 11 行、存续融资金额 438,505,396.00 元。同步 `Y-2026-12` 当前提醒 `2026-07-19 · 预计交单日期，需提前确认是否装船`，没有复制 `Y-2026-19`、`Y-2026-20` 在 Staging 验收中产生又撤销的测试审计轨迹。剔除测试轨迹和空值表现差异后，Staging 与 Production 订单融资业务指纹均为 `83a28fa487f1b2c1597d98ecbb6f13f1`。
- 自动化验证：合并后的 Node 前端测试 62 项全部通过，后端 `compileall`、JavaScript 语法和 `git diff --check` 通过；Python 139 项通过，仍有 1 项项目既有且与本次发布无关的实时行情共享缓存 mock 次数断言失败，未把该项描述为全量通过。王景泽、龙云飞、公司领导张胜根及曹骧四类代表账号均在 Production 实际登录成功，身份、部门和改密提醒符合规则。
- 浏览器验收：正式版用户管理实际显示 29/29 人，王景泽和龙云飞权限摘要正确；操作日志首次只加载最近 100 条，可按需加载更多，90,000 余条历史日志不会在打开页面时一次性传输。订单融资进度显示 67 个合同/项次、存续融资 43,850.54 万，并正确展示 `Y-2026-12` 备注提醒；浏览器控制台无 error/warning。
- 冷归档状态：归档表和受控脚本已随代码上线，但 Production 尚未配置私有 Storage 归档参数，也未实际执行归档或删除热日志；执行前仍须先 dry-run 并核对归档范围。
- 回滚方式：代码恢复到 `6be9076`；认证、权限、会话、日志和订单融资数据只从上述五张 Production 备份表恢复，不触碰其它业务台账表。

## 2026-07-10 Production 订单融资三页签、风险与本周重点正式发布
- 环境：Production 正式版
- 发布内容：将 Staging 已验收的订单融资模块发布到正式版。Excel 导入仅关注 `订单`、`额度`、`预警` 三个页签；页面保持原订单融资进度和融资资金监控展示形式，补齐最迟装船日、真实还款及时性、展期到期日、手工确认/撤销装船、字段级红黄风险、本周重点及备注提醒。最迟装船日前 10 天进入联系工厂范围；手工跟进日在 10 天内或已逾期时形成中风险并进入本周重点，超过 10 天不触发。
- 来源分支：`staging -> main`
- commit / deploy：业务代码 `e6e4d50`；Render Production 自动部署已验证，正式页加载 `/static/app.js?v=order-finance-weekly-focus-20260710` 和对应样式版本。
- 是否涉及数据库：应用启动时幂等新增 `shipment_confirmed_date`、`shipment_confirmed_by`、`shipment_confirmed_at` 三个可空字段；发布前只读核对正式库 `order_finance_progress` 为 0 条，因此没有业务数据需要导出。本次未向正式库导入 Staging Excel、样例数据或测试记录。
- 验证结果：合并后 Python 回归 99 项通过、1 项既有且与本模块无关的实时行情缓存用例排除；Node 前端测试 53 项通过；JavaScript 语法、后端 `compileall`、`git diff --check` 通过。正式版 `/api/health` 返回 OK；管理员登录后可进入订单融资进度，显示本周重点、最迟装船/交单/还款缺失指标及合同聚合区域，当前正式库 0 条订单，符合本次代码发布不迁移测试数据的边界。
- 回滚点：发布前正式版代码为 `12c5b42`；新增字段均为可空增量列，可保留且不影响旧代码。如需回滚功能，可将代码恢复到 `12c5b42`，无需删除字段。

## 2026-07-10 Staging 操作日志分页、降噪与冷归档
- 环境：仅 Staging 测试版；`main`、Production 代码和 Production 数据均未改动。
- 发布内容：操作日志默认仅加载最近 100 条，最大单页 200 条，改用 `(created_at, id)` 游标分页，支持操作类型、用户名和日期筛选；前端按需“加载更多”，导出仅包含当前已加载记录。自动实时信息计算不再写入“批量计算指标”日志，只有用户主动批量计算才记录。
- 冷归档能力：新增月度 gzip NDJSON 归档、SHA256/行数校验、Supabase 私有 Storage 上传、归档目录按需读取与点击下载，以及受控恢复脚本。归档默认 dry-run，排除当月数据，保留最近 12 个月或最近 20 万条热日志；上传并校验成功后才在同一数据库事务中登记元数据、保留用户历史关联并删除热数据。当前没有符合归档条件的历史日志，因此未删除任何现有记录，也未创建付费定时任务。
- commit / deploy：数据库结构 `aa48b55`、后端游标分页 `4e14d4b`、前端增量加载 `bb90ee2`、自动计算降噪 `cb3033d`、归档与恢复 `8f00569`、归档下载 `dd5d9e3`、发布保护文档 `7ad4c6b`；Render Staging 已加载 `/static/app.js?v=operation-log-retention-20260710`。
- 数据库备份与迁移：部署前在 Staging 私有 schema `codex_backups` 创建 `users_before_operation_log_retention_20260710_7ad4c6b`（30 条）和 `operation_logs_before_operation_log_retention_20260710_7ad4c6b`（12,103 条）。迁移后新增 `operation_log_archives`、`operation_log_archive_users` 及 3 个日志游标/筛选索引；复核热日志仍为 12,103 条、归档元数据为 0 条。
- 验证结果：Python 全量 139 项通过，仍有 1 项项目既有且与本功能无关的实时行情缓存 mock 次数断言失败；本功能与鉴权 focused 测试 37 项通过；Node 前端全量 62 项通过；后端 `compileall`、JavaScript 语法和 `git diff --check` 通过。Staging 浏览器确认首屏 100 条、加载更多后 200 条、添加用户筛选命中 30 条、历史归档为空且不主动下载；进入实时信息汇总后“批量计算指标”日志计数保持 4,147 条不变，控制台无 error/warn。
- 运维说明：真正执行冷归档前，需在对应环境配置服务端 `SUPABASE_URL`、`SUPABASE_SERVICE_ROLE_KEY` 和私有归档 bucket；先运行 `scripts/archive_operation_logs.py` dry-run 核对范围，再使用 `--apply`。恢复同样先 dry-run，再明确指定归档 ID 执行。
- 回滚点：代码可依次回滚 `7ad4c6b`、`dd5d9e3`、`8f00569`、`cb3033d`、`bb90ee2`、`4e14d4b`、`aa48b55`；空归档表和新增索引可保留或单独删除。认证与日志数据仅从上述备份表恢复，不触碰业务台账表。Production 上线仍需用户单独确认。

## 2026-07-10 Staging 公司领导、名单建号与分部门初始密码规则
- 环境：仅 Staging 测试版；`main`、Production 数据库和 Production 账号均未改动。
- 发布内容：固定部门新增“公司领导”；页面预览、创建、默认重置和名单导入统一使用按用户类型/部门选择的初始密码策略，并保留曹骧个人例外。受控名单脚本兼容当前三列 Excel，整批预检通过后逐人复用用户创建接口。王景泽保留为“管理部门 / 管理员”，通过受控接口设置用户指定密码且 `password_change_recommended = 0`；审计日志不记录明文密码。
- 权限结果：真实名单共创建 26 个启用账号，其中公司领导 3、贸易处 13、期货组 3、财企处 5、资金处 2；领导 4、普通用户 22。三名公司领导各有 9 个已上线业务模块查看权限，日常和敏感权限均为 0、无后台入口。龙云飞在贸易处默认快照上额外获得“订单融资进度”和“融资资金监控”日常操作权限，两个模块敏感权限均为 0。
- commit / deploy：规格 `27a959b`、计划 `8da89de`、密码策略 `e525c76`、统一密码接口 `051e520`、公司领导下拉 `34654d9`、名单导入 `48f050f`、上海钧能纯查看按钮修复 `85c0daf`、首轮静态资源刷新 `82244ff`、九个业务模块纯查看控件审计修复 `12bf07c`、共享预警确认鉴权修复 `89717d1`；Render Staging 已加载 `/static/app.js?v=roster-permissions-20260710-3`。
- 数据库备份：建号前在 Staging Supabase 私有 schema `codex_backups` 创建 `users_before_roster_20260710`（4 条）、`module_permissions_before_roster_20260710`（39 条）、`user_sessions_before_roster_20260710`（327 条）、`operation_logs_before_roster_20260710`（12020 条）。备份迁移标识为 `backup_auth_before_roster_20260710`，未读取或记录密码哈希。
- 名单预检与写入：`/Users/wangjingze/建龙/人员名单.xlsx` dry-run 为 26 人、26 个唯一账号、零非法部门、零账号冲突；密码规则命中其他领导 3、贸易处/期货组普通规则 15、曹骧例外 1、财企处/资金处规则 7。正式 apply 创建 26 人并完成王景泽受控配置；最终数据库再次核对新增 26、唯一账号 26、公司领导权限行 27 且日常/敏感行均为 0，管理员设置密码审计摘要未命中本次受控密码。
- 自动化验证：名单与权限 focused 测试 28 项通过；Python 全量 121 项通过，仍有 1 项项目既有且与本功能无关的实时行情缓存 mock 次数断言失败；Node 前端 57 项通过，后端 `compileall`、JavaScript 语法和 `git diff --check` 通过。三轮 RED/GREEN 回归覆盖上海钧能纯查看控件、九个已上线业务模块的日常/敏感控件，以及共享预警历史单条/批量确认的后端鉴权；管理员受控设密测试确认目标会话注销，名单脚本脱敏测试同时覆盖标准输出和错误输出。
- Staging 验收：贸易处、期货组、财企处、资金处代表普通账号、公司领导、曹骧、龙云飞和王景泽均实际登录成功。公司领导无后台；上海钧能只保留查看控件；事中风险 4 个策略组/4 条持仓均只保留查看，新增、编辑、删除和价格刷新不可见；风险预警新增、扫描、批量启停/删除、选择框和行级编辑/启停/模拟/删除均不可见；共享预警通知仍可阅读但不可点击确认，批量确认和历史“标记已读”隐藏，单条/批量确认 API 均返回 403 且临时记录保持未读；数据整合导入/导出和数据管理导入均不可见；订单融资 67 个查看明细入口保留，装船、备注和导入控件不可见。龙云飞可进入两个融资页面，实际调用两个敏感导入入口均返回 403；王景泽页头为管理员且改密提醒隐藏。验收页面控制台无 error/warning，临时预警设置和历史已精确清理且残留为 0。
- 回滚点：代码依次回滚 `89717d1`、`12bf07c`、`82244ff`、`85c0daf`、`48f050f`、`34654d9`、`051e520`、`e525c76`；数据仅从上述四张认证备份表恢复用户、模块权限、会话和操作日志，不触碰任何业务台账表。Production 上线及真实 Production 建号仍需用户单独确认。

## 2026-07-10 Staging 用户、权限与密码管理优化
- 环境：Staging 测试版；Production 未改动，真实名单未创建。
- 发布内容：用户新增独立唯一登录账号、`用户/领导/管理员` 三类身份、五个固定部门、首次改密提醒、用户自助改密、管理员重置密码、账号启停、最后一名启用管理员保护和仅无历史误建账号可删除。后台用户管理拆分为新增、编辑资料、四级权限配置、密码重置、启停和操作日志；新增用户支持账号、临时密码、部门默认权限、个人调整及最终差异确认。
- 权限规则：模块权限统一为无权限、查看、日常操作、敏感操作四级；查看、新增修改、删除/导入/导出/管理均由后端鉴权。普通用户按部门生成权限快照，领导默认全部已上线业务模块查看，管理员固定全部业务和后台敏感权限且归属管理部门；历史管理员部门已幂等归一到管理部门。部门规则变化不静默改已有快照，贸易处用户可单独增加订单融资等个人例外。
- commit / deploy：主功能 `2f7b1f0`，PostgreSQL 多行 `INSERT OR IGNORE` 兼容修复 `422bf57`，历史管理员部门归一 `a8030b7`；Render Staging 自动部署已验证。
- 数据库与备份：迁移前在 Staging Supabase 私有 schema `codex_backups` 建立 `backup_auth_before_user_permissions_20260710`，备份用户 4、模块权限 39、会话 317、操作日志 12007 条。迁移幂等新增 `users.username`、`users.password_change_recommended`、`module_permissions.can_sensitive`，现有姓名原样回填账号，原 `can_edit` 同时迁移为日常和敏感权限；不改业务台账表。
- 自动化验证：Python 全量 110 项通过，仍有 1 项项目既有且与本功能无关的实时行情缓存 mock 调用次数断言失败；Node 前端 54 项通过；后端 `compileall`、JavaScript 语法和 `git diff --check` 通过。SQLite 旧库重建、PostgreSQL SQL 重写、部门矩阵、个人例外、四级权限、后台边界、最后管理员保护、拼音账号、改密/重置/会话注销和受控名单 dry-run 均有覆盖。
- Staging 验收：在真实页面创建贸易处、期货组、财企处、资金处普通用户、领导和管理员六类临时账号；部门菜单分别为 6、7、5、5 个默认模块，领导 9 个业务模块只读，管理员 14 个模块敏感权限。贸易处个人例外增加订单融资进度查看后实际可进入页面，但后台入口和敏感导入按钮不可见；领导访问用户接口、期货组日常账号调用敏感导出均返回 403。自助改密保留当前会话并注销其它会话，管理员重置恢复临时密码提醒，停用立即使会话和登录失效，重新启用恢复登录；浏览器控制台无 error/warn。
- 测试数据清理：六个 `codexqa_*` 账号及关联权限、会话和测试审计记录已精确删除，复核测试账号、权限、会话和日志均为 0，Staging 用户总数恢复为 4（含系统访客）。
- 回滚点：代码可依次回滚 `a8030b7`、`422bf57`、`2f7b1f0`；认证相关数据可从 `codex_backups` 对应备份表恢复，回滚范围仅限用户、权限、会话和日志表。Production 迁移、真实名单 dry-run 和正式建号需用户另行确认。

## 2026-07-10 Staging 订单融资本周重点与备注提醒
- 环境：Staging 测试版
- 发布内容：将筛选和顶部指标 `本周重点/高风险` 统一改为 `本周重点`。本周重点采用滚动 10 天规则，包含所有高风险业务、未来 0–10 天需要联系工厂确认装船的业务，以及手工跟进日期在未来 0–10 天或已经逾期的业务；其它仅因融资到期形成的中风险不自动进入本周重点。最迟装船提醒由提前 7 天调整为提前 10 天。
- 备注提醒：存续卡片新增 `备注/提醒`，支持填写备注内容和跟进日期、编辑及二次确认清除。手工跟进日期进入 10 天范围或逾期时形成中风险并进入本周重点，超过 10 天不产生风险；卡片在下一步下方紧凑显示日期和备注，不改变原四列字段布局。备注和日期同步同项次全部融资行并在 Excel 再导入后保留。
- commit / deploy：规格 `137d934`、实施计划 `c05e7d3`、本周重点计算 `b905441`、备注接口 `e330897`、前端交互 `0355e0a`；Render Staging 自动部署已验证，页面加载 `/static/app.js?v=order-finance-weekly-focus-20260710` 和对应样式版本。
- 是否涉及数据库：否。复用现有 `manager_note` 和 `next_follow_up_date` 字段，不新增表和字段，不涉及 Production。
- 数据写入：为验证重新导入保留逻辑，Staging 重新导入最新 `/Users/wangjingze/建龙/贸易处/YOLANDA和香港建龙出口钢材信用证台账.xlsx`，更新 70 条、无新增、有效预警 3 条。验收期间对 `Y-2026-19` 写入临时备注/日期，完成后已清除并恢复原状态。
- 验证结果：订单融资与权限测试 32 项、Node 前端测试 53 项通过；Python 全量回归 99 项通过，1 项既有且与本模块无关的实时行情缓存测试失败；JavaScript 语法、后端 `compileall`、`git diff --check` 通过。真实台账存续风险仍为高 3、中 5、低 2，本周重点为 4 项：`Y-2026-12`、`Y-2026-20`、`Y-2026-14` 三项高风险，以及最迟装船日正好在未来 10 天的 `H-2026-3`。
- 浏览器验收：`Y-2026-19` 填写未来第 11 天提醒后保持低风险且不进入重点；改为第 10 天后变中风险、重点由 4 增至 5；改为逾期日期后仍为中风险并持续留在重点；重新导入后备注仍存在；清除后恢复低风险、无备注、重点回到 4。`H-2026-3` 显示 `10 天内需联系工厂` 且装船框为黄色；页面控制台无 warning/error。
- 回滚点：依次回滚 `0355e0a`、`e330897`、`b905441` 可恢复到此前高风险筛选和 7 天装船提醒；未发布到 Production。

## 2026-07-10 Staging 订单融资字段级风险与手工装船确认
- 环境：Staging 测试版
- 发布内容：保留卡片总体高/中/低风险标签，具体触发高风险的字段框标红、触发中风险的字段框标黄，低风险和已完成业务字段不着色。最迟装船日为空或逾期时只标红最迟装船框；融资到期在 8–30 天时只标黄融资到期框；多笔融资和展期状态本身不着色。删除页面下方导入报告区域。
- 手工管理：存续且尚未识别装船的项次新增 `确认已装船`，保存实际装船日、确认人和确认时间；确认后进入 `已装船待回款`、消除缺失/逾期装船风险并显示 `待确认交单`，支持撤销。同一项次多笔融资同步确认，重新导入 Excel 后保留，不写入提单日。
- commit / deploy：功能提交 `5186a81`，浏览器验收边界修正 `23baa9d`，前端缓存刷新 `473de65`；Render Staging 自动部署已验证，页面加载 `/static/app.js?v=order-finance-indicator-risk-20260710-2`。
- 是否涉及数据库：测试库表新增 `shipment_confirmed_date`、`shipment_confirmed_by`、`shipment_confirmed_at` 三个可空字段；迁移为增量加列，不改已有订单事实数据。迁移前已备份 70 条订单融资记录到 `/tmp/order_finance_staging_backup_before_shipment_confirmation_20260710_122043.json`。不涉及 Production。
- 验证结果：订单融资与权限测试 29 项、Node 前端测试 52 项通过；Python 全量回归 96 项通过，1 项既有且与本模块无关的实时行情缓存用例失败；JavaScript 语法、后端 `compileall`、`git diff --check` 通过。真实台账 70 条记录复核后，10 笔存续业务风险仍为高 3、中 5、低 2。Staging 浏览器确认：`Y-2026-20` 仅最迟装船框为红色；`H-2026-3` 仅融资到期框为黄色；`Y-2026-19` 和已完成业务字段均无红黄底色；导入报告不存在。手工确认 `Y-2026-20` 后总体风险由高变低、阶段变为已装船待回款、装船框取消红色，撤销后完整恢复，测试数据已清理。
- 回滚点：代码依次回滚 `473de65`、`23baa9d`、`5186a81`；新增列为可空字段，可保留不影响旧代码。未发布到 Production。

## 2026-07-10 Staging 订单融资装船日期、还款及时性与风险口径修正
- 环境：Staging 测试版
- 发布内容：订单卡片将 `提单日` 改为 `最迟装船日`，读取 `订单.最迟装船日`；存续业务显示距今天数，7 天内黄色提示联系工厂、已超过日期红色提示，已结案业务只显示历史日期、不倒计时、不标红。`还款情况` 改为 `还款日`；展开明细删除无可靠数据来源的提单日，保留交单日和还款日；缺失节点文案改为 `缺最迟装船/交单/还款`。
- 到期与结案口径：融资风险优先使用 `新到期日`；新到期日为空且存在展期天数时，使用 `原到期日 + 展期天数`。已结案业务不再用融资到期日与今天比较，而是用真实还款日与有效到期日比较，显示提前、按期或逾期还款；2025 年及其他已结案业务不参与风险和数据异常统计。
- 风险口径：高风险为存续且有效到期日逾期/不足 7 天未还款，或存在有效预警；中风险为有效到期日在 8–30 天，或已还款但仍标记存续；低风险为有效到期日在 30 天以后且无有效预警。多笔融资只显示标记，不作为异常、不自动加级；业务阶段不再自动将所有存续订单设为中风险。
- commit / deploy：规格与计划提交 `c354f8a`、`03ec033`，功能提交 `8c3ea50`，结案装船提示边界修正 `2e49310`；Render Staging 自动部署已验证，页面加载 `/static/app.js?v=order-finance-shipment-risk-20260710-2` 和对应样式版本。
- 数据写入：重新导入 `/Users/wangjingze/建龙/贸易处/YOLANDA和香港建龙出口钢材信用证台账.xlsx`，归档旧 Excel 来源记录 70 条、更新 70 条、无新增；只读取 `订单/额度/预警` 三页签。导入后有效预警 3 条，风险分布为高 3、中 5、低 2、已完成 57；不改数据库表结构，不涉及 Production。
- 验证结果：订单融资与权限测试 26 项通过；Python 回归 93 项通过、1 项既有实时行情缓存测试排除；Node 测试 50 项通过；JavaScript 语法、后端 `compileall` 和 `git diff --check` 通过。Staging 浏览器确认：`Y-2026-12` 最迟装船日显示 `2026-07-05 / 已超过 5 天` 且为存续红色提示；`H-2026-3` 保留多笔融资标记、风险为中、最迟装船日为 `2026-07-20`；已结案 `Y-2025-37` 显示展期 14 天、有效到期日 `2026-02-14`、真实还款日 `2026-03-02`、`逾期 16 天还款`，最迟装船日不再倒计时标红。展开明细无提单日列；资金监控仍为总授信 87,466 万、占用 43,850.54 万、剩余额度 43,615.46 万、使用率 50.1%；页面控制台无 error/warn。
- 回滚点：代码可回滚 `2e49310`、`8c3ea50`，恢复到 `ac5d5cb`；如需恢复 Staging 数据，可重新导入上一版台账文件。未发布到 Production。

## 2026-07-10 Staging 订单融资最新台账数据更新
- 环境：Staging 测试版
- 来源文件：`/Users/wangjingze/建龙/贸易处/YOLANDA和香港建龙出口钢材信用证台账.xlsx`，文件修改时间 2026-07-10 10:38:16。
- 数据变化：继续只读取 `订单`、`额度`、`预警` 三个页签。与上一版台账相比记录数仍为 70、聚合项次仍为 67、额度数据不变；唯一订单字段变化是 `Y-2026-13` 增加银行交单日 `2026-07-01`，业务阶段由 `已放款待装船` 调整为 `已交单待回款`。本周重点/高风险由 5 降为 4，数据异常由 10 降为 9。
- 数据写入：通过 Staging `/api/order-finance/import-file` 导入，归档旧 Excel 来源记录 70 条、更新 70 条、无新增记录；不涉及 Production 数据，不改数据库表结构。
- 验证结果：导入接口返回 1 个文件、70 条记录、9 条异常，三个目标页签均识别成功。Staging 浏览器确认订单融资进度为未结算 10、存续融资 43,850.54 万、已放款待装船 9、已交单待回款 1、已完成 57；`Y-2026-13` 明细交单日为 `2026-07-01`。资金监控显示总授信 87,466 万、占用 43,850.54 万、剩余额度 43,615.46 万、使用率 50.1%，额度占用与存续订单金额差额为 0；页面控制台无 error/warn。
- 回滚方式：如需恢复上一版 Staging 数据，重新导入 `YOLANDA和香港建龙出口钢材信用证台账(副本).xlsx`。未发布到 Production。

## 2026-07-10 Staging 订单融资三页签取数与节点语义修正
- 环境：Staging 测试版
- 发布内容：订单融资 Excel 导入改为只读取 `订单`、`额度`、`预警` 三个页签，忽略工作簿中的其它页签；以 `订单.项次` 为主键聚合并保留同项次多笔融资明细，融合吨数、品名/材质、供应商/钢厂、合同、贷款行、金额、借款日、到期日、提单日、银行交单日、还款日及存续/结案状态。显式 `存续/结案` 作为完成状态权威口径，`提单日`、`银行交单日`、`还款日` 分开处理；页面继续保留原有 `订单融资进度` 与 `融资资金监控` 两页、指标卡、筛选、卡片和明细布局，仅修正字段名称和业务语义。额度监控改为读取 `额度` 页签真实总额度、占用、可用额度和银行条件，预警按项次关联；订单金额单位会依据额度占用对账识别万元口径。
- 来源分支：staging
- commit / deploy：功能提交 `6f9c33d`，静态资源缓存刷新提交 `3508a85`；Render Staging 自动部署已验证，页面加载 `/static/app.js?v=order-finance-three-sheet-20260710-2` 和对应样式版本。
- 是否涉及数据库：不改表结构。为验收三页签取数，Staging 导入当前真实工作簿 70 条 `订单` 记录，归档旧 Excel 来源记录 70 条并插入新记录 70 条；不涉及 Production 数据。
- 验证结果：本地订单融资与权限测试 23 项通过；Python 全量相关回归 90 项通过、1 项已知且与本模块无关的实时行情缓存测试排除；Node 前端测试 49 项通过，`node --check frontend/app.js`、后端 `compileall` 和 `git diff --check` 通过。真实工作簿解析为 70 条融资记录、67 个项次，10 笔存续、57 个已完成项次，存续融资金额 43,850.54 万；额度占用与订单存续金额差额为 0。Staging 浏览器验收确认：订单进度显示 `提单日`、`展期状态`、`还款情况`，不再显示旧 `最迟装船日/船名航次`；明细表按 `提单日/交单日/还款日` 展示；`已完成` 筛选命中 57 张卡且确认状态为 `已结案`。资金监控显示总授信 87,466 万、占用 43,850.54 万、剩余额度 43,615.46 万、使用率 50.1%，UOB 明细 4 条；页面控制台无 error/warn。
- 回滚点：代码可回滚 `3508a85` 和 `6f9c33d`，恢复到发布前 `f104e00`；如需回滚 Staging 订单融资数据，可重新导入上一版台账模板。未发布到 Production。

## 2026-07-09 Staging 订单融资资金监控原型还原与装船字段拆分
- 环境：Staging 测试版
- 发布内容：融资资金监控页按已确认的模拟原型版式重排为 4 列指标卡、左侧银行额度占用列表、右侧主体/供应商拆分与资金日历、下方选中银行明细；银行行选中态使用浅绿色高亮，保留真实台账数据计算结果。订单融资进度卡片将原 `装运节点` 拆分为 `最迟装船日` 和 `船名/航次`，避免已完成业务因有船名而显示船名、其它业务因无船名而显示装船日期的混用问题。
- 来源分支：staging
- commit / deploy：功能提交 `45d0941` / Render Staging 自动部署已验证，测试版 HTML 已加载 `/static/app.js?v=order-finance-prototype-capital-20260709` 和 `/static/styles.css?v=order-finance-prototype-capital-20260709`。
- 是否涉及数据库：否，不改表结构，不写入业务数据；验证只做登录、读取、页面切换、银行行选择和阶段筛选点击。
- 验证结果：本地 `.venv/bin/python -m pytest tests/test_order_finance.py -q` 12 项通过；`node --test tests/auth_frontend.test.mjs tests/info_summary_frontend.test.mjs tests/pnl_colors.test.mjs tests/data_visualization_frontend.test.mjs tests/order_finance_frontend.test.mjs` 48 项通过；`node --check frontend/app.js` 和 `git diff --check` 通过。Staging 浏览器验证通过：管理员登录后进入 `融资资金监控`，资金摘要、银行额度、主体与工厂、资金日历和银行明细结构均存在，点击 UOB 后选中态和明细可用；切回 `订单融资进度` 后页面正常切换，点击 `已完成` 后卡片显示 `最迟装船日` 与 `船名/航次` 两个独立字段，页面控制台无 error。
- 回滚点：回滚 `45d0941` 可恢复到上一版资金监控布局与旧装运节点展示。
- 备注：未发布到 Production；正式版发布前仍需用户确认。

## 2026-07-09 Staging 订单融资阶段筛选与资金监控布局修正
- 环境：Staging 测试版
- 发布内容：订单融资进度页根据当前台账数据自动隐藏无业务的中间阶段筛选项，当前隐藏 `已装船待回款`、`已收汇待还款`、`已回款待结算`，保留 `全部`、`本周重点/高风险`、`已放款待装船`、`已完成`、`多笔融资`；修复从 `融资资金监控` 点击回 `订单融资进度` 时页面仍停留在资金监控的问题；重排 `融资资金监控` 为 4 列资金摘要、银行占用与选中银行明细并排、主体/供应商/到期区间三块拆分，改善阅读顺序。
- 来源分支：staging
- commit / deploy：功能提交 `9d9742b` / Render Staging 自动部署已验证，测试版 HTML 已加载 `/static/app.js?v=order-finance-capital-filter-20260709` 和 `/static/styles.css?v=order-finance-capital-filter-20260709`。
- 是否涉及数据库：否，不改表结构，不写入业务数据；公网验证只做登录、读取、筛选、刷新、文件选择器触发和银行行选择。
- 验证结果：本地 `.venv/bin/python -m pytest tests/test_order_finance.py -q` 12 项通过；`node --test tests/auth_frontend.test.mjs tests/info_summary_frontend.test.mjs tests/pnl_colors.test.mjs tests/data_visualization_frontend.test.mjs tests/order_finance_frontend.test.mjs` 47 项通过；`node --check frontend/app.js` 和 `git diff --check` 通过。本地与 Staging 浏览器点击验证均通过：管理员登录后进入订单融资进度，三个空中间阶段筛选项不可见，`全部`、`本周重点/高风险`、`已放款待装船`、`已完成`、`多笔融资` 均可点击并更新 active 状态，`导入最新台账` 能触发文件选择器，`查看明细/收起明细` 可用；进入融资资金监控后 `刷新`、银行行选择和 `UOB 明细` 展示可用，再点击 `订单融资进度` 能正常切回，控制台无业务错误。
- 回滚点：回滚 `9d9742b` 可恢复到上一版订单融资导入与筛选体验。
- 备注：未发布到 Production；正式版发布前仍需用户确认。

## 2026-07-09 Staging 订单融资导入与筛选体验修正
- 环境：Staging 测试版
- 发布内容：订单融资进度页移除本地路径输入框、`手动新增` 和 `刷新` 按钮，仅保留 `导入最新台账`；点击后打开本地文件选择器并上传 Excel 内容到后端导入，导入报告保留原始文件名。调整 `按合同/项次聚合` 下方阶段筛选行的内边距和垂直位置，筛选按钮默认灰色，当前选中项为绿色，默认选中 `全部`。
- 来源分支：staging
- commit / deploy：`18b1c69` / Render Staging 自动部署已验证。
- 是否涉及数据库：不改表结构；上传导入继续写入/更新 `order_finance_progress`，不涉及生产数据。
- 验证结果：本地 `.venv/bin/python -m pytest tests/test_order_finance.py tests/test_auth_permissions.py -q` 16 项通过；`node --check frontend/app.js`、`py_compile` 和 `git diff --check` 通过。本地浏览器验证确认页面不再出现路径输入框、手动新增和刷新按钮；点击 `导入最新台账` 会打开文件选择器，选择 `YOLANDA和香港建龙出口钢材信用证台账(副本).xlsx` 后导入 70 条记录并聚合为 68 个合同/项次；筛选默认 `全部` 为绿色，切换 `本周重点/高风险` 后该按钮变绿且命中 2 条，其它按钮为灰色，重置后恢复 `全部`。
- 备注：未发布到 Production；正式版发布前仍需用户确认。

## 2026-07-09 Staging 订单融资管理按需求说明重构
- 环境：Staging 测试版
- 发布内容：按 `订单融资管理业务需求说明书` 和已确认页面方案重构订单融资模块，不再沿用旧的单表格 `进度监控` 页面。菜单调整为 `订单融资管理 / 订单融资进度` 与 `订单融资管理 / 融资资金监控` 两个独立页面；订单融资进度按合同/项次聚合，一张卡片可包含多笔融资记录，展示 11 个顶部指标、阶段筛选、8 个信息块、下一步提示和单笔融资明细展开；融资资金监控展示授信额度、当前占用、剩余额度、使用率、接近上限银行、30 天到期金额、银行额度列表、主体/供应商拆分、到期区间和选中银行明细。
- 来源分支：staging
- commit / deploy：`0d9611d` / Render Staging 自动部署已验证。
- 是否涉及数据库：不新增业务表；新增菜单模块 `order_finance_capital` 和对应权限映射，复用 `order_finance_progress` 现有事实数据计算聚合视图和资金监控视图。测试版导入仍写入/更新 `order_finance_progress`，无生产数据操作。
- 验证结果：本地 `.venv/bin/python -m pytest tests/test_order_finance.py tests/test_auth_permissions.py -q` 15 项通过；`node --check frontend/app.js`、`py_compile` 和 `git diff --check` 通过。本地浏览器按钮级验证确认管理员登录后菜单出现 `订单融资进度` 和 `融资资金监控`；订单融资进度点击 `导入最新台账` 后读取 1 个文件、70 条融资记录，聚合为 68 个合同/项次，顶部指标为未结算业务 10、存续融资金额 43,850.54 万、30 天内到期 5、本周重点/高风险 2、已完成 58、数据异常数 1；搜索 `H-2026-3` 命中 1 条，`本周重点/高风险` 命中 2 条，`多笔融资` 命中 2 条，点击 `查看明细` 可展开单笔融资记录。融资资金监控点击刷新后显示总授信额度 106,400 万、当前占用 43,850.54 万、整体使用率 41.2%，银行列表 5 家，点击 UOB 可切换到 `UOB 明细`，控制台无 error/warn。
- 回滚点：回退本次提交可恢复到 `7adcbff` 的旧进度表格页面；如需保留新 Excel 全量导入但撤回页面重构，可仅回退前端页面、`order_finance_capital` 菜单和新增聚合接口。
- 备注：未发布到 Production；正式版发布前仍需用户确认。

## 2026-07-09 Staging 订单融资新模板全量导入
- 环境：Staging 测试版
- 发布内容：订单融资管理 / 进度监控改为读取新模板 `YOLANDA和香港建龙出口钢材信用证台账(副本).xlsx`，解析 `数据合并` 页签中的 2025 和 2026 全部项次。测试版 Render 无法访问本机 `/Users/...` Excel 路径时，使用同一解析结果生成的内置种子数据导入，确保 staging 可直接验收。导入新模板前会归档既有 Excel 来源记录，避免旧六厂 `.xls` 数据与新模板数据混在当前列表；手动新增记录保留，命中同合同 key 时继续合并并保留人工管理字段。
- 来源分支：staging
- commit / deploy：`b548467` / Render Staging 自动部署已验证。
- 是否涉及数据库：不改表结构；测试版通过订单融资导入接口写入/更新 `order_finance_progress`，并归档旧 Excel 来源记录。无删除表、无删除字段、无生产数据操作。
- 验证结果：本地 `.venv/bin/python -m pytest tests/test_order_finance.py tests/test_auth_permissions.py -q` 13 项通过；`node --check frontend/app.js`、`py_compile` 和 `git diff --check` 通过。本地浏览器验证导入新模板后当前列表为 70 条，汇总为在手合同 11、合同总数 70、融资余额 CNY 438,505,396、30 天内到期 6；筛选、关键词、重置、明细、保存管理计划、手动新增弹窗、刷新按钮均可用。Staging `https://ltm-web-staging.onrender.com/?codex=b548467` 加载新静态资源版本 `order-finance-xlsx-all-years-20260709`，管理员登录后进入“订单融资管理 / 进度监控”，点击“导入最新台账”成功导入 70 条，导入报告显示 1 个文件、70 条记录、异常 1，控制台无 error/warn。
- 回滚点：回退 `b548467` 可恢复到旧 `.xls` 目录导入逻辑；如需清理测试库数据，可在 staging 重新导入旧台账或按 `source_file` 归档订单融资测试记录。
- 备注：未发布到 Production；正式版发布前仍需用户确认，并根据正式环境是否能访问 Excel 文件决定使用上传、对象存储或后台管理导入方式。

## 2026-07-08 Production 事中风险监控持仓显示修复
- 环境：Production 正式版
- 发布内容：修复事中风险监控策略组列表接口 500，恢复正式版策略组和持仓明细显示。根因是 2026-07-07 轻量生产化改造后，代码读取 `strategy_groups.created_by / updated_by`，但正式库旧表仍只有 `creator`，导致 `/api/mid-event/groups` SQL 报错。修复方式为启动时兼容式补齐 `strategy_groups.created_by`、`strategy_groups.updated_by`，并从旧 `creator` 回填 `created_by`；新增策略组改为写入 `created_by`；同步修正 Supabase 初始化 DDL 和事中风险持仓建表字段。
- 来源分支：`staging -> main`
- commit / deploy：`5922068` / Render Production 自动部署已验证。
- 是否涉及数据库：是，应用启动时幂等新增 `strategy_groups.created_by`、`strategy_groups.updated_by` 两个兼容列，并从既有 `creator` 回填创建人；不删除表、不删除字段、不重建表、不改持仓业务数据。
- 验证结果：本地 `.venv/bin/python -m pytest -q` 79 项通过；`node --test tests/*.mjs` 44 项通过；Staging `/api/mid-event/groups` 返回 200；正式版 `/api/mid-event/groups` 返回 200，策略组 2 个；第一个策略组持仓明细接口返回 200，持仓 4 条；正式版关键只读接口复扫中，登录、模块、实时信息汇总、风险预警、上海钧能、订单融资、数据可视化、用户、操作日志和事中风险监控均返回 200。
- 回滚点：如生产出现代码问题，可回滚到发布前 `main` 提交 `d446b2b`；本次新增兼容列可保留，不影响旧字段 `creator` 和既有持仓数据。
- 备注：未发现其他关键读取接口存在同类 500；后续应增加真实库 schema 兼容检查，避免代码字段名和线上表结构漂移。

## 2026-07-07 Production Render + Supabase 轻量生产化修复发布
- 环境：Production 正式版
- 发布内容：将 Staging 已验证的 Render + Supabase 轻量生产化改造和后续体验修复发布到正式版。包含访客登录与后端访客权限、登录 loading 状态、手动登录恢复、实时信息汇总自动刷新回补、数据可视化数据管理分页、数据展示全量年份语义恢复、主要列表体验回滚、GZip、轻量 TTL cache、API 性能日志、CORS/session/PBKDF2 基础安全兼容改造。
- 来源分支：`staging -> main`
- commit / deploy：`8b1793b` / Render Production 自动部署已验证。
- 是否涉及数据库：不执行破坏性 migration；应用启动时兼容式幂等新增 `users.is_guest`、`users.cannot_change_password`、`user_sessions.expires_at`。正式版访客登录验证会创建/维护内置访客用户和一条访客 session。
- 验证结果：发布前 Staging `/api/health` 返回 OK；正式版推送后 `https://ltm-web-gt13.onrender.com/api/health` 返回 OK；正式版首页加载 `/static/app.js?v=guest-system-user-20260707` 和 `/static/styles.css?v=guest-system-user-20260707`，登录页包含“以访客身份访问”和登录中提示。正式版 `POST /api/auth/guest-login` 返回显示名“访客”，只包含“信息预警管理 / 实时信息汇总”和“数据可视化管理 / 数据展示”两个菜单；正式版 `admin/admin` 登录返回 200。
- 回滚点：如生产出现代码问题，可回滚到发布前 `main` 提交 `9f36214`；本次无破坏性数据库结构回滚项。如需临时禁用访客入口，可隐藏登录页访客按钮并关闭 `/api/auth/guest-login`。
- 备注：本次发布保留正式版原有 `2026-07-01 Production 实时信息汇总历史缓存刷新修复` 记录；合并冲突仅发生在版本记录文件，业务代码无冲突。

## 2026-07-07 Staging Render + Supabase 轻量生产化改造
- 环境：Staging 测试版
- 发布内容：按 Render + Supabase 现有架构完成轻量生产化改造第一批。包含基础备份脚本和恢复文档、`render.yaml` 移除 `plan` 字段、`integration/latest` SQL 聚合化、主要列表分页/字段白名单、GZip、轻量 TTL cache、API 性能日志、访客登录、后端权限框架、session 过期、PBKDF2 密码哈希兼容升级、CORS 环境变量化、轻量 monitoring status。
- 反馈修正：`26790a0` 取消前端 token 持久化，测试版每次进入均显示登录页，必须手动输入账号和密码；信息预警页面进入时不再自动执行“计算全部”，改为手动点击刷新，避免登录后首屏等待外部行情计算；数据可视化管理 / 数据管理表格增加 `limit/offset` 分页，默认 50 周并提供“加载更多”。
- 续改补充：预警设置、预警历史、用户管理、操作日志接口增加默认 50 / 最大 200 的 `limit/offset` 分页和 `pagination` 元信息；前端预警管理和用户管理兼容新分页返回并显示当前页/总数；数据可视化整合导出和上海钧能台账导出补充 `operation_logs` 记录。
- 体验修正：登录和访客入口增加“正在登录/正在以访客身份进入”的 loading 状态并禁用按钮，避免网络等待时误以为未点击；实时信息汇总首屏先渲染卡片，再后台读取缓存状态；页面从隐藏恢复可见时不再自动触发 `calculateAllInfo` 批量计算，只刷新缓存状态和通知，手动“计算全部”仍保留。
- 核心体验回补：恢复实时信息汇总进入页面后自动执行受控 `calculateAllInfo`，刷新按钮也触发同一批量计算并通过 in-flight 状态避免重复并发；访客保持只读刷新。修复数据可视化 `table/chart` 年份语义，空年份/全选年份表示全部年份，不再默认只查当前年；仅数据管理保留 `limit/offset` 分页和“加载更多”。回收非数据管理页面半截分页影响，预警、用户、操作日志、订单融资、上海钧能默认返回量恢复到原体验所需范围；上海钧能导出不再按页面 200 条截断。
- 访客修正：`/api/auth/me` 对系统访客返回显示名/角色为“访客”，顶部不再显示 `guest`；系统访客从用户管理列表隐藏，并禁止通过用户管理编辑、删除或修改访客权限。访客权限继续由后端 `permissions.py` 固定控制。实时信息汇总计算接口改为 view 权限可调用，访客进入实时信息汇总和点击刷新可正常展示卡片数据，但仍不显示单项计算、导入、导出、编辑、删除等操作。
- 来源分支：staging
- commit / deploy：`b8b2771`、`26790a0` / Render Staging 自动部署已验证。
- 是否涉及数据库：涉及兼容式新增列，由应用启动时幂等执行：`users.is_guest`、`users.cannot_change_password`、`user_sessions.expires_at`。不删除表、不删除字段、不执行破坏性 migration。`POST /api/auth/guest-login` 会在 staging 库创建或重置内置 `guest` 用户权限。
- 验证结果：本地 `.venv/bin/python -m pytest tests/test_auth_permissions.py tests/test_data_visualization.py tests/test_info_summary_backfill.py tests/test_info_summary_rules.py tests/test_order_finance.py tests/test_sh_junneng_ledger.py -q` 75 项通过；`node --test tests/info_summary_frontend.test.mjs tests/data_visualization_frontend.test.mjs tests/auth_frontend.test.mjs` 40 项通过；`py_compile` 和 `git diff --check` 通过。本地 HTTP 验证确认 `admin/admin` 登录 200、guest 登录 200、guest 只返回实时信息汇总和数据展示菜单、guest 可访问 `/api/info-summary/config`、访问订单融资和数据导出返回 403、logout 后返回 401、API gzip 生效、恶意 Origin 不返回 CORS allow header。Staging 验证确认 `/api/health` 200，`https://ltm-web-staging.onrender.com/?codex=26790a0` 已包含“以访客身份访问”且登录框无 `admin/admin` 预填；staging 静态资源不再包含 `localStorage.getItem("token")`，包含 `dvDataLoadMoreBtn` 和 `limit/offset`；staging API 验证 `admin/admin` 登录 200、guest 登录 200、guest 无权限 API 403、logout 后 401，`/api/data-visualization/table?metric=shipment&limit=1&offset=0` 返回 1 行且 `pagination.has_more=true`。
- 未完成项：当前本机会话没有 `DATABASE_URL`，因此 `scripts/backup_database.py` 的 staging full/schema/csv 备份和恢复校验尚未实际执行；需在具备 staging 连接串的安全环境执行，且不得打印连接串。
- 回滚点：回退 `b8b2771`。如需临时回滚会话改动，可先让 token 校验忽略 `expires_at` 并隐藏访客按钮；如需回滚 Render plan 管理方式，可恢复 `render.yaml` 的 `plan` 字段，但不建议这样做。
- 备注：未发布到 Production；正式发布仍需生产备份、staging 备份恢复校验和用户确认。

## 2026-07-01 Production 实时信息汇总历史缓存刷新修复
- 环境：Production 正式版
- 发布内容：将 Staging 已验证的实时信息汇总历史统计修复发布到正式版。历史缓存过旧时不再当作昨日、前日、均值、最小、最大、标准差展示；普通指标历史缓存合约口径改为跟今日值一致的选中月份；新增每天 16:30 后基于收盘价自动刷新历史缓存任务；前端展示历史缓存截至日期和自动收盘缓存状态。
- 来源分支：`staging -> main`
- commit / deploy：`136e12d` / Render Production 自动部署已验证。
- 是否涉及数据库：不改表结构；运行时自动任务会按既有缓存表写入 `daily_prices` 和 `calculated_data`，用于记录收盘价和计算历史统计。
- 验证结果：发布前合并后的 `main` 通过 `.venv/bin/python -m pytest -q`（70 项）、`node --test tests/*.mjs`（40 项）和 `git diff --check`。正式版 `https://ltm-web-gt13.onrender.com/?codex=136e12d` 页面标题正常，加载 `/static/app.js?v=info-summary-stale-history-20260701`，实时信息汇总页面显示过旧历史缓存为 `--` 并标注“历史缓存截至 ...，已刷新今日值”，控制台无 error。16:30 自动任务尚未到点（发布验证时间 16:22），线上实跑待到点后由状态栏展示。
- 回滚点：如生产出现代码问题，可回滚到发布前 `main` 提交 `6dc6e04`；本次无数据库结构回滚。
- 备注：掉期月差和内外盘差历史缺口仍按已确认的正常限制处理。

## 2026-06-30 Production 数据可视化正式库全量刷新与导出修复
- 环境：Production 正式版
- 发布内容：将正式库数据可视化整合库刷新为测试版已验证的全量港口库存整合数据，并修复全量数据放大后「数据整合」导出 Excel 可能 502 的问题。导出实现改为 openpyxl 流式写入，保留 `整合明细`、`发运`、`到港`、`库存`、`表需`、`批次信息` 页签和既有字段。
- 来源数据：`outputs/data_visualization/iron_ore_integrated_full_import_port_inventory_refresh_2026-06-30.xlsx`
- commit / deploy：`8eec5ff` 已导入正式库数据；导出修复提交随本条记录发布到 `main` 并由 Render Production 自动部署。
- 是否涉及数据库：涉及正式库数据替换；导入接口先清空旧 `dv_integrated_points` / `dv_integration_batches`，再写入全量整合 Excel。导入前已导出旧正式库备份 `outputs/data_visualization/production_backup_before_full_import_20260630_154745.xlsx`。
- 验证结果：导入前正式库批次 `id=9` 共 12204 点，年份 2020-2026；测试库批次 `id=22` 共 44631 点，年份 2017-2026。正式库导入后批次 `id=10`，来源文件 `iron_ore_integrated_full_import_port_inventory_refresh_2026-06-30.xlsx`，共 44631 点，其中库存 36752、发运 2786、到港 2702、表需 2391，年份 2017-2026，摘要与测试版一致。导出修复发布前本地通过 `.venv/bin/python -m pytest tests/test_data_visualization.py -q`（39 项）、`node --test tests/data_visualization_frontend.test.mjs`（28 项）和 `git diff --check`。
- 回滚点：如需回滚数据，可在正式版数据管理重新导入备份文件 `production_backup_before_full_import_20260630_154745.xlsx`；如需回滚导出代码，可回退本条导出修复提交。
- 备注：本次完成后，正式版「数据管理」和「数据整合」均以 44631 点全量整合库为最新基线；后续只需在「数据整合」导入新增 Mysteel 模板，再下载整合 Excel 进入「数据管理」上传。

## 2026-06-30 Production 数据可视化大文件导入与库存图表修复
- 环境：Production 正式版
- 发布内容：将 Staging 已验证的数据可视化大文件整合 Excel 导入链路发布到正式版，并补发库存图表年份颜色和 hover 命中修复。数据管理的整合 Excel 导入改为后台预检和后台提交，前端轮询任务状态，避免大文件预检/导入请求在 Render 上因请求体或同步处理过重失败；库存图表年份颜色扩展为 24 个互异颜色，并按最近数据点命中 hover，避免颜色标记年份与悬浮提示年份不一致。
- 来源分支：`staging -> main`
- commit / deploy：`d2c4110` 大文件导入链路；`8eec5ff` 库存图表修复；Render Production 自动部署已验证。
- 是否涉及数据库：不改表结构；本次只发布代码，未向正式库重新导入整合 Excel，也未修改正式库数据。后续可在数据整合导入 Mysteel 模板 Excel，下载整合 Excel 后，在数据管理上传该整合 Excel 进行正式库导入。
- 验证结果：发布前在合并后的 `main` 通过 `.venv/bin/python -m pytest tests/test_data_visualization.py -q`（38 项）、`node --test tests/data_visualization_frontend.test.mjs`（28 项）、`node --check frontend/app.js` 和 `git diff --check`。正式版 `/api/health` 返回 OK；正式版静态 `app.js` 已加载 `#0f766e` 新颜色和 `findClosestChartHitPoint` 最近点 hover 逻辑。
- 回滚点：如生产出现代码问题，可回滚到发布前 `main` 提交 `d2c4110` 撤销图表修复，或回滚到 `533daf4` 撤销本轮大文件导入链路变更。
- 备注：正式版发布已完成；生产数据仍保持发布前状态。

## 2026-06-30 Production 数据可视化主流矿最终名单修正
- 环境：Production 正式版
- 发布内容：将 Staging 已验证的数据可视化主流矿最终名单和历史标签同步逻辑发布到正式版。主流矿固定为 `PB粉`、`麦克粉`、`纽曼粉`、`金布巴粉`、`超特粉`、`混合粉`、`卡粉`、`巴混`、`SP10粉`、`几内亚（粉矿）`、`杨迪粉`、`罗伊山粉`、`罗伊山MB粉`、`印度（粉矿）`、`IOC6`、`PB块`、`纽曼块`、`SP10块`、`南非（块矿）`、`罗伊山MB块`、`PMI块`、`印度（球团）`、`乌克兰（精粉）`、`卡拉拉精粉`，并按该顺序展示。
- 来源分支：`staging -> main`
- commit / deploy：`8ef43f8` / Render Production 自动部署已验证。
- 是否涉及数据库：不改表结构；正式版访问数据可视化筛选、表格、图表和导出接口前会幂等同步历史整合数据的 `mainstream_status`。本次未重新上传整合 Excel，因为正式版当前批次已与测试版摘要一致。
- 验证结果：发布前在合并后的 `main` 通过 `.venv/bin/python -m pytest -q`（53 项）、`node --test tests/*.mjs`（34 项）、`node --check frontend/app.js` 和 `git diff --check`。正式版 `/api/health`、登录、`/api/data-visualization/filters`、`/api/data-visualization/integration/latest` 均返回 200；主流品种池 24 项完整且顺序正确，`missing=[]`、`overlap=[]`、`orderOk=true`。正式版当前批次 `iron_ore_integrated_production_2026-06-29.xlsx` 共 12204 点，其中库存 4216、发运 2786、到港 2702、表需 2500；与测试版批次 `staging_roundtrip_integrated_2026-06-29.xlsx` 在总点数、指标计数、品种/种类/国家/周数、日期范围和主流品种池上无差异。
- 回滚点：如生产出现代码问题，可回滚到发布前 `main` 提交 `f195c43`；本次没有额外上传生产数据，如需回滚标签可重新导入发布前生产整合 Excel。
- 备注：正式版发布已完成。

## 2026-06-30 Staging 数据可视化主流矿最终名单修正
- 环境：Staging 测试版
- 发布内容：按最终业务口径修正主流矿名单与排序：`PB粉`、`麦克粉`、`纽曼粉`、`金布巴粉`、`超特粉`、`混合粉`、`卡粉`、`巴混`、`SP10粉`、`几内亚（粉矿）`、`杨迪粉`、`罗伊山粉`、`罗伊山MB粉`、`印度（粉矿）`、`IOC6`、`PB块`、`纽曼块`、`SP10块`、`南非（块矿）`、`罗伊山MB块`、`PMI块`、`印度（球团）`、`乌克兰（精粉）`、`卡拉拉精粉`。其中业务名称 `BRBF` 对应现有命名 `巴混`，`SP10` 对应 `SP10粉`，`几内亚` 对应 `几内亚（粉矿）`，`南非块` 对应 `南非（块矿）`。
- 是否涉及数据库：不改表结构；测试版访问筛选、表格、图表和导出接口前仍会幂等同步历史整合数据的 `mainstream_status`。
- commit / deploy：`249ca75` / Render Staging 自动部署已验证。
- 验证结果：本地 `.venv/bin/python -m pytest -q` 53 项通过，`node --test tests/*.mjs` 34 项通过，`node --check frontend/app.js` 和 `git diff --check` 通过。测试版 `/api/health`、登录和 `/api/data-visualization/filters` 均返回 200；主流品种池返回上述 24 项且顺序完全一致，`missing=[]`、`overlap=[]`、`orderOk=true`，非主流池 92 项且不包含上述主流项。
- 回滚点：回退 `249ca75`。
- 备注：未发布到 Production；正式发布仍需单独确认。

## 2026-06-30 Staging 数据可视化主流矿扩展与历史标签同步
- 环境：Staging 测试版
- 发布内容：扩展主流矿定义并按现有命名规则排序展示：原有主流名单优先，新增 `罗伊山MB粉`、`印度（粉矿）`、`IOC6`、`罗伊山MB块`、`PMI块`、`印度（球团）`、`乌克兰（精粉）`、`卡拉拉精粉`，新增部分按粉矿、块矿、球团、精粉顺序排列。主流判断从仅按品种名改为支持 `品种 + 种类` 身份，避免 `印度（粉矿）`、`印度（球团）`、`乌克兰（精粉）` 这类现有命名无法正确入主流。
- 历史数据同步：筛选、表格、图表和导出前会幂等同步 `dv_integrated_points.mainstream_status`，将旧整合数据物理更新到新主流定义；改标后如遇同一业务键重复，保留较新的目标标签记录并删除旧冲突记录；影响库存/到港/表需时会重算表需并刷新批次计数。
- 是否涉及数据库：不改表结构；测试版访问数据可视化接口时会对测试库历史整合数据执行上述标签同步。本次不改变 `is_calculable` 规则，主流矿定义不等于表需可计算范围。
- commit / deploy：`6e586b5` / Render Staging 自动部署已验证。
- 验证结果：本地 `.venv/bin/python -m pytest -q` 53 项通过，`node --test tests/*.mjs` 34 项通过，`node --check frontend/app.js` 和 `git diff --check` 通过。使用本地真实历史整合 Excel `iron_ore_mainstream_historical_integrated_2026-06-26.xlsx` 解析 11268 行并重导到临时库验证，导出中 `巴混`、`印度（球团）` 均为主流。测试版 `/api/health` 返回 OK；`/api/data-visualization/filters` 主流池按新顺序返回且非主流无重叠；测试版聚合表格 `主流矿合计`、`非主流矿合计` 均返回 25 行；测试版导出 Excel 共 12204 行，`巴混`、`印度（粉矿）`、`印度（球团）`、`乌克兰（精粉）` 均为主流。
- 未完成项：本机项目和 Documents 路径下未找到 6 月 Mysteel 原始模板文件，因此本地未执行“6 月原始模板文件重新导入”；测试版验证使用测试库现有最新整合数据和导出结果。
- 回滚点：回退 `6e586b5`；如需回滚测试库标签，可重新导入发布前导出的整合 Excel。
- 备注：未发布到 Production；正式发布仍需单独确认。

## 2026-06-30 Staging 数据可视化主流矿定义与筛选联动更新
- 环境：Staging 测试版
- 发布内容：按新的业务定义固定主流矿范围和展示顺序：`PB粉`、`麦克粉`、`纽曼粉`、`金布巴粉`、`超特粉`、`混合粉`、`卡粉`、`巴混`、`SP10粉`、`几内亚粉`、`杨迪粉`、`罗伊山粉`；主流品种池、数据管理表格和数据展示图表按该顺序返回当前有数据的品种。选择主流矿或非主流矿品种池时，后端同步按归一化后的 `主流/非主流` 状态过滤，避免旧整合数据中历史状态残留导致主流池混入非主流品种；非主流品种保持原有展示规则。
- 来源分支：staging
- commit / deploy：`e6547b4` 主流矿定义与排序；`a878160` 读层归一化过滤；Render Staging 自动部署已验证。
- 是否涉及数据库：不改表结构、不重导数据；旧整合数据在读取时按新主流矿定义重新归一化展示。
- 验证结果：本地 `.venv/bin/python -m pytest -q` 51 项通过，`node --test tests/*.mjs` 34 项通过，`node --check frontend/app.js` 和 `git diff --check` 通过。测试版 `/api/health` 返回 OK；`/api/data-visualization/filters` 中主流品种池按新顺序返回 `PB粉`、`麦克粉`、`纽曼粉`、`金布巴粉`、`超特粉`、`混合粉`、`卡粉`、`巴混`、`SP10粉`、`杨迪粉`、`罗伊山粉`，无超出新主流名单的品种，且上述主流品种未出现在非主流品种池；2026 年发运表和图表在 `product_pool=mainstream` 下按当前有数据的主流品种子集返回。
- 回滚点：回退 `a878160` 和 `e6547b4`。
- 备注：未发布到 Production；正式发布仍需单独确认。

## 2026-06-30 Production 数据可视化国家级品种展示名修复
- 环境：Production
- 发布内容：将 Staging 已验证的数据可视化国家级品种展示名修复发布到正式版：国家级发运显示为 `国家（全品种）`，国家对应品类显示为 `国家（种类）`，例如 `俄罗斯（全品种）`、`俄罗斯（球团）`、`俄罗斯（精粉）`；高级筛选里的来源/国家和种类仍保持独立原字段，品种池、数据管理表格和图表统一使用展示名，并能映射回原始整合数据查询。
- 来源分支：`staging -> main`
- commit / deploy：`b4604c5` / Render Production 自动部署已验证。
- 是否涉及数据库：不改表结构、不重导数据；仅调整后端筛选池、表格和图表的展示标签与查询映射。
- 验证结果：发布前在合并后的 `main` 通过 `.venv/bin/python -m pytest -q`（50 项）、`node --test tests/*.mjs`（34 项）、`node --check frontend/app.js` 和 `git diff --check`。正式版 `https://ltm-web-gt13.onrender.com/?codex=b4604c5` 页面标题正常、控制台无 error；`/api/health` 返回 OK；`/api/data-visualization/filters` 中非主流和自定义品种池已包含 `俄罗斯（全品种）`、`俄罗斯（球团）`、`俄罗斯（精粉）`，不再包含原始 `俄罗斯`；用 `俄罗斯（全品种）` 查询发运表返回 6 行，用 `俄罗斯（精粉）` 查询库存表返回 4 行，图表 series 返回 `俄罗斯（精粉）`。
- 回滚点：如生产出现代码问题，可回滚到发布前 `main` 提交 `68e8f3b`；本次无数据库结构或业务数据回滚项。
- 备注：正式版发布已完成。

## 2026-06-30 Staging 数据可视化国家级品种展示名修复
- 环境：Staging 测试版
- 发布内容：修复数据可视化管理中非主流国家级品种的展示名：国家级发运显示为 `国家（全品种）`，例如 `俄罗斯（全品种）`；国家对应品类显示为 `国家（种类）`，例如 `俄罗斯（球团）`、`俄罗斯（精粉）`。高级筛选里的来源/国家和种类仍保持独立原字段，品种池、数据管理表格和图表统一使用展示名，并能映射回原始整合数据查询。
- 来源分支：staging
- commit / deploy：`85d352a` / Render Staging 自动部署已验证。
- 是否涉及数据库：不改表结构、不重导数据；仅调整后端筛选池、表格和图表的展示标签与查询映射。
- 验证结果：本地 `.venv/bin/python -m pytest tests/test_data_visualization.py -q` 34 项通过，`node --test tests/data_visualization_frontend.test.mjs` 24 项通过，`node --check frontend/app.js` 和 `git diff --check` 通过。测试版 `https://ltm-web-staging.onrender.com/?codex=85d352a` 页面标题正常、控制台无 error；`/api/data-visualization/filters` 中非主流和自定义品种池已包含 `俄罗斯（全品种）`、`俄罗斯（球团）`、`俄罗斯（精粉）`，不再包含原始 `俄罗斯`；用 `俄罗斯（全品种）` 查询发运表返回 6 行，用 `俄罗斯（精粉）` 查询库存表返回 4 行，图表 series 返回 `俄罗斯（精粉）`。
- 回滚点：回退 `85d352a`。
- 备注：未发布到 Production；正式发布仍需单独确认。

## 2026-06-29 Production 数据可视化整合/管理/展示发布
- 环境：Production
- 发布内容：将 Staging 已验证的数据可视化整合、数据管理和数据展示更新发布到正式版。包含 Mysteel 新模板整合、空白数量按 0 记录、发运/到港/库存/表需口径拆分、库存细颗粒度命名规范、数据管理长列名展示与数值居中、整体对比筛选、图表维度一致性和高级筛选冲突修复。
- 来源分支：`staging -> main`
- commit / deploy：`4912743` 合并发布；Render Production 自动部署已验证。
- 是否涉及数据库：不改表结构、不执行迁移；本次正式验证未导入或修改生产业务数据，仅执行正常登录和只读数据展示接口验证。
- 验证结果：合并前在 `main` 通过 `.venv/bin/python -m pytest -q`（42 项）、`node --test tests/*.mjs`（33 项）和 `git diff --check`；正式版 `https://ltm-web-gt13.onrender.com/api/health` 返回 200，首页返回 200；正式版页面已加载 `/static/app.js?v=dv-chart-display-consistency-20260629` 和 `/static/styles.css?v=dv-chart-display-consistency-20260629`；浏览器验证「数据展示 -> 品种对比图 -> 整体对比 -> 取消非主流矿合计」后页面状态正确、控制台无错误；正式版只读图表接口确认只返回 `主流矿合计`，即使附带冲突参数 `mainstream_status=非主流` 也不返回 `非主流矿合计`。
- 回滚点：如生产出现代码问题，可回滚到发布前 `main` 提交 `6ff64c9`；本次无数据库结构或业务数据回滚项。
- 备注：正式版发布已完成。生产数据是否补导最新 Mysteel/历史整合数据，需要另行按业务数据发布流程执行。

## 2026-06-24 Production 实时信息汇总批量刷新发布
- 环境：Production
- 发布内容：将 Staging 已验证的实时信息汇总批量刷新优化发布到正式版；包含一次批量请求刷新全部卡片、同轮实时行情去重并发抓取、今日值每轮实时抓取、按用户当前选择月份/年份构造合约、历史统计优先读取缓存、自动刷新间隔调整为 60 秒并避免重叠刷新。
- 来源分支：`staging -> main`
- commit / deploy：`de0ffb4` / Render Production 自动部署已验证
- 是否涉及数据库：否，不改表结构、不执行迁移；功能运行时继续读取既有缓存表，历史缓存刷新仍需用户主动触发。
- 验证结果：发布前在 `staging` 和合并后的 `main` 均通过 `.venv/bin/python -m unittest tests/test_info_summary_rules.py tests/test_info_summary_backfill.py`、`node --test tests/info_summary_frontend.test.mjs tests/pnl_colors.test.mjs`、`git diff --check`。正式版验证：`https://ltm-web-gt13.onrender.com/api/health` 返回 200；首页返回 200，并引用 `/static/app.js?v=info-summary-2026062402`。
- 回滚点：如生产出现代码问题，可回滚到发布前 `main` 提交 `76c2ba8`；本次不涉及数据库结构回滚。
- 备注：正式版发布已完成。当前卡片刷新耗时仍取决于外部实时行情源响应，尤其 SGX/Barchart 方向。

## 2026-06-29 Staging 数据可视化主流矿划分口径修复
- 环境：Staging 测试版
- 发布内容：将数据可视化整合层的主流矿口径调整为“澳巴发运模板中澳洲已映射品种 + 巴西卡粉”；`巴混` 不再归入主流矿，`RTX`、`RTX块`、`高锰粉矿`、`未知`、`库宾粉` 等未映射项不进入主流矿。保存整合数据时同步迁移同一品种同一业务周的旧主流状态，避免筛选池出现旧/新状态重复。
- 来源分支：staging
- commit / deploy：`a5c6a5e` / Render Staging 自动部署已验证。
- 是否涉及数据库：不改表结构；测试版使用现有最新整合数据导出再导入生成批次 21，来源 `staging_roundtrip_integrated_2026-06-29.xlsx`，总点数 12204，其中库存 4216、发运 2786、到港 2702、表需 2500。
- 验证结果：本地 `.venv/bin/python -m pytest tests/test_data_visualization.py -q` 31 项通过，`node --test tests/data_visualization_frontend.test.mjs` 24 项通过，`git diff --check` 通过。测试版 `https://ltm-web-staging.onrender.com/?codex=a5c6a5e` 页面标题正常、控制台无 error；最新数据重导前 `SP10粉` 在非主流、`巴混` 在主流，重导后 `SP10粉` 在主流且不在非主流，`巴混` 在非主流且不在主流，`卡粉` 在主流。主流到港表 290 行且包含 `SP10粉`、不含 `巴混`；非主流库存表 322 行且包含 `巴混`，示例来源为 `巴西 / 粉矿 / 非主流`；聚合图表接口正常返回 `主流矿合计`。
- 回滚点：回退 `a5c6a5e`；如需回滚测试数据，可重新导入批次 20 的整合 Excel。
- 备注：未发布到 Production；正式发布仍需单独确认。未上传本机 Mysteel 工作簿到公网测试版；本次刷新使用测试版已有整合数据导出再导入。

## 2026-06-29 Staging 上海钧能台账小数数量修复
- 环境：Staging 测试版
- 发布内容：修复上海钧能台账新增交易时“数量”字段不能输入小数的问题；新增交易数量输入框补充 `min="0.0001"` 和 `step="0.0001"`，与“本次平仓数量”的小数规则保持一致。
- 来源分支：staging
- commit / deploy：`d0476f1` / Render Staging 自动部署已验证。
- 是否涉及数据库：否；网页测试中临时创建的 `TEST2606` 测试记录已通过页面删除，今日交易、即期持仓、已平仓数据中均无残留。
- 验证结果：新增失败优先测试确认修复前会被 `shJunnengTradeQuantity` 缺少小数步长拦截；修复后 `node --test tests/data_visualization_frontend.test.mjs` 24 项通过，`node --test tests/*.mjs` 34 项通过，`.venv/bin/python -m pytest -q tests/test_sh_junneng_ledger.py` 2 项通过，`node --check frontend/app.js` 和 `git diff --check` 通过。测试版 `https://ltm-web-staging.onrender.com/?codex=d0476f1` 已验证新 HTML 生效：新增交易数量字段 `min/step=0.0001`；页面实测可新增 `TEST2606` 数量 `3.7`，可用“平仓”对该记录提交 `1.2` 的部分平仓，接口返回 200，页面显示已平数量和已平仓明细；控制台无 error/warn。
- 回滚点：回退 `d0476f1`。
- 备注：未发布到 Production；正式发布仍需单独确认。

## 2026-06-29 Staging 上海钧能台账开平仓结构重构
- 环境：Staging 测试版
- 发布内容：将上海钧能台账从旧单表开平仓记录改为兼容式“开仓持仓表 + 平仓明细表”；支持一笔开仓多次部分平仓，平仓弹窗新增“本次平仓数量”，即期持仓显示原始数量、已平数量和剩余数量，已结算持仓按每次平仓逐笔展示；同步修正项目文档、菜单和操作日志中的公司名称为“上海钧能”，并在业务需求文档中记录名称规范。
- 来源分支：staging
- commit / deploy：`6fb609b` 上海钧能台账部分平仓重构；`cbe93ea` 静态资源版本更新；Render Staging 自动部署已验证。
- 是否涉及数据库：涉及测试版表结构新增/迁移；新增 `sh_junneng_positions` 和 `sh_junneng_close_trades`，保留旧 `sh_junneng_trades` 并补齐兼容字段，启动迁移会将旧单表数据回填到新开仓/平仓结构。
- 验证结果：本地 `.venv/bin/python -m pytest -q` 45 项通过，`node --test tests/*.mjs` 35 项通过，`node --check frontend/app.js`、`PYTHONPYCACHEPREFIX=/tmp/ltm-pycache .venv/bin/python -m compileall backend/app` 和 `git diff --check` 通过。测试版 `https://ltm-web-staging.onrender.com/?codex=cbe93ea` 已加载 `/static/app.js?v=sh-junneng-partial-close-20260629`，页面标题/副标题显示“上海钧能台账”，错误写法无残留；即期持仓表头显示“原始数量 / 已平数量 / 剩余数量”，已结算表头显示“本次平仓数量 / 持仓状态”；平仓弹窗已验证存在“本次平仓数量”并默认带入当前剩余数量；控制台无 error/warn。本次浏览器验证未提交新增、平仓或删除业务数据。
- 回滚点：回退 `cbe93ea` 和 `6fb609b`；如需回滚测试版表结构，应先备份并评估 `sh_junneng_positions` / `sh_junneng_close_trades` 中新录入测试数据。
- 备注：未发布到 Production；正式发布仍需单独确认。

## 2026-06-29 Staging Mysteel 球团库存品种命名统一
- 环境：Staging 测试版
- 发布内容：修复 Mysteel 库存明细新版中「球团」sheet 的国家列命名归一问题：`乌克兰` 库存统一写入品种 `乌克兰球`，`印度` 库存统一写入品种 `印球`，并与历史整合数据保持 `主流` 口径一致；保存层同步迁移此前测试库中已写入的旧键，避免 `乌克兰` / `印度` 球团旧列与新列并存。
- 来源分支：staging
- commit / deploy：`c964567` 统一球团库存品种命名；`6489fb7` 迁移旧品种键；Render Staging 自动部署已验证。
- 是否涉及数据库：不改表结构；已在测试版重新导入 3 个 Mysteel 文件生成批次 18，输入点数 982，更新 222 条，跳过相同值 760 条，当前整合库总点数 12216，其中发运 2786、到港 2702、库存 4228、表需 2500。
- 验证结果：本地失败优先测试覆盖球团页 `乌克兰` / `印度` 表头归一和旧键迁移；`.venv/bin/python -m pytest -q` 37 项通过，`node --test tests/*.mjs` 29 项通过。测试版预览接口确认 `乌克兰` -> `乌克兰球`、`印度` -> `印球` 已生效；测试版数据管理接口确认 2026 年 6 月 W23-W26 的 `乌克兰球` 库存均返回有效值，`乌克兰` 在球团类别下已无残留，`印球` 6 月库存正常返回。
- 回滚点：回退 `6489fb7` 和 `c964567`；如需回滚测试数据，可重新导入批次 17 前的整合 Excel。
- 备注：未发布到 Production；正式发布仍需单独确认。

## 2026-06-29 Staging Mysteel 空白数量按 0 整合
- 环境：Staging 测试版
- 发布内容：调整数据可视化管理 / 数据整合的 Mysteel 模板读取规则：当模板中已列出品种/国家/种类，且该统计周存在，但数量单元格为空白时，按 `0` 写入整合明细；未列出的品种或不存在的统计周仍不生成记录，数据管理和数据展示继续显示为 `-`。
- 来源分支：staging
- commit / deploy：`a811a4d` / Render Staging 自动部署已验证
- 是否涉及数据库：不改表结构；已在测试版重新导入 3 个 Mysteel 文件生成批次 17，输入点数 982，新增 55 条，当前整合库总点数 12216，其中发运 2786、到港 2702、库存 4228、表需 2500。
- 验证结果：新增失败优先测试覆盖澳洲发运、澳洲到港、巴西卡粉发运、巴西折算到港、全球非主流发运和库存空白数量按 0 记录；本地 `tests/test_data_visualization.py` 20 项通过，`.venv/bin/python -m pytest -q` 34 项通过，`node --test tests/*.mjs` 29 项通过。测试版合成 Mysteel 预览从旧规则 4 条变为新规则 10 条且包含 0 值；测试版数据管理接口确认 `2026-06-08` 澳大利亚球团发运显示 `0.0`，不是缺失。
- 回滚点：回退 `a811a4d`；如需回滚测试数据，可重新导入批次 16 使用的整合 Excel。
- 备注：未发布到 Production；正式发布仍需单独确认。

## 2026-06-29 Staging 数据可视化整合全流程复测与展示修复
- 环境：Staging 测试版
- 发布内容：将数据管理、数据展示指标页签顺序统一为「发运、到港、库存、表需」，默认打开发运；修复数据管理页先加载表格、后加载筛选项导致默认显示「暂无数据」的问题，改为等待年份/品种筛选项初始化完成后再请求表格；同步更新 `app.js` 缓存参数。
- 来源分支：staging
- commit / deploy：`9e91809` 页签顺序调整；`0c1136f` 触发测试版部署；`6611cc4` 数据管理默认空表修复；Render Staging 自动部署已验证。
- 是否涉及数据库：不改表结构；本次测试版完整流程写入测试库，最终批次为 16，来源文件 `staging_integrated_flow_20260629.xlsx`，状态 `committed`，总点数 12161，其中发运 2762、到港 2680、库存 4228、表需 2491。
- 验证结果：测试版已加载 `/static/app.js?v=dv-data-filter-init-20260629`；端到端验证完成「数据整合导入 3 个 Mysteel 文件 -> 下载整合 Excel -> 数据管理导入下载的整合 Excel -> 数据管理/数据展示正常展示」。下载文件大小 1969482 字节，导入预览 12161 条、错误 0、重复业务 key 0，确认导入返回「已导入 12161 条数据」。数据管理四个页签均有数据：发运 286 行、到港 290 行、库存 322 行、表需 272 行；数据展示四个页签 canvas 均可见且无错误状态。本地 `node --check frontend/app.js` 通过，`node --test tests/*.mjs` 29 项通过，`.venv/bin/python -m pytest -q` 33 项通过。
- 已发现问题：Mysteel 3 文件整合接口实际写入成功生成批次 15，但客户端等待 240 秒仍超时，说明大文件整合接口响应时间偏慢；本次未改后端性能，只记录为后续优化项。数据管理默认空表问题已在 `6611cc4` 修复并验证。
- 回滚点：回退 `6611cc4` 可撤销数据管理默认空表修复；回退 `9e91809` 可撤销页签顺序调整。测试数据可通过重新导入上一版整合 Excel 恢复。
- 备注：未发布到 Production；正式发布仍需单独确认。

## 2026-06-29 Staging 数据整合 Mysteel 澳洲发运补读
- 环境：Staging 测试版
- 发布内容：补充读取 Mysteel 澳巴发货模板中 `澳洲发货量` sheet 的「澳洲铁矿石发货总量（分品种）」区域，将 PB粉、PB块、混合粉、纽曼块、纽曼粉、超特粉、金布巴粉、麦克粉等澳洲分品种发运写入 `shipment`；到港仍读取 `澳洲预计到达中国锚地量`，表需仍按到港+库存计算，不用发运替代到港。
- 来源分支：staging
- commit / deploy：650193c / Render Staging 自动部署已验证
- 是否涉及数据库：不改表结构；已在测试版重新导入 3 个 Mysteel 文件并生成批次 14，输入点数从 768 增至 927，当前整合库总点数 12161，其中 `shipment` 2762 条。
- 验证结果：本地新增失败优先测试覆盖澳洲分品种发运，修复后 `tests/test_data_visualization.py` 19 项通过，`.venv/bin/python -m pytest -q` 33 项通过，`node --check frontend/app.js` 通过，`node --test tests/*.mjs` 27 项通过；测试版预览接口返回 `total_points=927`；测试版批次 14 导入返回 inserted 149、updated 161、skipped 617；只读接口确认 2026 年 6 月主流发运 W23-W25 已包含 PB块、PB粉、卡粉、混合粉、纽曼块、纽曼粉、超特粉、金布巴粉、麦克粉等。
- 回滚点：回退 650193c；如需回滚测试数据，可清理批次 14 对应覆盖结果或重新导入上一版整合数据。
- 备注：未发布到 Production；正式发布仍需单独确认。

## 2026-06-29 Staging 数据整合增量合并与日期去重修复
- 环境：Staging 测试版
- 发布内容：修复数据可视化管理 / 数据整合的增量合并口径：统一整合 Excel 与 Mysteel 上传产生的日期字段为 `YYYY-MM-DD`，避免同一业务周因 `T00:00:00` 被拆成多行；Mysteel 巴西卡粉发运表新增写入 `shipment`；Mysteel 增量写入后按当前完整整合库重算表需；数据管理表格和汇总图按业务年份/业务周聚合，兼容测试库中已有的混合日期格式。
- 来源分支：staging
- commit / deploy：dd3cb51 / Render Staging 自动部署已验证
- 是否涉及数据库：不改表结构；后续 Mysteel 增量上传会更新 `dv_integrated_points` 并重算整合表需。已存在的测试库旧数据无需迁移即可在查询展示层按业务周合并显示。
- 验证结果：`/api/health` 返回 OK；测试版 `https://ltm-web-staging.onrender.com/?codex=dd3cb51` 可打开；只读接口验证 2026 年 6 月到港由混合日期拆行收敛为 5 个业务周，W23-W26 覆盖 PB块、PB粉、卡粉、混合粉、纽曼块、纽曼粉、超特粉、金布巴粉、麦克粉等主流品种，W24 另含澳大利亚球团；本地 `.venv/bin/python -m pytest -q` 33 项通过，`node --check frontend/app.js` 通过，`node --test tests/*.mjs` 27 项通过。
- 回滚点：回退 dd3cb51。
- 备注：未发布到 Production；正式发布仍需单独确认。澳洲 Mysteel 当前模板可按品种读取的是“预计到中国锚地量”，本次继续归入到港，不伪造为澳洲品种发运。
## 2026-06-26 Staging 侧栏一级菜单背景强化
- 环境：Staging 测试版
- 发布内容：为左侧一级菜单标题增加独立背景色与圆角，突出一级菜单层级；二级菜单保持透明背景和原有样式。同步更新静态资源版本，避免浏览器缓存旧样式。
- 来源分支：staging
- commit / deploy：8f7cc96 / Render Staging 自动部署已验证
- 是否涉及数据库：否
- 验证结果：`node --test tests/*.mjs` 27 项通过；`.venv/bin/python -m pytest -q` 29 项通过；`node --check frontend/app.js` 和 `git diff --check` 通过；测试版页面已加载 `/static/styles.css?v=sidebar-title-bg-20260626` 与 `/static/app.js?v=sidebar-title-bg-20260626`；浏览器已验证一级菜单背景为 `rgba(255, 255, 255, 0.08)`、二级菜单背景保持透明，折叠交互正常。
- 回滚点：回退 8f7cc96。
- 备注：未发布到 Production；正式发布仍需单独确认。

## 2026-06-26 Staging 侧栏菜单折叠与数据整合文案统一
- 环境：Staging 测试版
- 发布内容：将左侧一级菜单顺序调整为「台账管理 / 信息预警管理 / 数据可视化管理 / 后台管理」；一级菜单改为可点击折叠/展开，并将一级菜单字号调为 15px、二级菜单字号调为 14px；数据整合页上传入口文案由「上传 Excel」统一为「导入 Excel」。
- 来源分支：staging
- commit / deploy：ee8bb03、c25d4d9 / Render Staging 自动部署已验证
- 是否涉及数据库：否
- 验证结果：`node --test tests/*.mjs` 27 项通过；`.venv/bin/python -m pytest -q` 29 项通过；`node --check frontend/app.js` 和 `git diff --check` 通过；测试版页面已加载 `/static/styles.css?v=sidebar-collapse-font-20260626` 与 `/static/app.js?v=sidebar-collapse-font-20260626`；浏览器已验证菜单顺序、一级/二级菜单字号、折叠展开状态，以及数据整合页「导入 Excel」文案。
- 回滚点：回退 c25d4d9 和 ee8bb03。
- 备注：未发布到 Production；正式发布仍需单独确认。

## 2026-06-26 Staging 数据整合 - 增量上传与页面精简
- 环境：Staging 测试版
- 发布内容：将数据可视化管理 / 数据整合改为上传 Excel 后自动整合并写入基础历史库；移除普通页面中的「读取本地模板」「写入本地整合结果」「写入上传结果」和样例数据表，仅保留「上传 Excel」「下载整合 Excel」及整合概览。后端整合写入从全量替换改为按业务唯一键增量合并：新周新增，重复周新值覆盖旧值，相同值跳过，空值不覆盖已有有效值；下载整合 Excel 改为导出当前完整历史库。
- 来源分支：staging
- commit / deploy：073058b / Render Staging 自动部署已验证
- 是否涉及数据库：是，写入现有 `dv_integrated_points` / `dv_integration_batches` 数据；不新增表结构。Staging 已用一份 Mysteel 原始模板做真实上传验证，生成批次 11。
- 验证结果：`node --test tests/*.mjs` 25 项通过；`.venv/bin/python -m pytest -q` 29 项通过；`node --check frontend/app.js`、`PYTHONPYCACHEPREFIX=/tmp/ltm-pycache .venv/bin/python -m compileall backend/app` 和 `git diff --check` 通过；测试版已加载 `/static/styles.css?v=dv-integration-auto-merge-20260626` 与 `/static/app.js?v=dv-integration-auto-merge-20260626`；真实 Staging API 单文件上传返回 200，本次 input_points 187、updated 68、skipped 119、当前 total_points 11999；下载当前完整整合 Excel 返回 200，文件大小约 1.97MB。
- 回滚点：回退 073058b；如需回滚 Staging 测试数据，可按批次或业务键清理本次批次 11 的覆盖结果。
- 备注：未发布到 Production；正式发布仍需单独确认。三文件一次性 API 上传在本机代理通道中出现连接断开，单文件真实上传和下载链路已验证通过。

## 2026-06-26 Staging 数据可视化管理 - 筛选区两行布局调整
- 环境：Staging 测试版
- 发布内容：按验收反馈重排数据管理/数据展示筛选区：数据展示第一行固定为视图模式、品种池、年份，品种因选项较多单独占第二行；数据管理第一行固定为品种池、年份，品种单独占第二行；高级筛选中种类与主流/非主流同一行，来源/国家单独占下一行，并加大同一行维度间隔。
- 来源分支：staging
- commit / deploy：e5b382a / Render Staging 自动部署已验证
- 是否涉及数据库：否
- 验证结果：`node --test tests/*.mjs` 16 项通过；`.venv/bin/python -m pytest` 25 项通过；`node --check frontend/app.js` 和 `git diff --check` 通过；测试版页面已加载 `/static/styles.css?v=dv-filter-layout-20260626c` 与 `/static/app.js?v=dv-filter-layout-20260626c`；本地有数据页面和测试版页面均已截图确认布局结构，控制台无 error/warn。
- 回滚点：回退 e5b382a。
- 备注：未发布到 Production；正式发布仍需单独确认。

## 2026-06-26 Staging 数据可视化管理 - 筛选区横向分组微调
- 环境：Staging 测试版
- 发布内容：按验收反馈继续调整数据管理/数据展示筛选区：将品种池、年份、品种，以及高级筛选中的种类、来源/国家、主流/非主流改为横向分组排列；各维度标题在上、选项在下；保留全选/全不选按钮当前小尺寸。
- 来源分支：staging
- commit / deploy：9accb1f / Render Staging 自动部署已验证
- 是否涉及数据库：否
- 验证结果：`node --test tests/*.mjs` 16 项通过；`.venv/bin/python -m pytest` 25 项通过；`node --check frontend/app.js` 和 `git diff --check` 通过；测试版页面已加载 `/static/styles.css?v=dv-filter-layout-20260626b` 与 `/static/app.js?v=dv-filter-layout-20260626b`；数据展示页筛选区横向分组可见，控制台无 error/warn。
- 回滚点：回退 9accb1f。
- 备注：未发布到 Production；正式发布仍需单独确认。

## 2026-06-26 Staging 数据可视化管理 - 筛选区与图表显示美化
- 环境：Staging 测试版
- 发布内容：优化数据可视化管理中数据管理/数据展示的筛选区排版，统一筛选按钮高度、字号、间距和高级筛选样式；图表容器改为固定可视高度并在容器内滚动，避免全品种图谱撑出页面；品种对比图取消用虚线区分品种，改为多颜色实线和右侧图例，避免线尾文字堆叠。
- 来源分支：staging
- commit / deploy：0465270 / Render Staging 自动部署已验证
- 是否涉及数据库：否
- 验证结果：`node --test tests/*.mjs` 16 项通过；`.venv/bin/python -m pytest` 25 项通过；`node --check frontend/app.js` 和 `git diff --check` 通过；测试版页面已加载 `/static/styles.css?v=dv-filter-layout-20260626` 与 `/static/app.js?v=dv-filter-layout-20260626`；测试版数据展示页可打开，筛选区高度约 99px，图表容器内部滚动生效，控制台无 error/warn。
- 回滚点：回退 0465270。
- 备注：未发布到 Production；正式发布仍需单独确认。

## 2026-06-26 Staging 数据可视化管理 - 筛选与整合导入预检修复
- 环境：Staging 测试版
- 发布内容：修复整合 Excel 导入后的数据管理/数据展示筛选链路：默认勾选全部筛选项，支持全不选返回空结果，主流/非主流多选改为后端 IN 查询；整合表无旧数据点 id 的单元格不再触发内联编辑；整合 Excel 预检新增数据类型、数值有效性和重复业务 key 统计。
- 来源分支：staging
- commit / deploy：d8d5fab / Render Staging 自动部署已验证
- 是否涉及数据库：否，不新增表结构；使用现有 Staging Supabase 数据表。
- 验证结果：`node --test tests/*.mjs` 11 项通过；`.venv/bin/python -m pytest` 23 项通过；`node --check frontend/app.js` 和 `git diff --check` 通过；本地整合文件预览/导入 823 条通过；测试版页面已加载 `/static/app.js?v=dv-filter-fix-20260626`，数据管理和数据展示页面可打开且控制台无错误。
- 回滚点：回退 d8d5fab。
- 备注：未发布到 Production；正式发布仍需单独确认。

## 2026-06-25 Staging 数据可视化管理 - Bug 修复
- 环境：Staging 测试版
- 发布内容：修复三个 bug：(1) 数据管理导入整合 Excel 后，图表页（数据展示）的筛选控件不刷新，因 dvChartControlsInitialized 标记未重置；(2) 导入对话框文件输入和预览状态在关闭/取消后未清理；(3) index.html 中旧卡粉占位文本替换为通用「品种」；(4) backend 中 Query(regex=...) 替换为 Query(pattern=...) 消除 DeprecationWarning。
- 来源分支：staging
- commit / deploy：4041f53 / Render Staging 自动部署
- 是否涉及数据库：否
- 验证结果：node --check frontend/app.js 通过；pytest 7 项通过；导入流使用 /api/data-visualization/import/integrated/preview 和 commit 已验证。
- 回滚点：回退到 93f964e
## 2026-06-25 Staging 数据可视化管理 - 前端整合改造
- 环境：Staging 测试版
- 发布内容：数据管理和数据展示模块前端 JS 改造，消费整合版 Excel 和 dv_integrated_points 表。导入改走 /api/data-visualization/import/integrated/preview 和 commit；筛选维度从旧卡粉硬编码改为动态读取 /api/data-visualization/filters，支持品种、种类、来源/国家、主流/非主流多维度联动筛选；移除硬编码 DV_PRODUCT_LIST，改用通用 buildCheckboxes；图表和数据表格同步支持新筛选参数。
- 来源分支：staging
- commit / deploy：待提交 / Render Staging 自动部署
- 是否涉及数据库：否
- 验证结果：node --check frontend/app.js 通过；git diff --check 通过
- 回滚点：回退本次 app.js 改动
- 备注：未发布到 Production。HTML 筛选行已在上次提交中预留。


## 2026-06-25 Staging 数据可视化管理 - 数据整合初版
- 环境：Staging 测试版
- 发布内容：新增「数据可视化管理 / 数据整合」二级功能，支持读取本地 Mysteel 三模板或上传多个 Excel，解析澳洲预计到中国锚地量、巴西中国大陆发运量估算卡粉、全球其他国家发运、港口库存明细，并生成标准整合长表；整合结果前置生成统计周日、业务年份、业务周次、周次标签；支持下载整合版 Excel，包含整合明细、发运、库存、表需、批次信息 5 个 sheet；数据管理和数据展示接口优先读取整合表，整合表为空时回退旧卡粉 MVP 数据。
- 来源分支：staging
- commit / deploy：staging 推送后触发 / Render Staging 自动部署待验证
- 是否涉及数据库：是，新增 dv_integration_batches / dv_integrated_points 两张表；不触碰正式数据库。
- 验证结果：本地 `.venv/bin/python -m pytest tests/test_data_visualization.py` 7 项通过；`node --check frontend/app.js` 通过；`git diff --check` 通过；本地接口抽样返回整合 823 条、表需 76 条；浏览器验证「数据整合 / 数据管理 / 数据展示」均可打开并读取整合结果。
- 回滚点：回滚本次数据整合相关后端、前端、测试和表结构改动；如需清理测试数据，可清空 dv_integration_batches / dv_integrated_points。
- 备注：未发布到 Production；正式发布仍需单独确认。初版先以本地模板验证，页面已预留多文件上传入口。

## 2026-06-24 Staging DV 种子数据替换为真实数据
- 环境：Staging
- 发布内容：将数据可视化的种子数据从随机合成数据替换为建龙 Excel 真实卡粉数据。发运 284 行（2021-01-03 ~ 2026-06-07），卡粉库存 272 行（2021-01-05 ~ 2026-06-09），2026 年数据至 6 月份截止，不再有全年虚构数据。种子数据通过 match_and_merge_weeks + recalc_apparent_demand 同一流程入库。
- 来源分支：staging
- commit / deploy：b6baf89 / Render Staging 自动部署待验证
- 是否涉及数据库：否，新增表已存在；种子数据重新入库时会清除旧种子数据并重建
- 验证结果：git push staging 成功；代码语法检查通过。数据源验证：Excel 文件存在 `/Users/wangjingze/建龙/期货组/本地数据库/副本铁矿data base.xlsx` (2.8MB)，发运 col 2/51，库存 col 1/2。
- 回滚点：如需回滚，回退 b6baf89；种子数据可用旧版随机数据或手动重新导入
- 备注：未发布到 Production。首次部署后需要等待 Render 启动时 seed_dv_data() 自动执行，清空旧种子数据后重新填入真实数据。

## 2026-06-24 Staging 数据可视化管理 - 卡粉 MVP
- 环境：Staging
- 发布内容：新增一级菜单「数据可视化管理」，含图表数据管理和数据展示两个子页。支持上传 Excel 导入卡粉库存和发运数据、自动归属业务周、自动计算表需、表格内联编辑、人工修正保护、Canvas 折线图按年份展示。
- 来源分支：staging
- commit / deploy：bcb0d49 及后续 / Render Staging 自动部署待验证
- 是否涉及数据库：是，新增 dv_week_keys / dv_data_points / dv_import_batches / dv_change_log 四张表，db.MODULES 新增 data_visualization 条目
- 验证结果：.venv/bin/python tests/test_data_visualization.py 6 项通过；node --check frontend/app.js 通过；前端 DOM 引用缺失已修复
- 回滚点：如需回滚，回退 bcb0d49 及后续；可保留四张 DV 表或手动 drop
- 备注：未发布到 Production；正式发布仍需单独确认。Staging 浏览器全流程验证尚未完成。非管理员用户需要手动赋予 data_visualization 模块权限。

## 2026-06-24 Staging 实时信息汇总批量刷新优化
- 环境：Staging
- 发布内容：将实时信息汇总的「计算全部 / 自动刷新」从 8 张卡片串行请求改为一次批量请求；同一轮刷新中共享的实时行情只抓取一次，今日值仍在每次刷新时重新抓取，用户切换年份/月别后按当前选择构造合约；历史统计不在每次刷新时重新联网计算，优先读取已有缓存，缺失当天缓存时读取最近可用历史缓存补齐展示。
- 来源分支：staging
- commit / deploy：`e0475bc`、`fc8a357` / Render Staging 自动部署已验证
- 是否涉及数据库：否，不改表结构；本次只读既有历史缓存，不新增回填写入。
- 验证结果：`.venv/bin/python -m unittest tests/test_info_summary_rules.py tests/test_info_summary_backfill.py` 通过；`node --test tests/info_summary_frontend.test.mjs tests/pnl_colors.test.mjs` 通过；`git diff --check` 通过。Staging API 实测 `/api/info-summary/calculate-all` 一次返回 8 张卡片，耗时约 26.64 秒；煤矿比今日值 `3.17`、标准差 `0.21`，内外盘差2 的 09 月今日值 `-1.66`，批量请求统计为 requested 43 / unique 22。
- 回滚点：如需回滚，回退 `e0475bc` 和 `fc8a357`；不涉及数据库回滚。
- 备注：未发布到 Production；正式发布仍需单独确认。当前耗时仍受外部实时行情源影响，尤其是 SGX/Barchart 方向；本次优化目标是去重、并发和避免重复历史计算，不缓存跨刷新轮次的今日值。

## 2026-06-24 Staging 实时信息汇总自动刷新复测与重入修复
- 环境：Staging
- 发布内容：复测 DS 提交的实时信息汇总自动刷新功能时发现单轮全量计算耗时超过 30 秒会导致定时器重入，多轮计算重叠后状态栏长期被单项「已计算 / 计算中」覆盖，难以稳定显示「自动刷新完成」。本次为自动刷新增加进行中保护，上一轮未结束时跳过下一次定时触发；同时将前端 JS 版本参数更新为 `info-summary-20260624`，避免浏览器继续使用旧脚本缓存。
- 来源分支：staging
- commit / deploy：`ee5390f`、`2fa0d3d` / Render Staging 自动部署已验证
- 是否涉及数据库：否
- 验证结果：`.venv/bin/python -m unittest tests/test_info_summary_rules.py tests/test_info_summary_backfill.py` 通过；`node --test tests/info_summary_frontend.test.mjs tests/pnl_colors.test.mjs` 通过；`git diff --check` 通过。测试版浏览器验证：页面加载 `/static/app.js?v=info-summary-20260624`，初始状态为「页面已加载并完成计算」，自动刷新触发后约 66 秒完成并稳定显示「自动刷新完成」，控制台无 error/warn；煤矿比标准差 `0.21`、盘面钢厂利润标准差 `38.49`，未回落为 `--`。
- 回滚点：如需回滚，回退 `ee5390f` 和 `2fa0d3d`；不涉及数据库回滚。
- 备注：未发布到 Production；正式发布仍需单独确认。掉期月差、内外盘差、内外盘差2 的历史统计缺口仍按既有已知限制处理。

## 2026-06-23 Staging 实时信息汇总自动刷新修复
- 环境：Staging
- 发布内容：修复实时信息汇总模块「实时更新：开启」显示但实际无自动刷新的问题。新增 startInfoSummaryAutoRefresh 和 stopInfoSummaryAutoRefresh，在进入模块时启动 30 秒间隔定时器自动重新计算全部指标；离开模块时停止定时器。
- 来源分支：staging
- 修改文件：frontend/app.js
- 是否涉及数据库：否
- 验证结果：本地代码审查已确认定时器逻辑正确（与事中风险监控的 5 秒定时器模式一致）
- 回滚点：回滚本 commit 即可
- 备注：自动刷新仅重新计算今日指标（从缓存读取），不触发大规模联网回填。

## 2026-06-23 Staging 回填 std 缺失修复
- 环境：Staging
- 发布内容：修复 get_existing_calculated_dates 将 std_value=NULL 的缓存行视为「已存在」导致回填跳过、煤矿比和盘面钢厂利润等标准差永远不补足的问题。修改两处：cache_service 过滤条件增加 AND std_value IS NOT NULL；calculate_info_summary 增加 std_value 缺失时触发重算的条件。
- 来源分支：staging
- 修改文件：backend/app/cache_service.py、backend/app/main.py
- 是否涉及数据库：否
- 验证结果：9 个后端测试 + 4 个前端测试全通过
- 回滚点：回滚本 commit 即可
- 备注：未发布到 Production。

## 2026-06-23 Staging 更新
- 环境：Staging
- 发布内容：盈亏颜色修正，正值为红色 (`var(--danger)`)、负值为绿色 (`#087443`)；实时信息汇总历史缓存回填机制：新增统一回填服务 `info_summary_backfill.py`，通过新浪拉取国内期货历史行情写入 `daily_prices` 并计算 `calculated_data`，前端新增「刷新历史缓存」按钮和状态展示；支持增量回填、过期数据自动清理（超过 210 天）、幂等写入；FE 铁矿石和内外盘差暂跳过，待数据源就绪后再接入。
- 来源分支：staging
- commit / deploy：`f13e745` / Render Staging 自动部署已验证
- 是否涉及数据库：是（只写 `daily_prices` 和 `calculated_data`，不改表结构）
- 验证结果：全部 18 个测试通过（12 Python + 6 Node）；Staging 公网 API 实测：Config 含掉期月差且 month_options_by_type 正确，Cache Status 返回全部 8 个指标，Backfill 国内期货 skipped（已最新）、FE 掉期月差 skipped（源未就绪）、内外盘差 skipped（未实现）。
- 回滚点：回滚 `ae1b9d5`~`19a095a` 共 7 个 commit 即可；因不涉及表结构变更，回滚后 `daily_prices` 和 `calculated_data` 旧数据仍可正常使用。
- 备注：未发布到 Production；正式发布需要 Production 单独确认。掉期月差仅展示今日值（FE 历史源未就绪），内外盘差/内外盘差2 历史回填暂未实现。

本文件用于记录测试版更新和正式版发布。只记录业务目的、验证结果、环境信息和回滚线索；不得记录密码、完整数据库连接串、Deploy Hook 或其他密钥。


## 2026-06-23 Staging 信息汇总缓存回填效率优化
- 环境：Staging
- 发布内容：
  1. 增量数据拉取：SinaHistoryProvider 和 StaticHistoryProvider 支持 since_date 参数，回填时只获取近 210 天的数据，避免全量下载。
  2. 增量写入：价格行与计算结果跳过 DB 中已存在的日期。
  3. 自动清理：每天回填后自动删除 daily_prices 和 calculated_data 中超过 210 天的旧数据。
  4. 修复 delete_old_daily_prices / delete_old_calculated_data 在连接关闭后访问 conn.total_changes 的 bug。
  5. 修复 force=true 时增量逻辑仍跳过已计算日期的问题。
- 来源分支：staging
- 修改文件：backend/app/info_summary_backfill.py、backend/app/cache_service.py
- 是否涉及数据库：否
- 验证结果：12 个后端测试 + 4 个前端测试全通过
- 回滚点：回滚本 commit 的上述文件改动即可
- 备注：未发布到 Production。FE 历史行情源仍是已知缺口。

## 2026-06-23 Staging 信息汇总缓存回填机制完善
- 环境：Staging
- 发布内容：补齐信息汇总历史缓存回填机制中缺失的关键功能——
  1. 回填结果追踪：后台回填结果现在写入模块级变量，`GET /api/info-summary/cache/status` 的 `last_backfill` 字段返回最近一次回填的完整结果（不再是永远 `null`）。
  2. `force` 参数生效：`force=false`（默认）时，若 `daily_prices` 已有近 2 日内数据则跳过，避免重复拉取；`force=true` 时强制执行回填和重算。
  3. FE 铁矿石历史行情不可用时，掉期月差回填返回 `skipped`（而非 `failed`），明确标记"FE 历史行情源未就绪"。
  4. 内外盘差/内外盘差2 回填规则尚未实现时返回明确跳过提示。
- 来源分支：staging
- 修改文件：`backend/app/info_summary_backfill.py`、`backend/app/main.py`、`tests/test_info_summary_backfill.py`
- 是否涉及数据库：否，不改表结构。
- 验证结果：`test_info_summary_backfill` 9 个测试全通过，`test_info_summary_rules` 3 个测试全通过，前端测试 4 个全通过。
- 回滚点：回滚本 commit 的 3 个文件改动即可。
- 备注：未发布到 Production；FE 历史行情和内外盘差历史回填仍在第二阶段设计中。

## 记录模板

### YYYY-MM-DD Staging 更新 / Production 发布
- 环境：Staging / Production
- 发布内容：
- 来源分支：
- commit / deploy：
- 是否涉及数据库：否 / 是（说明结构变更、迁移或数据变更）
- 验证结果：
- 回滚点：
- 备注：

## 2026-06-26 Staging 数据可视化导入与筛选修复
- 环境：Staging
- 发布内容：数据可视化管理同步四类数据展示后的数据管理筛选逻辑，数据管理页新增“主流矿 / 非主流矿 / 整体对比 / 自定义”品种池，并将种类、来源/国家、主流/非主流收纳到高级筛选；整合 Excel 导入确认阶段增加长任务提示和超时提示，避免页面一直停留在“导入中”；表格接口支持主流矿合计与非主流矿合计的整体对比。
- 来源分支：staging
- commit / deploy：`6598f0d` 修复导入与筛选；`2ef337d` 增加测试版健康检查脚本；Render Staging 自动部署已生效。
- 是否涉及数据库：是，仅测试版写入一份 5 条合成整合 Excel 验证数据，不涉及表结构变更，不影响 Production。
- 验证结果：`python3 scripts/check_staging_health.py --attempts 12 --sleep 10 --timeout 20` 返回 `/api/health` 200；测试版首页已引用 `/static/app.js?v=dv-data-filter-import-20260626`；静态脚本包含 `dvDataProductPool`、`导入中，请稍候` 和 `导入超时`；测试版整合 Excel 预览与确认导入完成，批次 `codex_staging_integrated_import_check.xlsx` 写入 5 条；筛选接口返回 `aggregate/custom/mainstream/non_mainstream` 品种池；到港整体对比表格返回 `主流矿合计=100`、`非主流矿合计=20`；图表接口返回 `主流矿合计` 和 `非主流矿合计` 两条序列。
- 回滚点：如需回滚，回滚 `6598f0d` 和 `2ef337d`；测试版合成数据可通过后续导入真实整合 Excel 覆盖或按测试库数据清理流程删除。
- 备注：未发布到 Production；正式发布需要单独确认。测试版公网访问过程中偶发 SSL 握手超时，按健康检查脚本重试后确认服务可达。

## 2026-06-23 Staging 开发协作边界更新
- 环境：Staging
- 发布内容：更新项目开发流程备忘，明确本地、`staging`、测试版和新功能分支内，Codex 默认自主完成完整开发闭环，不做许可式反复提问；普通开发、测试、提交、推送测试分支、Render Staging 自动部署、测试版 API/页面验证和明确的测试版临时数据写入清理均默认直接执行。正式版、Production、`main`、生产数据、生产环境变量、正式部署和生产回滚仍需单独确认。
- 来源分支：staging
- commit / deploy：文档更新，待后续随 staging 提交记录
- 是否涉及数据库：否
- 验证结果：已检查项目文档，未发现独立 PRD；项目管理规则集中在 `开发流程_备忘.md` 和本文件。本次仅同步开发协作边界，不改功能代码。
- 回滚点：回滚本次 `开发流程_备忘.md` 和 `版本更新记录.md` 文档改动即可。
- 备注：本条不代表 Production 发布。

## 2026-06-23 Staging 实时信息汇总历史缓存回填设计与第一阶段实现
- 环境：Staging
- 发布内容：为信息预警管理的实时信息汇总增加历史缓存回填第一阶段能力：新增后端回填服务、缓存状态接口和后台启动式回填接口；实时信息汇总工具条新增“刷新历史缓存”轻入口；旧桌面缓存继续作为 `daily_prices` / `calculated_data` 的初始化种子，后续回填直接写入同一套缓存表；国内历史源按新浪轻量历史行情接口接入，FE 历史源暂以明确失败状态隔离，不伪造历史数据。
- 来源分支：staging
- commit / deploy：staging 分支本次更新 / Render Staging 自动部署待验证
- 是否涉及数据库：是，使用时会写入既有 `daily_prices` 和 `calculated_data` 缓存表；不涉及表结构变更。
- 验证结果：`.venv/bin/python -m unittest tests/test_info_summary_rules.py tests/test_info_summary_backfill.py` 通过；`node --test tests/info_summary_frontend.test.mjs tests/pnl_colors.test.mjs` 通过；`git diff --check` 通过。
- 回滚点：回滚本次后端回填服务、前端轻入口、测试和依赖改动即可；未改数据库结构。
- 备注：未发布到 Production；正式发布需要单独确认。FE 历史源仍需后续确认稳定数据接口后完整接入。

## 2026-06-23 Staging 盈亏颜色调整
- 环境：Staging
- 发布内容：按国内交易习惯调整盈亏颜色显示，盈利/正数显示红色，亏损/负数显示绿色；同时给 CSS 链接增加版本参数，避免浏览器继续使用旧样式缓存。
- 来源分支：staging
- commit / deploy：`84e06ef` 交换盈亏颜色；`b9b872f` 增加 CSS 缓存破坏参数；Render Staging 自动部署已生效。
- 是否涉及数据库：否
- 验证结果：`node --test tests/pnl_colors.test.mjs` 通过；测试版首页已引用 `/static/styles.css?v=pnl-colors-20260623`；浏览器实际加载规则为 `.numeric-up = var(--danger)`、`.numeric-down = rgb(8, 116, 67)`。
- 回滚点：回滚 `84e06ef` 和 `b9b872f` 即可恢复原颜色显示；不涉及数据库回滚。
- 备注：未发布到 Production；正式发布需要单独确认。

## 2026-06-23 Staging 实时信息汇总新增掉期月差
- 环境：Staging
- 发布内容：实时信息汇总新增 `掉期月差` 指标，放在 `月差` 下方；`月差` 和 `掉期月差` 默认年月改为复刻原始 Python 动态规则；螺矿比和盘面钢厂利润的月份下拉改为仅显示 `01/05/09`；前端 `app.js` 增加版本参数避免旧缓存。
- 来源分支：staging
- commit / deploy：`9506c7e` 已推送到 `staging`，Render Staging 自动部署已生效。
- 是否涉及数据库：否
- 验证结果：`.venv/bin/python -m unittest tests/test_info_summary_rules.py` 通过；`node --test tests/info_summary_frontend.test.mjs tests/pnl_colors.test.mjs` 通过；测试版首页已引用 `/static/app.js?v=info-summary-20260623`；测试版 API 返回指标顺序为 `月差` 后接 `掉期月差`，默认年月为 `2026-09 / 2027-01`；浏览器页面验证 `掉期月差` 卡片已显示，今日值计算返回 `0.35`。
- 回滚点：回滚 `9506c7e` 即可恢复本次实时信息汇总改动；不涉及数据库回滚。
- 备注：未发布到 Production；正式发布需要单独确认。

## 2026-06-21 Staging 环境搭建
- 环境：Staging
- 发布内容：建立测试版 Render 服务和测试版 Supabase 数据库隔离。
- 来源分支：staging
- commit / deploy：Render `ltm-web-staging` 首次部署成功；commit `d57b9c8`
- 是否涉及数据库：是，创建并初始化测试数据库 `LTM WEB STAGING`，不影响正式数据库。
- 验证结果：测试版 `/api/health` 正常，网页登录成功并能读取缓存数据；正式版 `/api/health` 正常。
- 回滚点：删除或停用测试服务不会影响正式版；正式版仍由 `main` / `ltm-web` / `LTM WEB` 提供。
- 备注：测试服务 URL 为 `https://ltm-web-staging.onrender.com`。

## 2026-06-22 Staging 更新
- 环境：Staging
- 发布内容：事中风险监控保留一个统一刷新按钮；刷新行情失败时保留已有价格兜底；USD/CNH 价格录入和显示支持 4 位小数；新增策略组后自动选中新组；策略组更新时间显示到秒。
- 来源分支：staging
- commit / deploy：staging 分支本次更新 / Render Staging 自动部署待验证
- 是否涉及数据库：否
- 验证结果：本地静态检查通过；后端刷新兜底逻辑已用不连接数据库的单元验证覆盖。待部署到 Staging 后做浏览器可见验证。
- 回滚点：回滚本次前端和后端代码改动即可；不涉及数据库回滚。
- 备注：未发布到 Production；正式发布需要单独确认。

## 2026-06-22 Staging 环境修复
- 环境：Staging
- 发布内容：补充 `HEAD /` 健康检查响应，避免 Render 对根路径的 HEAD 探测返回 405 后反复判定实例失败。
- 来源分支：staging
- commit / deploy：staging 分支本次更新 / Render Staging 自动部署待验证
- 是否涉及数据库：否
- 验证结果：待部署到 Staging 后验证实例不再反复失败，并继续完整测试信息预警管理板块。
- 回滚点：回滚本次 `HEAD /` 路由改动即可。
- 备注：未发布到 Production；正式发布需要单独确认。

## 2026-06-22 Staging 交互修复
- 环境：Staging
- 发布内容：修复新增策略组后未自动选中新组的问题；策略组更新时间去掉时区后缀，仅显示到秒；策略组和持仓保存失败时给出明确提示。
- 来源分支：staging
- commit / deploy：staging 分支本次更新 / Render Staging 自动部署待验证
- 是否涉及数据库：否
- 验证结果：待部署到 Staging 后复测事中风险监控新增策略组、新增持仓和刷新。
- 回滚点：回滚本次前端交互改动即可。
- 备注：未发布到 Production；正式发布需要单独确认。

## 2026-06-22 Staging 数据库连接修复
- 环境：Staging
- 发布内容：修复数据库连接上下文退出后未关闭连接的问题，避免 Supabase session pool 被请求和后台扫描耗尽。
- 来源分支：staging
- commit / deploy：`9f510d2` 修复连接关闭；`b272989` 触发 Supabase 重启后的 Staging 重新部署
- 是否涉及数据库：否，代码级连接释放修复；不改表结构和业务数据。运维上重启了测试版 Supabase 项目以释放旧 session。
- 验证结果：Supabase Staging 从 Healthy 恢复后，`postgres / Supavisor / idle` 连接从 15 降到 2；`HEAD /` 返回 200，`/api/health` 返回 `{"status":"ok"}`，最新前端静态文件已包含 `preferredGroupId` 和保存失败提示修复。
- 回滚点：代码回滚到 `2632256` 前会恢复旧连接泄漏风险，不建议；若本次重试提交有问题，可回滚空提交 `b272989`，但保留 `9f510d2`。
- 备注：未发布到 Production；正式版当前未出现连接池占满，但旧正式代码仍有同类连接泄漏风险，正式发布需要单独确认。

## 2026-06-22 Staging 信息预警管理完整功能测试
- 环境：Staging
- 发布内容：对信息预警管理中的实时信息汇总和事中风险监控做测试版公网 API 功能验证。
- 来源分支：staging
- commit / deploy：`1d56644` 后的 Staging 部署
- 是否涉及数据库：是，仅写入测试版临时测试数据；测试结束后已删除临时指标记录、策略组和持仓。
- 验证结果：实时信息汇总的指标录入、读取、按录入数据计算均通过；卷螺差、月差、内外盘差真实取数样本均返回正常。事中风险监控的策略组新增、编辑、读取、删除和持仓新增、编辑、读取均通过；USD/CNH 开仓价 `6.7387` 保存精度正确，刷新取到 USD/CNH 当前价且无 missing contracts。连续观察中，实时信息汇总 30 秒内 6 次取数未出现空值；事中风险监控 60 秒内 12 次轮询单条持仓浮盈亏、单组盈亏、所有组总盈亏均未回落为 `--` 对应的空值。
- 回滚点：本次为测试记录，无代码回滚需求；功能代码仍以 `9f510d2` 连接修复和此前事中风险监控刷新修复为基础。
- 备注：Chrome 扩展本轮拦截直接打开测试版页面，因此完整功能验证使用已授权的测试版公网 API 完成；未发布到 Production。

## 2026-06-22 Production 发布
- 环境：Production
- 发布内容：发布 Staging 已验证的信息预警管理修复：事中风险监控统一刷新按钮、USD/CNH 4 位价格录入和显示、刷新行情保留旧价兜底、新增策略组自动选中、更新时间显示到秒；同时发布数据库连接关闭修复和 Render `HEAD /` 健康检查修复。
- 来源分支：`staging` 合并到 `main`
- commit / deploy：`f3c5461` 合并发布；随后补充本条生产发布记录
- 是否涉及数据库：否，不改表结构和生产业务数据。
- 验证结果：正式版 `HEAD /` 返回 200，`/api/health` 返回 `{"status":"ok"}`，未登录访问 `/api/mid-event/groups` 正常返回权限提示；正式版前端静态文件已包含 `preferredGroupId`、保存失败提示和 `dateTimeToSecond` 修复。发布过程中有短暂 Render 响应超时，最终复验通过。
- 回滚点：如生产出现代码问题，可回滚到发布前 `origin/main` 的 `d57b9c8`；本次无数据库迁移或数据变更。
- 备注：生产发布前已在 Staging 完成信息预警管理完整功能测试，并已清理测试数据。

## 2026-07-12 Staging 交易管理批量归属修复
- 环境：Staging
- 发布内容：修复全部交易筛选后无法全选、已选数量错乱和归属保存无反馈；全选改为单次请求当前筛选的全部成交 ID，增加选择与保存进度、成功数量及失败信息。
- 来源分支：staging
- commit / deploy：`75ef13f` / Render Staging 自动部署，资源版本 `20260712f`
- 是否涉及数据库：否。Staging 功能验证曾对 1 条未归属成交执行临时归属，验证后已删除并确认恢复为未归属。
- 验证结果：187 项 Python 测试和 76 项 Node 前端测试全部通过；Staging 全部 2,753 条成交 ID 单次获取成功（约 2.0 秒）；临时成交的保存、已归属筛选命中、删除恢复均通过。
- 回滚点：回滚 `75ef13f`。
- 备注：未发布到 Production；正式发布需要单独确认。

## 2026-07-13 Staging 数据展示布局与管理分页调整
- 环境：Staging
- 发布内容：修正期现数据展示的年份、品种筛选左对齐；扩大现货和期现图表可视区；现货数据管理、期现数据管理统一改为默认 20 条并支持 20/50/100、上一页和下一页的服务端分页。
- 来源分支：staging
- commit / deploy：`b45a596` / Render Staging 自动部署，前端资源版本 `data-visualization-pagination-20260713`。
- 是否涉及数据库：否；未修改表结构、业务数据和计算规则，测试请求均为只读数据查询（登录会话除外）。
- 验证结果：本地 T2 共 142 项通过（89 项 Node 前端测试、53 项 Python 只读接口测试），3 个 JavaScript 文件语法检查和 `git diff --check` 通过。Staging 实测期现管理 60,424 条、现货管理默认筛选 286 条，20/50/100、上下翻页及筛选后回到第 1 页均通过；桌面端筛选左边缘差值为 0，两个图表区高度均为 740px；390×844 下图表宽 342px、高 584px且无横向溢出。业务页面无脚本错误；浏览器仅存在项目既有的 `/favicon.ico` 404。
- 回滚点：回滚 `b45a596`；不涉及数据库回滚。
- 备注：未发布到 Production；正式发布需要单独确认。

## 2026-07-15 Staging 订单融资明细显示返工
- 环境：Staging
- 发布内容：交单状态仅显示状态；订单融资明细移除数据来源；统一使用“融资到期日”和“回款状态”文案；第二行两项等宽铺满、固定为与第一行相同高度并禁止文字换行。
- 来源分支：staging
- commit / deploy：`b319992` / Render Staging 自动部署，前端资源版本 `order-finance-display-rework-20260715`。
- 是否涉及数据库：否；未修改表结构、业务数据、风险规则或同步逻辑。
- 验证结果：订单融资前端定向测试 10 项全部通过，JavaScript 语法和差异格式检查通过；Staging 浏览器实测资源版本、字段文案和单笔融资明细表头正确，第一行四项各 224px、第二行两项各 455px，六项高度均为 58px且第二行不换行，控制台无错误。
- 回滚点：回滚 `b319992`；不涉及数据库回滚。
- 备注：本次仅复测返工评估范围；未进入 Gate B，未发布到 Production。

## 2026-07-15 Staging 订单融资状态摘要与异常口径返工
- 环境：Staging
- 发布内容：交单日期自动认定装船时，装船状态精简为“已根据交单日认定装船”；交单状态显示“已交单 / 交单日期”；多笔融资摘要调整为“银行 / 金额（N笔）”并保持单行、超长时省略；“数据异常”只统计系统识别的数据质量问题，不再累计 WPS“预警”页签业务预警，风险评价仍保留完整预警输入。
- 来源分支：staging
- commit / deploy：`dca0e96` 完成状态与异常口径返工，`46aef14`、`e9e5834` 补齐摘要单行和缓存版本；Render Staging 已部署，前端资源版本 `order-finance-status-refinement-nowrap-20260715`。
- 是否涉及数据库：否；未修改数据库结构或业务数据，也未修改 WPS 同步、风险等级和业务阶段规则。
- 验证结果：本地订单融资后端 43 项、前端 10 项定向测试全部通过，Python/JavaScript 语法和差异格式检查通过；Staging 已确认最新 HTML、JavaScript 和 CSS 资源，包含三项目标文案以及摘要单行、省略号规则。`H-2026-3` 业务卡片的实际显示由用户登录后人工验收。
- 回滚点：回滚 `dca0e96`、`46aef14` 和 `e9e5834`；不涉及数据库回滚。
- 备注：当前停在 Gate B 前，未发布到 Production。

## 2026-07-15 Staging 订单融资 ING 银行显示名称精简
- 环境：Staging
- 发布内容：订单融资管理全模块显示时，将 `918ING银行（香港）` 精简为 `ING（香港）`，将 `918ING银行（新加坡）` 精简为 `ING（新加坡）`；底层银行值、筛选、额度匹配、WPS 导入和数据库数据保持不变。
- 来源分支：staging
- commit / deploy：`0d26d69` / Render Staging 已部署，前端资源版本 `order-finance-ing-display-20260715`。
- 是否涉及数据库：否。
- 验证结果：按用户要求不执行功能测试；仅完成 JavaScript 语法和差异格式检查，并确认 Staging 已切换到最新资源版本。
- 回滚点：回滚 `0d26d69`；不涉及数据库回滚。
- 备注：未进入 Gate B，未发布到 Production。

## 2026-07-15 Staging 订单融资回款时点与默认筛选修正
- 环境：Staging
- 发布内容：未回款融资继续按今天与有效融资到期日比较，并明确显示“逾期 N 天未回款”；全部回款后改用实际回款日与有效融资到期日（含新到期日）的差值显示提前、按期或逾期回款；回款状态不再重复显示该差值。每次进入订单融资进度页面时默认选择“本周重点”并清空搜索条件。
- 来源分支：staging
- commit / deploy：`92a4cdf` / Render Staging 已部署，前端资源版本 `order-finance-due-timing-focus-20260715`。
- 是否涉及数据库：否；未修改后台日期字段、风险评价、WPS 同步或业务数据。
- 验证结果：按用户要求不执行功能测试；仅完成 JavaScript 语法和差异格式检查，并确认 Staging 已切换到最新资源版本。
- 回滚点：回滚 `92a4cdf`；不涉及数据库回滚。
- 备注：未进入 Gate B，未发布到 Production。

## 2026-07-16 Staging 订单融资单一 WPS 数据源双库同步
- 环境：Staging
- 发布内容：新增确定性订单融资事实快照、受保护只读快照接口和 `snapshot_follower` 同步模式；测试版不再读取或刷新 WPS 凭证，而是按与正式版一致的 09:00 / 17:00 时点跟随正式版发布的事实快照。测试版和正式版仍使用各自独立数据库，人工管理字段不随快照覆盖。
- 来源分支：staging
- commit / deploy：`d52e8d4` / Render Staging 已部署；当前验收使用 Staging 自回环上游，仅用于在 Production 发布前验证完整快照链路。
- 是否涉及数据库：否；不新增或修改表结构。Staging 仅写入自己的订单融资事实字段和既有同步状态，不连接 Production 数据库。
- 验证结果：本地完整质量门禁 317 项测试全部通过，Python 编译、JavaScript 语法和差异格式检查通过。Staging 页面标题和资源正常；正确密钥访问快照接口返回 200，错误密钥返回 404；快照 schema 版本 1、来源版本 787、记录数和记录数组长度均为 70，事实哈希前缀为 `fbadacf1fcc1`。
- 回滚点：回滚 `d52e8d4` 及此前本功能提交，并将 Staging 同步模式恢复为原配置；不涉及数据库迁移回滚。
- 备注：未发布到 Production。正式发布需 Gate B 单独确认；Production 发布后再将 Staging 上游地址从自回环切换到 Production 快照地址。

## 2026-07-16 Production 订单融资还款风险规则修复
- 环境：Production
- 发布内容：订单已交单且尚未回款时，融资到期日在未来的标记为中风险且不进入本周重点；融资已逾期或当天仍未还款的标记为高风险并进入本周重点；融资到期日缺失时保持中风险并提示补录；全部融资已还款但业务未关闭时为低风险。前端同步补充到期日缺失提示并更新静态资源版本。
- 来源分支：Staging 验收提交 `f83de77` 选择性发布到 `main`。
- commit / deploy：生产代码提交 `15ce5e0`，测试断言对齐提交 `f036c47`；Render Production 自动部署已生效。
- 是否涉及数据库：否；不改表结构、生产业务数据、WPS 同步机制或本地管理字段。
- 验证结果：本地 261 项 Python 测试和 91 项 Node 测试全部通过，订单融资定向测试 49 项 Python 与 11 项 Node 全部通过，JavaScript 语法、Python 编译和 `git diff --check` 通过。正式版 `/api/health` 返回 200 和 `{"status":"ok"}`，静态资源版本为 `order-finance-repayment-risk-20260716`，合同 `H-2026-3 / XL260521PSB063` 实测显示“中风险 / 已交单待回款”、到期日 2026-07-24、待回款 0/2 笔，且不在默认本周重点列表中；页面控制台无错误或警告。页面显示最近一次 WPS 同步为 2026-07-16 09:04，共更新 14 条。
- 回滚点：如生产出现代码问题，可回滚到 `1fe2575`；本次不涉及数据库回滚。

## 2026-07-17 Staging 铁矿石期现快照发布链路
- 环境：Staging
- 发布内容：将铁矿石期现同步明确为 Staging 唯一 EBC/Sina 采集源，新增受保护、内容哈希版本化的结果与计算明细快照；Production 跟随端仅在完整校验后单事务追加缺失业务键，同键差异整包拒绝，不覆盖历史。两个环境继续使用各自独立 Supabase，未建立跨库连接。
- 来源分支：staging
- commit / deploy：`da60eff` / Render Staging deploy `dep-d9ctjrreo5us73b5a8n0` 已 live。
- 是否涉及数据库：无表结构变更；部署启动时既有源端调度按原规则自动补查，未手工触发同步。本次验收只读查询 Staging 数据。
- 验证结果：本地 Python 345 项、Node 98 项全部通过，Python 编译、JavaScript 语法和 `git diff --check` 通过。Staging 显式使用 `source` 模式；正确密钥访问快照返回 200、错误密钥返回 404；schema 版本 1，快照与数据库均到 2026-07-16，共 384 条 API 结果和 384 条明细，最新日 96 组，重复业务键和孤儿明细均为 0。访客页面“期现数据展示”顶部同样显示 2026-07-16，控制台无错误。
- 回滚点：回滚 `da60eff`，并在 Staging 保留或恢复 `IRON_ORE_BASIS_SYNC_MODE=source`；不涉及数据库迁移回滚。
- 备注：尚未发布到 Production。Production 发布前需备份正式数据库、完成 Gate B，并将正式版设置为 `snapshot_follower`；正式版不配置 EBC 凭据。

## 2026-07-20 Staging 交易所日结单/月结单单文件导入
- 环境：Staging
- 发布内容：交易管理导入由成交、平仓、持仓三份 Excel 改为单个交易所 TXT 结算单；自动识别日结单或月结单，月结优先覆盖重叠事实，保留来源版本和字段差异审计；日结单用于期初建立、月中更新或补数。预检展示脱敏账户、期间、成交/平仓/行权弃权/持仓数量及期初连续性结果。
- 来源分支：`codex/settlement-statement-import` 快进到 `staging`。
- commit / deploy：功能提交 `a9db31c`，迁移顺序修复 `35aea19`；Render Staging 自动部署已生效，前端资源版本 `20260720a`。
- 是否涉及数据库：是。Staging 仅执行可向前兼容的新增表、新增列和索引迁移，并为既有有效/已替代批次回填 `is_current`；未修改既有交易业务值。首次部署发现旧表先建新索引导致迁移未落地、交易概览返回 500，已在 `35aea19` 调整为先加列再建索引，复验新增列数和页面查询恢复。回滚代码时保留新增空表/列即可，不执行破坏性删表删列。
- 验证结果：本地 Python 355 项、Node 98 项全部通过，`git diff --check` 通过。真实样本验证：2026-05-29 日结单 190 笔成交、185 笔平仓、579 条持仓，2026-06 月结单 2,753 笔成交、2,351 笔平仓、1 条行权/弃权、669 条持仓；5 月 29 日期末持仓推导 6 月期初的手数差异为 0，两份账单确认后有效事实数量等于两期之和。Staging 浏览器实测标题、资源版本、当前持仓 23 组/4,082 手/保证金 26,177,056.5 元正常，“导入结算单”抽屉仅保留一个 TXT 文件控件并显示日结/月结识别及月结优先说明；修复部署后的新控制台窗口无错误或警告。
- 回滚点：代码回滚到发布前 `3b50266`；新增表、列和索引保持不动，避免破坏已导入来源审计。当前 Staging 尚未通过新入口确认任何测试结算单，因此无需清理测试批次。
- 备注：未发布到 Production；正式发布前必须备份 Production 数据库并完成 Gate B 确认。

## 2026-07-20 Staging 统一业务台账与实时估值
- 环境：Staging
- 发布内容：交易总览新增全部、基础套保、战略套保口径；上海钧能与期权业务台账统一只展示已完成开仓业务归属的分摊份额。上海钧能增加实时持仓浮盈、资金利息、净平仓盈亏及 80%/20% 结算；期权增加三档估值价、IV 与组合 Greeks。平仓、到期和混合归属统一按开平关系分摊，不再给整笔平仓成交保存单一业务归属。
- 来源分支：`codex/trading-ledger-valuation-20260720` 快进到 `staging`。
- commit / deploy：功能与审查修复截至 `8f052b4`；Render Staging 自动部署已生效，前端资源版本 `20260720d`。
- 是否涉及数据库：否；未修改表结构和交易事实数据。行情、浮动盈亏、IV 与 Greeks 仅在接口响应中计算。天勤仅使用环境变量认证的行情会话，不连接真实交易账户或调用交易接口。
- 验证结果：本地 Python 395 项、Node 103 项全部通过，JavaScript 语法和 `git diff --check` 通过；独立复审结论 Critical 0、Important 0、Ready to merge。真实 6 月月结单和 5 月末日结单专项测试通过，事实层保留 1,843 条期权了结记录，未归属期权在业务台账为 0，期初连续性手数差异为 0。Staging 浏览器确认页面标题、静态资源 `20260720d` 和控制台无错误；访客权限不包含交易管理，业务页登录态实测留待有权限账户复验。
- 回滚点：回滚到发布前 Staging `8f1a5ac`；本次无数据库迁移或数据回滚。
- 备注：未发布到 Production；`main`、生产环境变量和生产数据库均未变更。生产发布仍需单独确认。

## 2026-07-20 Staging 交易所 TXT 确认导入性能修复
- 环境：Staging
- 发布内容：结算单确认改为后台任务，前端分别显示事实写入、开平匹配和业务分摊进度；同一预检批次重复点击复用原任务。事实当前版本判断、合约乘数读取、现金变动、期权了结和持仓摘要均改为批量数据库操作，避免大月结单逐条访问远程数据库。
- 来源分支：`codex/trading-ledger-valuation-20260720` 推送到 `staging`。
- commit / deploy：`1b2b8a4`；Render Staging 自动部署已生效，前端资源版本 `20260720e`。
- 是否涉及数据库：无表结构变更。新 TXT 在同一数据库中新增来源批次和事实版本；重叠身份仅切换 `is_current`，旧 Excel 批次、旧事实和来源差异审计均保留。部署前旧版长事务已结束且回滚，6 月 TXT 批次仍为 `preview`，原 Excel 批次仍为 `active`。
- 验证结果：本地 Python 398 项、Node 103 项全部通过，真实 5 月日结单加 6 月月结单完整确认、匹配与分摊测试约 2.4 秒；旧事实保留、新事实成为当前版本的断言通过。Staging `/api/health` 返回 `{"status":"ok"}`，OpenAPI 已包含后台任务查询接口，浏览器确认静态资源 `20260720e` 且控制台无错误。因部署后旧实例临时预检文件失效，需登录后重新选择 6 月 TXT 才能完成真实页面导入验收。
- 回滚点：回滚 `1b2b8a4`；不涉及数据库迁移回滚。若已用新版本确认结算单，回滚代码不会删除已保留的事实版本。
- 备注：未发布到 Production；正式版、生产环境变量和生产数据库均未变更。

## 2026-07-20 Staging TXT 单一事实源与业务台账显示修复
- 环境：Staging
- 发布内容：按当前验收范围暂不处理期初持仓；交易事实切换为 TXT 单一真实数据源，移除旧 Excel 批次、来源行和对应事实，保留已有开仓业务归属并迁移到等价 TXT 开仓身份。TXT 持仓结算价作为实时行情不可用时的明确参考估值；行情提供器改为延迟初始化、失败可重试并返回具体状态。上海钧能平仓页默认定位到最近实际平仓月份，期权台账展示结算价参考、浮动盈亏及天勤行情不可用的具体原因。
- 来源分支：`codex/trading-ledger-valuation-20260720` 推送到 `staging`。
- commit / deploy：`492bf9e`；Render Staging 自动部署已生效，前端资源版本 `20260720f`。
- 是否涉及数据库：是，仅修改 Staging。清理前在私有 schema `codex_backup_20260720_txt_cutover` 完整备份 22 张 `trading_%` 表并逐表核对行数；随后移除 Excel 批次 1–5、36,242 条 Excel 来源行和对应旧事实，迁移 409 条缺失业务归属并清除 Excel 孤儿归属。当前有效事实仅来自 TXT 月结批次 7：成交 2,753、平仓/了结 2,352、持仓 669；有效开仓归属 1,135 条，失效归属 0。备份暂时保留，待业务验收后再决定是否清理。
- 验证结果：完整本地测试 Python 400 项、Node 103 项全部通过；最终定向测试 Python 23 项、Node 20 项通过，Python 编译、JavaScript 语法和差异格式检查通过。Staging 浏览器确认资源 `20260720f`、控制台无错误。上海钧能平仓页默认显示 2026-06-01 至 2026-06-30，共 98 条归属分摊，净平仓盈亏 61,210.28、资金利息 258.74、80%结算 48,761.19、20%结算 12,190.35、手续费 2,989.72，规则版本 `sh_junneng_v1`。期权当前持仓显示 14 组、1,735 手，全部有 TXT 结算价参考，组合浮盈 -186,912.35；因 Staging 未配置天勤行情认证，标的价格、IV 和 Greeks 暂无实时值，页面已明确显示“参考 · 天勤行情认证未配置”。
- 回滚点：代码回滚 `492bf9e`；数据可从私有备份 schema 恢复到清理前状态。恢复属于 Staging 数据变更，执行前需先核对当前新增导入和归属，避免覆盖清理后的新操作。
- 备注：未发布到 Production；`main`、生产环境变量和生产数据库均未变更。若需显示实时价格、IV 和 Greeks，下一步需要单独为 Staging 配置只读天勤行情认证并复验。

## 2026-07-21 Staging 历史 TXT 合约格式兼容修复
- 环境：Staging
- 发布内容：交易所 TXT 解析器新增上期所无连字符期权格式识别，例如 `rb2602c3400`；合约规格补充螺纹钢期权乘数 10、焦煤期货及期权乘数 60，支持 2025 年 12 月至 2026 年 6 月历史月结单连续导入。
- 来源分支：`codex/trading-ledger-valuation-20260720` 推送到 `staging`。
- commit / deploy：`b22c6a1`；Render Staging 自动部署已生效，页面响应 `Last-Modified` 为 2026-07-21 09:34:35（北京时间）。
- 是否涉及数据库：无表结构变更；服务启动时只补充缺失的 3 条合约规格参考数据，不修改交易事实、业务归属或资金数据。本次未向 Staging 上传或确认任何历史结算单。
- 验证结果：本地完整测试 407 项 Python、104 项 Node 全部通过，`git diff --check` 通过。真实 7 份月结单在隔离数据库中按 2025-12 至 2026-06 顺序确认成功，形成 16,019 条成交、13,649 条平仓及到期了结事实，6 段月间持仓连续性差异均为 0。Staging `/api/health` 返回 200 和 `{"status":"ok"}`，页面标题正确且控制台无错误或警告。
- 回滚点：回滚 `b22c6a1` 到 `7f47b62`；已补充的合约规格参考行可保留，不影响旧逻辑。本次无交易事实或业务数据回滚。
- 备注：未发布到 Production；正式版、生产环境变量和生产数据库均未变更。

## 2026-07-21 Staging 交易事实历史初始化
- 环境：Staging
- 发布内容：开平事实匹配从单批次扩展为同账户全部有效批次，能够把跨月平仓关联到前月真实开仓，同时扣除已经被其他平仓占用的开仓与平仓成交数量；随后以 TXT 结算单初始化 2025 年 12 月至 2026 年 7 月 20 日历史事实。
- 来源分支：`codex/trading-ledger-valuation-20260720` 推送到 `staging`。
- commit / deploy：`a6f3f9b`；Render Staging 自动部署已生效。
- 是否涉及数据库：是，仅修改 Staging。初始化前在私有 schema `codex_backup_20260721_before_txt_init` 完整备份 22 张 `trading_%` 表。当前有效批次共 21 个，全部为 TXT：2025 年 12 月至 2026 年 6 月共 7 个月结批次，2026 年 7 月 1 日至 7 月 20 日共 14 个日结批次。旧 6 月批次失效后按时间顺序重建，既有 1,135 条业务归属全部保留并仍能关联有效成交身份。
- 验证结果：本地完整测试 408 项 Python、104 项 Node 全部通过，Python 编译和 `git diff --check` 通过。除首个 2025 年 12 月批次无更早期初可校验外，后续 20 段持仓连续性差异均为 0；2026 年有效事实为成交 15,330 条、平仓及到期了结 13,253 条、期权到期事件 34 条。2026 年开仓分摊未匹配 0 条、平仓成交关联未匹配 0 条、期权事件未匹配 0 条，匹配手数差异为 0。7 月 20 日最新持仓为 708 条来源明细、4,977 手。Staging API 实测总览、事实持仓、导入批次、上海钧能和期权业务台账均返回 200；上海钧能已平仓 440 手，净平仓盈亏 61,210.28、资金利息 258.74、80%结算 48,761.19、20%结算 12,190.35；期权当前归属持仓 131 手并返回估值和 Greeks。
- 回滚点：数据可从私有备份 schema `codex_backup_20260721_before_txt_init` 恢复到初始化前状态；代码可回滚 `a6f3f9b` 到 `633548d`。恢复会覆盖初始化后的 Staging 交易操作，执行前必须重新核对新增归属和导入。
- 备注：4 月首轮匹配曾在 Render 新版本切换期间发生一次数据库死锁，匹配事务自动回滚；失效该批后重新导入成功，最终校验无半成品事实或分摊。未发布到 Production；`main`、正式 Render 和生产数据库均未变更。
