# 2026-09-08 Selective production release

Authorization: user explicitly approved implementation, production deployment and subsequent verification in this task. No second permission required for this frozen scope.

Scope: REL-1 spot/trade ledger; REL-2 effective positions/trades/live FIFO valuation; REL-3 existing visualization (main already equals pre-V2 backend); REL-4 unchanged collectors -> authenticated server replication with durable progress and deduplication; REL-5 verified missing settlement baseline and initial business-source data. Include weekly reports and V2 source integration per the user scope correction on 2026-09-08. Exclude all new financing/lifecycle, research/backtest and closing agents. Do not alter local collector, pairing or real trading terminal.

Assessment D3/T3/R3/C1: bounded existing business modules and their data integration; no new independent system or cross-module business workflow. Whole-module regression for released modules; source-to-follower integration for replication. Single primary agent.

Implementation: main-based isolated worktree; selectively import approved modules and shared adapters; test atomic cursor/retry, schema/environment/account guards, source conflicts, baseline and duplicate replay. Protect endpoints and tables. Candidate real browser acceptance before main push. Existing staging features must be restored after candidate acceptance and source replication integrated without deleting other developers' work.

Acceptance: unchanged 0.3.2 source devices continue heartbeats/ingest; follower imports source observations with verified counts/keys and retries idempotently; official settlement baseline checked; actual ledger/positions/trades/chart pages work; excluded modules remain main baseline. No claim of complete WH6 position-file ingestion (currently zero snapshots).

Rollback: main 2e9f3b5; staging 1379f03. Back up exact affected tables/schema before DB mutation; verify backup and restore in isolation. New additive tables can remain on code rollback; no whole database overwrite. Disable replication before data rollback. Record actual deployed commits and verification after deployment.

Current stage: candidate deployed to Staging; browser acceptance incomplete. Production untouched. Next action: resolve Chrome extension UI blocking automation, finish candidate acceptance, restore full Staging with replication, then complete authorized Production release.

Local evidence: Python 738 passed; two financing tests fail identically on untouched origin/main (dated business-status fixture and outdated Excel sheet fixture), excluded from this release regression. Node 165 passed before final pre-V2 DV CSS selection; DV-specific regression 30 passed. PostgreSQL production full dump 26,804,223 bytes, readable catalog 1,264 entries; public schema restored into isolated local UTF8 database. Candidate schema init verified after PostgreSQL-specific compatibility fixes. Real source replay: 829 observations -> 649 fills / 7 existing conflicts; second replay imports 0. Conflicts concern missing zero fees/close profit versus richer first observations, with same core trade quantities/prices. Original August statement hash matches Staging, continuity passed with zero lot difference; five September daily originals also hash-match and pass continuity. Restored candidate positions: 3,758 lots / 16 groups. No production mutations yet.

Staging candidate 4f5f5a0 dispatched through Render manual specific-commit deployment. This automatically disables Staging auto-deploy; restore On Commit after candidate acceptance and restoration of full Staging plus replication. Production deployment remains pending; current user authorization is valid. Pending business decision: spot ledger production source credentials absent, source-field replication versus direct source configuration asked asynchronously.

Continuation checkpoint: candidate branch 99a9eb8 includes retry/quarantine guards; replication regression 7 passed. Full Staging plus source replication pushed as ee4f056 (origin/staging). Staging live remains candidate 4f5f5a0; auto-deploy remains Off due specific-commit deployment. Browser explicitly reports another Chrome extension UI blocks automation; no further browser operations attempted after that explicit blocker. Candidate JS assets match repository byte-for-byte. Spot list reads 1547 rows, but detail/menu acceptance is incomplete. Production baseline remains 2e9f3b5; no production schema/import/env/main changes performed. Resume browser access first, restore Staging auto-deploy On Commit and deploy full Staging once candidate acceptance ends. Existing collectors were not modified.


2026-09-08 resumed browser acceptance:
- Computer Use dismissed Chrome password-manager notice; no password read or changed.
- Candidate actual administrator paths: spot list 1547 rows, record detail displays system/manual sections; Shandong filter 378 rows and page 2/19 passed. Positions 3758 lots/16 groups; all trades 27088 records, provisional filter 107 records/1054 lots. Shipment charts rendered; basis page date 2026-09-07, optimal warrant basis 17.34. No console error observed.
- Live quotes failed with provider connection unavailable; real-time PNL acceptance remains unresolved (not passed).
- Candidate startup deadlocked and rolled back replica schema. f8bef8e bounded retries verified in runtime but still exhausted; retries alone do not resolve live legacy initializer contention. Independently applied create_wh6_replication_state_20260908 to Staging only; both tables have RLS and no PUBLIC/anon/authenticated privileges. Collector page then showed Updated at 20:17:35. Existing 3 devices retained, ongoing device heartbeats verified. Production must apply required migrations explicitly before activation; do not depend on automatic startup schema creation.
- Preserve existing scheduler behavior when legacy initialization fails (candidate 737a8a4); no claim that legacy deadlock is fixed. Targeted tests: 10 startup/replication passed and 35 collector API/order snapshot passed.
- Full Staging restored by auto-deploy 2fd3ec9 (1m29s), On Commit restored. Actual refreshed administrator page shows weekly report and order lifecycle menus; candidate-only features are no longer hiding Staging development. Production remains 2e9f3b5, not modified. Spot production source choice and real quote validation remain pending.

Scope correction: user explicitly requires weekly PDF generation and V2 original upload, integration package, preview and confirmed import in this release. Ported b0064f1 and its report readiness/cache fixes into main-based candidate, excluding unrelated lifecycle/research imports. Verification: 9 report/source/schema/V2 tests, 33 frontend visualization tests, 66 visualization/auth tests passed. Staging dv_report_runs currently has zero records; real browser generation and production deployment are not yet verified. Billing read-only audit: existing organization is Pro; both Production and Staging are active projects in it, Staging total database size 568 MB. No subscription or compute size changes made.

Actual Staging report page checked after administrator login for 2026-W36: readiness=false, inventory prior/current 0/0, actual arrival 0/0, estimated arrival 0/0, apparent demand 0/0, price 455/455; Generate PDF disabled, no historical reports. This is missing report-input data, not evidence that generating a file in a unit test completed the live user flow. Required authoritative original uploads and confirmed ingestion remain pending before report acceptance.

2026-09-08 continued repair (local candidate only, not a Production acceptance):
- Actual arrival now selects the latest available week at or before the report week, with explicit W35/W34 labels for W36 and exclusion of future actual observations. Inventory/estimated arrival retain the report week.
- Report inventory uses V2 source total-product rows (103 identities), rather than the legacy 100-product compatibility summary. No change to legacy demand formulas or stored legacy values. Real Sep1 source reconciliation: 12542.124659072442 -> 12458.301156444903 tonnes in ten-thousands; matches source totals to <1e-6. Actual arrival 2717.4745 -> 1951.7687.
- Overlapping detail imports skip identical natural identities across files and packages. Conflicting values cancel the entire transaction; PostgreSQL table locks serialize the compare/insert operation. Local SQLite verified transaction rollback. PostgreSQL concurrent acceptance remains required.
- Seven real inventory originals: 39552 input product rows -> 23072 unique rows across 14 weeks. Across all detail tables first import30912 rows; a separate package containing the same sources inserted0. No conflicting product values found among these seven files.
- Avoid full workbook parsing for unrelated shipment/inventory extractors. Latest inventory plus three supply originals parsed in approximately1second locally, V2 workbook round-trip and isolated import succeeded.
- Generated local46-page R2 PDF; source totals and latest-available actual-arrival dates verified in extracted text. Viewed pages1 and34; fixed port-change label overlap after that inspection. This is not full visual/browser acceptance. Private temporary evidence root: /var/folders/4q/9d9z41813rzc3v2z1hz_gdc40000gn/T/weekly-real-tqyfg70q.
- Remaining historical source defect: 41.8MB legacy workbook uses 粉矿 instead of 粗粉, repeats horizontal header blocks, and carries old three-grade categories. Current V2 parser does not recognize it as inventory; current legacy extraction also attempts repeated full workbook loads. Do not claim historical charts complete. Must support this source with explicit old-grade semantics and first-block-only extraction before release.
- Still pending: full Staging upload/confirm/generate/download acceptance, price/history reconciliation, Production quote-provider failure, collector/follower and settlement prerequisites, and spot-ledger source decision. Production unchanged; no collector installation/configuration/binding changes.

2026-09-08 report review delivery (user requests a finished PDF before further release work):
- Historical reader now accepts 粉矿 sheets, first horizontal blocks, first repeated named columns, English port labels and Total. It retains old three-grade identity, maps old mid-grade to the compatible mid-low series, and records out-of-order source dates. Real 41.8MB history parsed in ~32s; 2019-01-07 excluded as the documented out-of-order observation.
- Renderer uses observed dates for historical stocks so two observations in one business week are not incorrectly added. It breaks high-grade history at the definition change and classification history at recorded membership-count changes; separates historical/current estimated-arrival series. Port pages now include mainstream/non-mainstream values, distinct leaf identities, and summary rates/contributions. Demand page shows inventory reduction and major product changes. Price lookup uses the latest date in each week. Renderer identity updated to version2 in snapshot hashing.
- Delivered artifact: canonical output/pdf/铁矿石周报_2026W36_修复后审阅版.pdf,46 pages/46 bookmarks, R3. Uses candidate renderer with original source facts and the previously archived Production read-only price/legacy snapshots from2026-09-07. This is a local report artifact, not a completed live browser upload/import/generation acceptance or Production deployment.
- Independently reconciled both periods:103 inventory leaves,24 mainstream identities,four grades,15 ports;47 arrival ports and each country/product/form slice. Basis differences verified for91 comparable quote pairs;58 existing demand formulas match raw inventory. All46 pages rendered and visually inspected; no text-span overlap, bounds overflow, or empty-history placeholder panels. Existing historical Karara anomalies withheld, original files preserved. Fixed sources and checks recorded privately in release-evidence/selective-20260908/report-review-input.json and report-review-checks.json.56 relevant tests passed.
- Production remains unchanged. Live workflow acceptance, historical/current import conflict handling and source precedence, quote provider, and previously recorded release prerequisites remain separate pending work; this PDF must not be used to claim those gates passed.

2026-09-08 release continuation and clarified data ownership:
- User explicitly clarified: initialize Production history from existing Staging data where required; future browser uploads, V2 integration and report generation must operate directly in Production, without ongoing Staging data dependency. Do not implement a permanent spot-ledger Staging-source mirror on the basis of the earlier proposal.
- User separately confirmed WH6 may temporarily retain the existing collector -> Staging -> Production path, with installed collectors and bindings unchanged. This exception does not apply to browser uploads.
- Fresh read-only audit: Production lacks the V2 report/source fact tables and collector/spot ledger tables; active settlement coverage stops at July (daily July22, monthly July31), while Staging includes August monthly and September7 daily. Thus data completeness and web one-click report equivalence are not yet accepted; Production remains unchanged.
- Fixed historical report loader leaking later weeks into inventory/estimated-arrival/demand appendices. Reproduced with a failing regression, added report-week cutoff across historical tables, bumped renderer identity to prevent stale snapshot reuse. Six report tests pass with the project Python environment. This is local verification, not live release acceptance.
- Live Staging administrator recheck after dismissing the Chrome password-manager popup via Computer Use: report W36 data check and report-history list initially show request failures; Generate PDF remains disabled. Render logs show a service restart and startup database transaction retry during this interval; root cause is not yet established. Read-only DB counts: port inventory0, inventory summary0, report runs0. Do not describe this state as successful report generation. Next action: resolve the live failure, then exercise the actual V2 upload/confirm/generate path with authoritative sources and compare its database-derived report against the reviewed PDF.
