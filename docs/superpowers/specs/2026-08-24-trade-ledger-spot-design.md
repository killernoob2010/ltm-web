# 贸易台账管理—现货业务台账管理技术设计

版本：V1.0
日期：2026-08-24
状态：Gate A/设计已确认，进入本地实现
业务依据：`/Users/wangjingze/Documents/ChatGPT/贸易处台账/outputs/贸易台账管理_现货业务台账管理_业务需求说明书_V1.0.docx`
实现边界：当前隔离 feature worktree；仅本地和明确的非生产 fixture，不进入 main、Production 或正式数据。

## 1. 目标与边界

本模块在现有轻量化交易管理系统中增加一级导航“贸易台账管理”和二级页面“现货业务台账管理”。页面以销售合同商品明细为记录粒度，接收销售合同跟踪表的“现货 + 生效”数据，维护现货业务台账 A:AY 51 个字段，同时保留人工补录、待补录、同步异常、历史迁移、战略套保手工记录和 Excel 导出。

本期明确不做实时同步、手动立即同步、字段修改审计、待历史匹配、复杂部分套保、期货系统接入、真实交易操作、生产环境变量、生产数据库、正式数据迁移或 Production 部署。

无人值守接口目前只确认登录浏览器会话内可调用，未取得后端可持续刷新或服务账号方案。实现真实 HTTP 适配器时必须使用外部提供的已验证 request/response profile；profile 或认证缺失时任务以 `auth_unavailable` 明确失败，不猜请求协议、不读取网页 DOM、不把 fixture 结果标为真实同步。该限制是上线阻塞，不阻止本地开发和 fixture 验收。

## 2. Gate A 评估与验收

- D3：一个业务模块，包含字段规则、持久化、API、页面、调度和一个外部源适配器；没有第二个独立业务模块或新系统边界，因此不是 D4。
- T3：覆盖源适配、业务规则、数据库、API 和本地真实页面；不形成跨模块业务流程，因此不是 T4。
- R3：涉及数据库结构、核心业务口径、权限和外部认证；执行仅限本地/非生产。
- C1：单 Agent inline execution；不创建子 Agent。
- 回滚点：实现前 `76ee22b3cf40f84b8a6bcbaa710f6ed2de444ab7`，当前 worktree 无未提交改动。

### 2.1 验收条目

| ID | 验收要求 | 自动化证据 | 本地页面证据 |
|---|---|---|---|
| TL-NAV-01 | 新增一级/二级导航和现有权限接入 | 模块注册、权限矩阵测试 | 管理员登录后菜单/标题可见 |
| TL-FIELD-01 | A:AY 51 字段全部可展示并有来源属性 | 字段定义数量、映射和计算测试 | 详情页逐字段显示值与控制类型 |
| TL-SYNC-01 | 7 组、现货、生效；明细 ID 幂等；多明细不合并 | fixture 全量扫描和 upsert 测试 | 列表准入结果与数量回读 |
| TL-SYNC-02 | 日期、数量、B05/B09、组别差异和完整扫描软隐藏 | 规则、完整/不完整扫描测试 | 状态、筛选和隐藏行为回读 |
| TL-EDIT-01 | 敏感权限编辑人工字段；系统字段只读；条件必填 | 权限、格式、待补录测试 | 编辑保存后页面状态变化 |
| TL-VIEW-01 | 组合筛选、待补录、同步异常 | 查询和异常 run 测试 | 待补录/同步异常视图可见 |
| TL-MIG-01 | 历史唯一匹配迁移人工字段；无匹配/歧义留空 | 临时 xlsx 迁移测试 | 迁移结果可在详情回读 |
| TL-STRAT-01 | 战略套保完整开平；只支持全开全平 | 手工记录状态和校验测试 | 类型、状态和字段显示 |
| TL-EXPORT-01 | 当前筛选/全部生效 Excel 导出；技术主键显式开关 | xlsx 列顺序和数据回读 | 导出按钮与文件响应 |
| TL-TIME-01 | 面向用户的时间只到秒 | 时间序列化测试 | 同步时间无毫秒 |

## 3. 业务数据模型

### 3.1 记录身份

`spot_ledger_records.record_id` 为模块内部主键。现货记录的业务唯一键为销售合同商品明细 ID，保存于 `source_detail_id` 并加唯一约束；不使用销售合同号单独做主键。战略套保记录使用模块生成的 `strategy:<uuid>` ID，完整开仓/平仓字段保存在同一行。

记录同时保存：

- `record_source_type`：`现货同步` 或 `战略套保`；
- `is_active`：当前有效标识；
- `supplement_status`：`已完成` 或 `待补录`；
- `missing_fields`：JSON 字段名称数组；
- `sync_status`：`正常` 或 `异常`；
- `last_synced_at`、`sync_error_summary`；
- `source_payload_json`：仅保存当前源记录的非敏感标准化快照，不保存凭据。

同步任务另存于 `spot_ledger_sync_runs`，包括 slot、时间、状态、页数、记录数、插入/更新/隐藏数和脱敏错误摘要。任务表不记录人工字段变更历史。

### 3.2 A:AY 51 字段

字段定义由后端单一常量提供给计算、API、导出和前端详情使用。字段代码、名称和控制类型固定如下：

| 列 | 字段 | 控制 | 列 | 字段 | 控制 |
|---|---|---|---|---|---|
| A | 数据来源 | 计算 | Z | 销售价格（元/吨） | 系统 |
| B | 序列 | 计算 | AA | 实物资金收入含税单价 | 手工 |
| C | 建仓 | 手工/必填 | AB | 合同签约客户名称 | 系统 |
| D | 销售类型 | 系统映射 | AC | 最终客户名称 | 手工 |
| E | 销售组别 | 系统 | AD | 销售合同号 | 系统 |
| F | 操作抬头 | 系统转换 | AE | 收款条件 | 手工 |
| G | 采购日期 | 系统 | AF | 销售业务 | 系统 |
| H | 商品名称 | 系统 | AG | 销售执行 | 系统转换 |
| I | 港口 | 系统 | AH | 实物含税盈亏（万元） | 手工 |
| J | 模式 | 系统 | AI | 实物不含税盈亏（万元） | 手工 |
| K | 船名 | 系统优先补录/必填 | AJ | 期货量 | 手工 |
| L | 数量（吨） | 系统计算 | AK | 期货不含税利润（万元） | 手工 |
| M | 采购价格（元/吨） | 系统 | AL | 合计不含税利润（万元） | 手工 |
| N | 实物货物成本含税单价（锁汇） | 手工/必填 | AM | 备注 | 手工 |
| O | 实物资金成本含税单价 | 手工/必填 | AN | 利润跨月因素 | 手工 |
| P | 是否长协 | 条件必填 | AO | 跨月利润确定日期 | 手工 |
| Q | 供应商 | 系统转换 | AP | 利润组别 | 系统 |
| R | 付款条件 | 手工 | AQ | 是否跨组业务 | 计算 |
| S | 采购业务 | 系统 | AR | 上表项目 | 计算 |
| T | 采购执行 | 系统 | AS | 销售月份 | 计算 |
| U | 销售日期 | 系统 | AT | 利润月份 | 计算 |
| V | 客户性质 | 手工 | AU | 商品分类 | 计算/字典 |
| W | 第一次合作钢厂 | 手工 | AV | 利润合计校验 | 计算校验 |
| X | 销售数量（吨） | 系统计算 | AW | 利润日期校验 | 计算校验 |
| Y | 含利息销售价格（元/吨） | 手工/必填 | AX | 业务类型校验 | 计算校验 |
|  |  |  | AY | 客户性质校验 | 计算校验 |

模块新增字段追加在 A:AY 后：销售合同商品明细 ID、长协对象、记录来源类型、当前有效标识、补录状态、缺失字段清单、同步状态、最近同步时间、同步异常摘要、战略套保 ID，以及战略套保的组别、账户、合约/品种、开仓方向、开仓日期时间、开仓数量、数量单位、开仓价格、价格币种/单位、平仓日期时间、平仓数量、平仓价格、关联现货明细 ID和备注。

### 3.3 映射与计算

- `E` 使用量归属组；`AP` 使用业务毛利归属组；两者不一致只计算 `AQ=是`，不产生同步异常。
- `A=业务台账-<销售组别>`；`B` 为同销售组现有最大序列加一，不重排历史空档。
- `U` 统一使用签订日期；`G` 使用资源日期。
- `L` 和 `X`：合同已结案且结算数量有效时使用结算数量，否则使用合同数量。
- `D`：B07→现货-市场加价，B06→现货-背对背，B09/B05→船货-落地；未知值保留原始值并产生转换规则缺失异常。
- `F` 和 `Q` 使用显式维护的名称映射；未命中保留原始全称并生成映射提醒，不截断。
- `AG` 去除 `920109_` 等数字前缀；其他原始值保持。
- `K` 空值及 `--`、`***`、`---`、`**`、`****` 视为缺失；源系统后续有有效船名时覆盖临时人工值。
- `P` 仅船货-落地适用；该类型要求选择是/否，选择是时 `long_contract_object` 必填；其他类型保持空白。
- `AS` 为销售日期所在月首日；`AT` 在有跨月利润确定日期时取该月份，否则取 AS。
- `AU` 使用商品分类字典；未命中显示原值并标记校验异常。
- `AV`、`AW`、`AX`、`AY`为只读校验字段，不把非必填人工空值转成待补录。没有业务适用性时保持空白。

## 4. 来源适配与同步

### 4.1 标准化 source contract

同步核心只接收标准化 `SalesContractRecord` 字典。真实报表字段必须由已经验证的 profile 明确映射到标准字段；核心逻辑不按列序、模糊名称或网页 DOM 猜测字段。fixture 使用同一标准 contract，文件名和运行结果明确标为 `fixture`。

profile-driven HTTP adapter 只支持用户已确认的 POST 候选地址；它要求显式提供 request body、分页路径、记录路径、总数路径、认证提供器和外部字段映射。缺 profile、缺认证或认证过期时返回 `auth_unavailable`，不发起猜测请求。凭据只能从运行时安全环境取得，不写入仓库、前端、导出或日志。

### 4.2 全量扫描门禁

同步每个时点按 7 组、现货、生效条件全量读取分页，验证页序、总页数、总记录数、明细 ID 非空且唯一。只有验证完整成功，且记录处理没有导致整体事务失败，才将本次未出现的现货 ID 标记为 `is_active=0`。接口、鉴权、分页、解析或整体写入失败时保留旧可见记录，并写入异常 run。

同一明细 ID 重复扫描只更新系统字段；人工字段不被常规同步清空。系统优先字段只有在源有有效值时覆盖人工临时值。同步异常记录保留原始标准化值和定位信息，下一周期可重试。

### 4.3 调度

调度时区为 Asia/Shanghai，slot 为每天 09:00、10:00、11:00、12:00、13:00、14:00、15:00、16:00、17:00、18:00。模块启动默认不启用；只有 `SPOT_LEDGER_AUTO_SYNC_ENABLED=true` 且 source mode 配置完整时启动。页面不提供手动立即同步按钮，只显示调度和最近一次结果。

## 5. 权限与 API

模块 code 为 `spot_ledger`，贸易处用户默认可查看/普通编辑权限；人工字段 API 进一步要求 `can_sensitive`，因此只有具备敏感操作权限的贸易处人员和管理员可以修改。查看、筛选、待补录和异常不扩大既有查看权限。编辑不调用操作日志，符合“不做字段修改留痕”；同步 run 与错误摘要仍保留。

主要接口：

- `GET /api/spot-ledger/field-definitions`
- `GET /api/spot-ledger/records`
- `GET /api/spot-ledger/records/{record_id}`
- `PATCH /api/spot-ledger/records/{record_id}`
- `GET /api/spot-ledger/pending`
- `GET /api/spot-ledger/sync-errors`
- `GET /api/spot-ledger/sync-status`
- `GET /api/spot-ledger/export`
- `POST /api/spot-ledger/strategic-hedging`

不提供手动立即同步 API。fixture 同步仅通过本地开发脚本/测试调用，不暴露为业务页面按钮。

## 6. 历史迁移与战略套保

历史导入按销售合同号、商品名称、销售价格、合同数量或结算数量匹配。唯一匹配只迁移人工字段，系统字段取当前源值；无匹配或多候选不迁移人工字段，不产生待历史匹配。`——`等占位值不参与匹配。旧“是否长协”同时含是/否与对象名称时拆成 P 和长协对象，无法确定则留空。迁移命令默认 dry-run，`--apply` 才写入本地/指定非生产数据库。

战略套保一条记录保存完整开仓/平仓字段，只支持全开全平。无平仓信息为“未平仓”；平仓数量等于开仓数量为“已平仓”；数量不一致为“数据异常”。它不适用现货船名、客户和长协必填规则，不自动计算盈亏，期货量与期货不含税利润可为空。

## 7. 验证、发布与已知限制

实现前先为字段映射、数量/日期/类型、完整扫描隐藏、权限/待补录、历史迁移、战略套保、导出和调度写失败测试并确认失败；再逐项实现并复跑。完成后执行 Python 定向/全量测试、Node 前端测试、Python 编译、JavaScript 语法、`git diff --check`，启动本地 SQLite 服务并用管理员登录真实页面完成统一验收。

本轮停在本地验收，不推送 staging，不修改任何 Supabase 数据库。真实合同源无人值守认证、实际来源字段 profile 和真实历史 Excel 文件仍未取得，因此不能宣称真实接口同步或真实历史迁移完成；这三项作为上线前 Gate B 阻塞项保留。
